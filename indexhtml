<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>QMS Pro – Quality Management System</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Syne:wght@600;700;800&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
:root {
  --bg: #080b12;
  --surface: #0d1117;
  --surface2: #131920;
  --surface3: #1a2230;
  --border: #1e2d42;
  --border2: #253348;
  --accent: #4f8ef7;
  --accent-glow: rgba(79,142,247,0.15);
  --accent2: #00e5c3;
  --accent2-glow: rgba(0,229,195,0.12);
  --accent3: #f7b94f;
  --danger: #f75f5f;
  --danger-glow: rgba(247,95,95,0.12);
  --text: #e2e8f6;
  --text2: #6b7a9a;
  --text3: #3a4560;
  --green: #3dd68c;
  --green-glow: rgba(61,214,140,0.12);
  --orange: #f79c4f;
  --purple: #a78bfa;
  --radius: 14px;
  --radius-sm: 9px;
  --radius-xs: 6px;
  --sidebar-w: 248px;
  --shadow: 0 4px 24px rgba(0,0,0,0.4);
  --shadow-sm: 0 2px 10px rgba(0,0,0,0.3);
}

/* ── THEME: Purple Galaxy ── */
[data-theme="purple"] {
  --bg: #0a0812;
  --surface: #110e1a;
  --surface2: #18152a;
  --surface3: #201c35;
  --border: #2a2244;
  --border2: #352c55;
  --accent: #a78bfa;
  --accent-glow: rgba(167,139,250,0.15);
  --accent2: #f472b6;
  --accent2-glow: rgba(244,114,182,0.12);
  --accent3: #fbbf24;
  --text: #ede9fe;
  --text2: #7c6fa0;
  --text3: #3d3460;
  --green: #34d399;
  --green-glow: rgba(52,211,153,0.12);
  --purple: #c084fc;
}

/* ── THEME: Emerald Dark ── */
[data-theme="emerald"] {
  --bg: #061210;
  --surface: #0a1f1c;
  --surface2: #0f2b27;
  --surface3: #153832;
  --border: #1a4a42;
  --border2: #205c52;
  --accent: #10b981;
  --accent-glow: rgba(16,185,129,0.15);
  --accent2: #06d6a0;
  --accent2-glow: rgba(6,214,160,0.12);
  --accent3: #f59e0b;
  --text: #d1fae5;
  --text2: #5a8a7a;
  --text3: #2d5048;
  --green: #34d399;
  --green-glow: rgba(52,211,153,0.12);
  --purple: #a78bfa;
}

/* ── THEME: Midnight Red ── */
[data-theme="red"] {
  --bg: #0f0808;
  --surface: #1a0e0e;
  --surface2: #241414;
  --surface3: #2e1a1a;
  --border: #3d2020;
  --border2: #4d2828;
  --accent: #ef4444;
  --accent-glow: rgba(239,68,68,0.15);
  --accent2: #f97316;
  --accent2-glow: rgba(249,115,22,0.12);
  --accent3: #fbbf24;
  --text: #fee2e2;
  --text2: #8a5a5a;
  --text3: #4d3030;
  --green: #4ade80;
  --green-glow: rgba(74,222,128,0.12);
  --purple: #c084fc;
}

/* ── THEME: Ocean Teal ── */
[data-theme="teal"] {
  --bg: #060e12;
  --surface: #0c1a20;
  --surface2: #12252e;
  --surface3: #18303c;
  --border: #1e3f4e;
  --border2: #255060;
  --accent: #06b6d4;
  --accent-glow: rgba(6,182,212,0.15);
  --accent2: #22d3ee;
  --accent2-glow: rgba(34,211,238,0.12);
  --accent3: #f59e0b;
  --text: #cffafe;
  --text2: #4a7a8a;
  --text3: #254852;
  --green: #4ade80;
  --green-glow: rgba(74,222,128,0.12);
  --purple: #818cf8;
}

/* ── THEME: Sunset Gold ── */
[data-theme="gold"] {
  --bg: #0f0c04;
  --surface: #1a1508;
  --surface2: #261f0c;
  --surface3: #322810;
  --border: #443510;
  --border2: #564218;
  --accent: #f59e0b;
  --accent-glow: rgba(245,158,11,0.15);
  --accent2: #fb923c;
  --accent2-glow: rgba(251,146,60,0.12);
  --accent3: #facc15;
  --text: #fef3c7;
  --text2: #8a7040;
  --text3: #4d3e1a;
  --green: #4ade80;
  --green-glow: rgba(74,222,128,0.12);
  --purple: #c084fc;
}

/* ── THEME: Light Mode ── */
[data-theme="light"] {
  --bg: #f1f5f9;
  --surface: #ffffff;
  --surface2: #f8fafc;
  --surface3: #f1f5f9;
  --border: #e2e8f0;
  --border2: #cbd5e1;
  --accent: #3b82f6;
  --accent-glow: rgba(59,130,246,0.1);
  --accent2: #06b6d4;
  --accent2-glow: rgba(6,182,212,0.1);
  --accent3: #f59e0b;
  --danger: #ef4444;
  --danger-glow: rgba(239,68,68,0.08);
  --text: #0f172a;
  --text2: #64748b;
  --text3: #94a3b8;
  --green: #10b981;
  --green-glow: rgba(16,185,129,0.1);
  --orange: #f97316;
  --purple: #8b5cf6;
  --shadow: 0 4px 24px rgba(0,0,0,0.1);
  --shadow-sm: 0 2px 10px rgba(0,0,0,0.06);
}

/* ── THEME: Slate Pro ── */
[data-theme="slate"] {
  --bg: #0d1117;
  --surface: #161b22;
  --surface2: #1c2128;
  --surface3: #22272e;
  --border: #30363d;
  --border2: #3a424a;
  --accent: #58a6ff;
  --accent-glow: rgba(88,166,255,0.15);
  --accent2: #39d353;
  --accent2-glow: rgba(57,211,83,0.12);
  --accent3: #e3b341;
  --text: #c9d1d9;
  --text2: #6e7681;
  --text3: #3d444d;
  --green: #3fb950;
  --green-glow: rgba(63,185,80,0.12);
  --purple: #bc8cff;
}

/* ── THEME SWITCHER PANEL ── */
.theme-switcher-btn{
  width:34px;height:34px;border-radius:var(--radius-sm);background:var(--surface2);
  border:1px solid var(--border);display:flex;align-items:center;justify-content:center;
  cursor:pointer;font-size:16px;transition:all .2s;position:relative;
}
.theme-switcher-btn:hover{border-color:var(--accent);}
.theme-panel{
  position:absolute;top:calc(100% + 8px);right:0;width:280px;
  background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);
  box-shadow:var(--shadow);z-index:300;display:none;padding:16px;
  animation:fadeIn .2s ease;
}
.theme-panel.open{display:block;}
.theme-panel-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;margin-bottom:12px;}
.theme-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;}
.theme-swatch{
  border-radius:10px;padding:8px 6px;cursor:pointer;border:2px solid transparent;
  transition:all .2s;text-align:center;
}
.theme-swatch:hover{transform:translateY(-2px);}
.theme-swatch.active{border-color:var(--accent);}
.theme-swatch-preview{
  width:100%;height:28px;border-radius:6px;margin-bottom:5px;
}
.theme-swatch-label{font-size:9px;color:var(--text2);font-family:'DM Mono',monospace;}
*{box-sizing:border-box;margin:0;padding:0;}
body{font-family:'DM Sans',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;display:flex;overflow-x:hidden;}

/* ═══ SIDEBAR ═══ */
.sidebar{
  width:var(--sidebar-w);min-height:100vh;background:var(--surface);
  border-right:1px solid var(--border);display:flex;flex-direction:column;
  position:fixed;top:0;left:0;z-index:100;
}
.sidebar-logo{padding:22px 20px 14px;border-bottom:1px solid var(--border);}
.logo-mark{display:flex;align-items:center;gap:10px;}
.logo-icon{width:34px;height:34px;background:linear-gradient(135deg,var(--accent),var(--accent2));border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:16px;font-weight:900;color:#fff;font-family:'Syne',sans-serif;flex-shrink:0;box-shadow:0 0 16px rgba(79,142,247,0.3);}
.logo-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:var(--text);}
.logo-sub{font-family:'DM Mono',monospace;font-size:9px;color:var(--accent2);letter-spacing:2.5px;text-transform:uppercase;margin-top:2px;}
.sidebar-kpis{padding:14px 16px;border-bottom:1px solid var(--border);display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.sk{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:9px 12px;text-align:center;}
.sk-val{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;color:var(--accent);}
.sk-val.green{color:var(--green);}
.sk-lbl{font-size:9px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-top:1px;}
.nav{padding:10px 10px;flex:1;overflow-y:auto;}
.nav-section{font-family:'DM Mono',monospace;font-size:9px;color:var(--text3);letter-spacing:2.5px;text-transform:uppercase;padding:10px 10px 4px;}
.nav-item{
  display:flex;align-items:center;gap:10px;padding:9px 12px;border-radius:var(--radius-sm);
  cursor:pointer;color:var(--text2);font-size:13px;font-weight:500;margin-bottom:1px;
  transition:all .18s;border:1px solid transparent;user-select:none;
}
.nav-item:hover{background:var(--surface2);color:var(--text);border-color:var(--border);}
.nav-item.active{background:rgba(79,142,247,0.1);color:var(--accent);border-color:rgba(79,142,247,0.25);box-shadow:inset 2px 0 0 var(--accent);}
.nav-icon{font-size:15px;width:18px;text-align:center;flex-shrink:0;}
.nav-badge{margin-left:auto;background:var(--accent);color:#fff;font-size:10px;font-family:'DM Mono',monospace;padding:1px 7px;border-radius:20px;min-width:20px;text-align:center;}
.nav-badge.red{background:var(--danger);}
.nav-badge.orange{background:var(--orange);}
.sidebar-footer{padding:14px 16px;border-top:1px solid var(--border);}
.user-chip{display:flex;align-items:center;gap:10px;cursor:pointer;padding:6px;border-radius:var(--radius-sm);transition:background .2s;}
.user-chip:hover{background:var(--surface2);}
.user-avatar{width:32px;height:32px;border-radius:50%;background:linear-gradient(135deg,var(--accent),var(--purple));display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:#fff;flex-shrink:0;}
.user-name{font-size:13px;font-weight:600;color:var(--text);}
.user-role{font-size:10px;color:var(--text2);}
.online-dot{width:8px;height:8px;background:var(--green);border-radius:50%;margin-left:auto;box-shadow:0 0 6px var(--green);}

/* ═══ MAIN ═══ */
.main{margin-left:var(--sidebar-w);flex:1;min-height:100vh;display:flex;flex-direction:column;}
.topbar{
  padding:14px 28px;border-bottom:1px solid var(--border);display:flex;align-items:center;
  justify-content:space-between;background:rgba(13,17,23,0.9);backdrop-filter:blur(12px);
  position:sticky;top:0;z-index:50;
}
.page-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:700;display:flex;align-items:center;gap:10px;}
.page-title-badge{font-family:'DM Mono',monospace;font-size:10px;color:var(--text2);background:var(--surface2);border:1px solid var(--border);padding:2px 8px;border-radius:20px;font-weight:400;}
.topbar-actions{display:flex;gap:8px;align-items:center;}
.topbar-live{font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;display:flex;align-items:center;gap:6px;}
.live-dot{display:inline-block;width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 8px var(--green);animation:pulse 2s infinite;}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1);}50%{opacity:.6;transform:scale(.9);}}

/* Notifications */
.notif-btn{position:relative;width:34px;height:34px;border-radius:var(--radius-sm);background:var(--surface2);border:1px solid var(--border);display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .2s;font-size:16px;}
.notif-btn:hover{border-color:var(--accent);color:var(--accent);}
.notif-count{position:absolute;top:-4px;right:-4px;background:var(--danger);color:#fff;font-size:9px;font-family:'DM Mono',monospace;width:16px;height:16px;border-radius:50%;display:flex;align-items:center;justify-content:center;border:2px solid var(--bg);}
.notif-panel{position:absolute;top:calc(100% + 8px);right:0;width:320px;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);z-index:300;display:none;animation:fadeIn .2s ease;}
.notif-panel.open{display:block;}
.notif-header{padding:14px 16px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
.notif-header-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;}
.notif-mark-all{font-size:11px;color:var(--accent);cursor:pointer;}
.notif-item{padding:12px 16px;border-bottom:1px solid var(--border);display:flex;gap:10px;align-items:flex-start;cursor:pointer;transition:background .15s;}
.notif-item:hover{background:var(--surface2);}
.notif-item.unread{background:rgba(79,142,247,0.05);}
.notif-item:last-child{border-bottom:none;}
.notif-dot{width:8px;height:8px;border-radius:50%;margin-top:5px;flex-shrink:0;}
.notif-title{font-size:13px;color:var(--text);line-height:1.4;}
.notif-meta{font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;}

/* Buttons */
.btn{padding:8px 16px;border-radius:var(--radius-sm);font-size:13px;font-weight:500;cursor:pointer;border:none;transition:all .18s;font-family:'DM Sans',sans-serif;display:inline-flex;align-items:center;gap:6px;}
.btn-primary{background:var(--accent);color:#fff;box-shadow:0 0 16px rgba(79,142,247,0.25);}
.btn-primary:hover{background:#3d7ef0;transform:translateY(-1px);box-shadow:0 0 24px rgba(79,142,247,0.4);}
.btn-ghost{background:var(--surface2);color:var(--text);border:1px solid var(--border);}
.btn-ghost:hover{border-color:var(--accent);color:var(--accent);}
.btn-danger{background:var(--danger-glow);color:var(--danger);border:1px solid rgba(247,95,95,0.25);}
.btn-danger:hover{background:rgba(247,95,95,0.2);}
.btn-success{background:var(--green-glow);color:var(--green);border:1px solid rgba(61,214,140,0.25);}
.btn-success:hover{background:rgba(61,214,140,0.2);}
.btn-teal{background:var(--accent2-glow);color:var(--accent2);border:1px solid rgba(0,229,195,0.25);}
.btn-sm{padding:5px 12px;font-size:12px;}
.dash-preset.active{background:var(--accent);color:#fff;border-color:var(--accent);}
.dash-preset.active:hover{background:var(--accent);}
.btn-xs{padding:3px 9px;font-size:11px;}
.btn-icon{width:32px;height:32px;padding:0;justify-content:center;}

.content{padding:24px 28px;flex:1;}
.page{display:none;animation:fadeIn .25s ease;}
.page.active{display:block;}
@keyframes fadeIn{from{opacity:0;transform:translateY(6px);}to{opacity:1;transform:translateY(0);}}

/* ═══ KPI CARDS ═══ */
.kpi-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:24px;}
.kpi-card{
  background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);
  padding:20px;position:relative;overflow:hidden;transition:all .2s;cursor:default;
}
.kpi-card:hover{transform:translateY(-2px);box-shadow:var(--shadow);}
.kpi-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;}
.kpi-card.blue::before{background:linear-gradient(90deg,var(--accent),var(--purple));}
.kpi-card.green::before{background:linear-gradient(90deg,var(--green),var(--accent2));}
.kpi-card.orange::before{background:linear-gradient(90deg,var(--orange),var(--danger));}
.kpi-card.teal::before{background:linear-gradient(90deg,var(--accent2),var(--accent));}
.kpi-card::after{content:'';position:absolute;top:0;right:0;width:80px;height:80px;border-radius:50%;opacity:.04;transform:translate(20px,-20px);}
.kpi-card.blue::after{background:var(--accent);}
.kpi-card.green::after{background:var(--green);}
.kpi-card.orange::after{background:var(--orange);}
.kpi-card.teal::after{background:var(--accent2);}
.kpi-label{font-size:10px;text-transform:uppercase;letter-spacing:1.5px;color:var(--text2);font-family:'DM Mono',monospace;margin-bottom:10px;}
.kpi-icon{font-size:18px;float:right;opacity:.6;margin-top:-2px;}
.kpi-value{font-family:'Syne',sans-serif;font-size:30px;font-weight:800;line-height:1;}
.kpi-card.blue .kpi-value{color:var(--accent);}
.kpi-card.green .kpi-value{color:var(--green);}
.kpi-card.orange .kpi-value{color:var(--orange);}
.kpi-card.teal .kpi-value{color:var(--accent2);}
.kpi-sub{font-size:11px;color:var(--text2);margin-top:6px;}
.kpi-delta{display:inline-flex;align-items:center;gap:3px;font-size:11px;padding:2px 8px;border-radius:20px;margin-top:8px;font-family:'DM Mono',monospace;font-weight:500;}
.delta-up{background:rgba(61,214,140,0.1);color:var(--green);border:1px solid rgba(61,214,140,0.2);}
.delta-down{background:rgba(247,95,95,0.1);color:var(--danger);border:1px solid rgba(247,95,95,0.2);}

/* ═══ LAYOUT GRIDS ═══ */
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:18px;}
.grid-3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:18px;}
.grid-3-1{display:grid;grid-template-columns:2fr 1fr;gap:18px;}

/* ═══ CARDS ═══ */
.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:20px;margin-bottom:18px;}
.card-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;}
.card-header-left{display:flex;flex-direction:column;gap:2px;}
.card-title{font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:var(--text);}
.card-subtitle{font-size:11px;color:var(--text2);}
.card-actions{display:flex;gap:6px;align-items:center;}

/* ═══ TABLE ═══ */
.table-wrap{overflow-x:auto;}
table{width:100%;border-collapse:collapse;font-size:13px;}
thead th{text-align:left;font-family:'DM Mono',monospace;font-size:9.5px;letter-spacing:1.5px;text-transform:uppercase;color:var(--text2);padding:10px 14px;border-bottom:1px solid var(--border);white-space:nowrap;}
tbody td{padding:11px 14px;border-bottom:1px solid rgba(30,45,66,0.6);color:var(--text);vertical-align:middle;}
tbody tr{transition:background .12s;}
tbody tr:hover{background:var(--surface2);}
tbody tr:last-child td{border-bottom:none;}
.table-sortable th{cursor:pointer;}
.table-sortable th:hover{color:var(--accent);}

/* ═══ BADGES ═══ */
.score-badge{display:inline-flex;align-items:center;justify-content:center;min-width:44px;height:22px;padding:0 8px;border-radius:var(--radius-xs);font-family:'DM Mono',monospace;font-size:11.5px;font-weight:500;}
.score-high{background:rgba(61,214,140,0.12);color:var(--green);border:1px solid rgba(61,214,140,0.2);}
.score-mid{background:rgba(247,185,79,0.12);color:var(--accent3);border:1px solid rgba(247,185,79,0.2);}
.score-low{background:rgba(247,95,95,0.12);color:var(--danger);border:1px solid rgba(247,95,95,0.2);}
.ch-badge{display:inline-flex;align-items:center;gap:4px;font-size:11px;padding:3px 9px;border-radius:20px;font-weight:500;white-space:nowrap;}
.ch-phone{background:rgba(79,142,247,0.1);color:var(--accent);border:1px solid rgba(79,142,247,0.15);}
.ch-chat{background:rgba(0,229,195,0.1);color:var(--accent2);border:1px solid rgba(0,229,195,0.15);}
.ch-email{background:rgba(247,185,79,0.1);color:var(--accent3);border:1px solid rgba(247,185,79,0.15);}
.status-badge{display:inline-flex;align-items:center;gap:5px;font-size:11px;padding:3px 9px;border-radius:20px;}
.status-badge::before{content:'';width:5px;height:5px;border-radius:50%;flex-shrink:0;}
.status-pass{background:rgba(61,214,140,0.1);color:var(--green);border:1px solid rgba(61,214,140,0.2);}
.status-pass::before{background:var(--green);}
.status-fail{background:rgba(247,95,95,0.1);color:var(--danger);border:1px solid rgba(247,95,95,0.2);}
.status-fail::before{background:var(--danger);}
.status-pending{background:rgba(247,185,79,0.1);color:var(--accent3);border:1px solid rgba(247,185,79,0.2);}
.status-pending::before{background:var(--accent3);}

/* ═══ PROGRESS ═══ */
.prog-bar{height:5px;background:var(--surface3);border-radius:3px;overflow:hidden;}
.prog-fill{height:100%;border-radius:3px;transition:width .6s ease;}
.prog-bar-thick{height:8px;}

/* ═══ SCORECARD ═══ */
.sc-section-title{font-family:'Syne',sans-serif;font-size:12.5px;font-weight:700;color:var(--accent);margin-bottom:10px;padding-bottom:8px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
.sc-item{display:flex;align-items:center;padding:9px 0;border-bottom:1px solid rgba(30,45,66,0.5);gap:10px;}
.sc-item:last-child{border-bottom:none;}
.sc-item-label{flex:1;font-size:13px;color:var(--text);}
.sc-item-weight{font-family:'DM Mono',monospace;font-size:10px;color:var(--text2);width:36px;text-align:right;flex-shrink:0;}
.sc-item-score{display:flex;gap:3px;}
.sc-radio-btn{width:34px;height:26px;border-radius:5px;border:1px solid var(--border);background:var(--surface2);color:var(--text2);font-size:11px;font-family:'DM Mono',monospace;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .15s;user-select:none;}
.sc-radio-btn:hover{border-color:var(--accent);color:var(--accent);}
.sc-radio-btn.selected-0{background:rgba(247,95,95,0.15);border-color:var(--danger);color:var(--danger);}
.sc-radio-btn.selected-1{background:rgba(247,185,79,0.15);border-color:var(--accent3);color:var(--accent3);}
.sc-radio-btn.selected-2{background:rgba(61,214,140,0.15);border-color:var(--green);color:var(--green);}
.sc-critical{font-size:9px;color:var(--danger);font-family:'DM Mono',monospace;background:rgba(247,95,95,0.1);padding:1px 5px;border-radius:3px;margin-left:4px;}
.score-live-display{background:var(--surface2);border-radius:var(--radius-sm);padding:16px;display:flex;align-items:center;justify-content:space-between;border:1px solid var(--border);}
.score-live-num{font-family:'Syne',sans-serif;font-size:38px;font-weight:800;}
.score-live-verdict{font-size:13px;font-weight:600;padding:5px 14px;border-radius:20px;}
.score-breakdown{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.sb-item{display:flex;flex-direction:column;gap:5px;}
.sb-label{font-size:11px;color:var(--text2);display:flex;justify-content:space-between;}
.sb-pct{font-family:'DM Mono',monospace;font-size:11px;color:var(--text);}

/* ═══ TABS ═══ */
.tabs{display:flex;gap:3px;background:var(--surface2);padding:3px;border-radius:var(--radius-sm);margin-bottom:18px;width:fit-content;border:1px solid var(--border);}
.tab-btn{padding:6px 16px;border-radius:var(--radius-xs);font-size:12.5px;cursor:pointer;border:none;background:transparent;color:var(--text2);font-family:'DM Sans',sans-serif;font-weight:500;transition:all .18s;}
.tab-btn.active{background:var(--surface3);color:var(--text);box-shadow:0 1px 4px rgba(0,0,0,.3);}

/* ═══ FORMS ═══ */
.form-row{display:grid;gap:12px;margin-bottom:12px;}
.form-row.cols-2{grid-template-columns:1fr 1fr;}
.form-row.cols-3{grid-template-columns:1fr 1fr 1fr;}
.form-group{display:flex;flex-direction:column;gap:5px;}
label{font-size:10px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;font-family:'DM Mono',monospace;}
label .opt{color:var(--accent2);font-size:9px;letter-spacing:0;}
input,select,textarea{background:var(--surface2);border:1px solid var(--border);color:var(--text);border-radius:var(--radius-sm);padding:9px 12px;font-size:13px;font-family:'DM Sans',sans-serif;outline:none;transition:border-color .18s,box-shadow .18s;width:100%;}
input:focus,select:focus,textarea:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-glow);}
input::placeholder,textarea::placeholder{color:var(--text3);}
textarea{resize:vertical;min-height:78px;}
select option{background:var(--surface2);}
.search-wrap{position:relative;}
.search-wrap input{padding-left:34px;}
.search-icon{position:absolute;left:11px;top:50%;transform:translateY(-50%);color:var(--text2);font-size:14px;pointer-events:none;}

/* ═══ SPARKLINES ═══ */
.sparkline{display:flex;align-items:flex-end;gap:3px;height:36px;}
.spark-bar{flex:1;background:var(--accent);border-radius:2px 2px 0 0;opacity:0.5;transition:opacity .2s;min-width:6px;}
.spark-bar:hover{opacity:1;}
.spark-bar.current{opacity:.9;background:var(--accent2);}

/* ═══ DONUT ═══ */
.donut-wrap{display:flex;align-items:center;gap:18px;}
.donut{position:relative;width:86px;height:86px;flex-shrink:0;}
.donut svg{transform:rotate(-90deg);}
.donut-label{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);text-align:center;}
.donut-pct{font-family:'Syne',sans-serif;font-size:17px;font-weight:800;}
.donut-text{font-size:8px;color:var(--text2);}

/* ═══ TIMELINE ═══ */
.timeline{display:flex;flex-direction:column;gap:12px;}
.tl-item{display:flex;gap:12px;align-items:flex-start;}
.tl-dot{width:9px;height:9px;border-radius:50%;margin-top:4px;flex-shrink:0;position:relative;}
.tl-dot::after{content:'';position:absolute;left:4px;top:9px;width:1px;height:calc(100% + 3px);background:var(--border);}
.tl-item:last-child .tl-dot::after{display:none;}
.tl-content{flex:1;}
.tl-title{font-size:13px;color:var(--text);font-weight:500;}
.tl-meta{font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;}

/* ═══ ALERT STRIPS ═══ */
.alert-strip{background:var(--danger-glow);border:1px solid rgba(247,95,95,0.2);border-radius:var(--radius-sm);padding:9px 14px;display:flex;align-items:center;gap:8px;margin-bottom:16px;font-size:13px;color:var(--danger);}
.alert-strip-info{background:var(--accent-glow);border-color:rgba(79,142,247,0.2);color:var(--accent);}
.alert-strip-green{background:var(--green-glow);border-color:rgba(61,214,140,0.2);color:var(--green);}

/* ═══ DISPUTE CARDS ═══ */
.dispute-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:14px;margin-bottom:10px;transition:border-color .2s;}
.dispute-card:hover{border-color:var(--border2);}
.dispute-header{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:8px;}
.dispute-id{font-family:'DM Mono',monospace;font-size:10px;color:var(--accent);}
.dispute-body{font-size:12.5px;color:var(--text2);line-height:1.6;}

/* ═══ MODAL ═══ */
.modal-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);backdrop-filter:blur(6px);z-index:200;align-items:center;justify-content:center;}
.modal-overlay.open{display:flex;}
.modal{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:26px;max-width:600px;width:90%;max-height:88vh;overflow-y:auto;animation:fadeIn .2s ease;position:relative;box-shadow:var(--shadow);}
.modal-title{font-family:'Syne',sans-serif;font-size:17px;font-weight:700;margin-bottom:18px;}
.modal-footer{margin-top:18px;display:flex;gap:8px;justify-content:flex-end;}
.modal-close-x{position:absolute;right:18px;top:18px;cursor:pointer;color:var(--text2);font-size:20px;line-height:1;transition:color .2s;}
.modal-close-x:hover{color:var(--text);}

/* ═══ AGENT DRILL-DOWN MODAL ═══ */
.agent-modal{max-width:720px;}
.agent-modal-header{display:flex;align-items:center;gap:16px;margin-bottom:22px;padding-bottom:18px;border-bottom:1px solid var(--border);}
.agent-modal-avatar{width:52px;height:52px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:18px;font-weight:700;color:#fff;flex-shrink:0;}
.agent-modal-name{font-family:'Syne',sans-serif;font-size:18px;font-weight:700;}
.agent-modal-meta{font-size:12px;color:var(--text2);margin-top:2px;}
.agent-kpi-row{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:18px;}
.agent-kpi{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:12px;text-align:center;}
.agent-kpi-val{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;}
.agent-kpi-lbl{font-size:9px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-top:2px;}

/* ═══ EMAIL MODAL ═══ */
.email-modal-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.8);backdrop-filter:blur(6px);z-index:400;align-items:center;justify-content:center;}
.email-modal-overlay.open{display:flex;}
.email-modal{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);width:700px;max-width:96vw;max-height:92vh;overflow-y:auto;animation:fadeIn .25s ease;display:flex;flex-direction:column;box-shadow:var(--shadow);}
.email-modal-topbar{padding:16px 20px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}
.email-modal-topbar-title{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;display:flex;align-items:center;gap:8px;}
.email-modal-close{cursor:pointer;color:var(--text2);font-size:22px;line-height:1;transition:color .2s;}
.email-modal-close:hover{color:var(--text);}
.email-preview-wrap{margin:16px;background:#fff;border-radius:10px;overflow:hidden;box-shadow:0 8px 32px rgba(0,0,0,.4);}
.ep-header{background:linear-gradient(135deg,#0f2d6e 0%,#1d4ed8 100%);padding:28px 28px 24px;}
.ep-company{font-size:10px;font-weight:700;color:rgba(255,255,255,.45);letter-spacing:3px;text-transform:uppercase;margin-bottom:16px;}
.ep-score-row{display:flex;align-items:flex-end;gap:18px;}
.ep-score-num{font-family:'Syne',sans-serif;font-size:60px;font-weight:800;color:#fff;line-height:1;}
.ep-score-right{padding-bottom:4px;}
.ep-score-label{font-size:12px;color:rgba(255,255,255,.5);}
.ep-badge{display:inline-flex;align-items:center;gap:5px;margin-top:7px;padding:4px 13px;border-radius:20px;font-size:12px;font-weight:700;}
.ep-badge-pass{background:rgba(61,214,140,.2);color:#3dd68c;border:1px solid rgba(61,214,140,.3);}
.ep-badge-warn{background:rgba(247,185,79,.2);color:#f7b94f;border:1px solid rgba(247,185,79,.3);}
.ep-badge-fail{background:rgba(247,95,95,.2);color:#f75f5f;border:1px solid rgba(247,95,95,.3);}
.ep-meta{margin-top:14px;display:flex;gap:18px;flex-wrap:wrap;}
.ep-meta-item{font-size:11px;color:rgba(255,255,255,.4);}
.ep-meta-item strong{color:rgba(255,255,255,.7);}
.ep-body{padding:24px 28px;color:#1e293b;font-family:Georgia,serif;}
.ep-greeting{font-size:15px;font-weight:700;margin-bottom:7px;color:#0f172a;}
.ep-intro{font-size:13px;color:#475569;line-height:1.75;margin-bottom:20px;}
.ep-section-label{font-size:9.5px;text-transform:uppercase;letter-spacing:2px;color:#94a3b8;font-weight:700;font-family:sans-serif;margin-bottom:8px;}
.ep-breakdown{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-bottom:20px;}
.ep-bitem{background:#f8fafc;border:1px solid #e2e8f0;border-radius:8px;padding:11px 13px;font-family:sans-serif;}
.ep-bitem-label{font-size:10px;color:#64748b;margin-bottom:4px;}
.ep-bitem-score{font-size:19px;font-weight:800;color:#0f172a;line-height:1;}
.ep-bitem-max{font-size:10px;color:#94a3b8;}
.ep-bitem-bar{height:3px;background:#e2e8f0;border-radius:2px;margin-top:7px;overflow:hidden;}
.ep-bitem-fill{height:100%;border-radius:2px;transition:width .6s;}
.ep-notes{background:#f8fafc;border:1px solid #e2e8f0;border-radius:8px;padding:12px 14px;margin-bottom:10px;font-family:sans-serif;}
.ep-notes-label{font-size:9px;text-transform:uppercase;letter-spacing:1.5px;color:#64748b;font-weight:700;margin-bottom:5px;}
.ep-notes-text{font-size:13px;color:#334155;line-height:1.65;}
.ep-action{border-radius:8px;padding:12px 14px;margin-bottom:20px;font-family:sans-serif;}
.ep-action-none{background:#f0fdf4;border:1px solid #bbf7d0;}
.ep-action-coach{background:#fff7ed;border:1px solid #fed7aa;}
.ep-action-warn{background:#fef2f2;border:1px solid #fecaca;}
.ep-action-label{font-size:10px;text-transform:uppercase;letter-spacing:1px;font-weight:700;margin-bottom:3px;}
.ep-action-none .ep-action-label{color:#16a34a;}
.ep-action-coach .ep-action-label{color:#ea580c;}
.ep-action-warn .ep-action-label{color:#dc2626;}
.ep-action-text{font-size:13px;line-height:1.6;}
.ep-action-none .ep-action-text{color:#15803d;}
.ep-action-coach .ep-action-text{color:#c2410c;}
.ep-action-warn .ep-action-text{color:#b91c1c;}
.ep-footer{font-size:11px;color:#94a3b8;border-top:1px solid #e2e8f0;padding-top:14px;line-height:1.7;font-family:sans-serif;}
.email-modal-actions{padding:12px 16px;border-top:1px solid var(--border);display:flex;gap:8px;align-items:center;flex-shrink:0;flex-wrap:wrap;}
.email-modal-hint{font-size:11px;color:var(--text2);flex:1;min-width:120px;}
.btn-gmail{background:#ea4335;color:#fff;border:none;}
.btn-gmail:hover{background:#c5221f;}
.btn-outlook{background:#0078d4;color:#fff;border:none;}
.btn-outlook:hover{background:#005a9e;}

/* ═══ TOAST ═══ */
  .toast{position:fixed;bottom:30px;right:30px;background:var(--surface);border:1px solid var(--green);border-radius:var(--radius-sm);padding:13px 22px;color:var(--green);font-size:14px;font-weight:500;z-index:9999;display:none;box-shadow:0 8px 32px rgba(0,0,0,0.6);animation:fadeIn .3s ease;max-width:320px;}
  .toast.show{display:flex;align-items:center;gap:8px;}
  .toast.error{border-color:var(--danger);color:var(--danger);background:var(--surface);}

/* ═══ SCROLLBAR ═══ */
::-webkit-scrollbar{width:5px;height:5px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:var(--border2);border-radius:3px;}
::-webkit-scrollbar-thumb:hover{background:var(--text3);}

/* ═══ ANALYTICS CHARTS ═══ */
.chart-canvas-wrap{position:relative;width:100%;height:160px;}
.chart-canvas-wrap canvas{position:absolute;inset:0;}
.chart-legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:10px;}
.chart-legend-item{display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2);}
.chart-legend-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0;}

/* ═══ MINI STATS ROW ═══ */
.mini-stats-row{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:12px;padding:12px;background:var(--surface2);border-radius:var(--radius-sm);border:1px solid var(--border);}
.mini-stat{text-align:center;}
.mini-stat-val{font-family:'DM Mono',monospace;font-size:16px;font-weight:700;}
.mini-stat-lbl{font-size:9px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-top:2px;}

/* ═══ REPORTS PAGE ═══ */
.report-kpi-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:18px;}
.report-kpi{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:14px;text-align:center;}
.report-kpi-val{font-family:'Syne',sans-serif;font-size:24px;font-weight:800;}
.report-kpi-lbl{font-size:10px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-top:3px;}

/* ═══ EMPTY STATE ═══ */
.empty-state{text-align:center;padding:44px 24px;color:var(--text2);}
.empty-icon{font-size:36px;margin-bottom:10px;opacity:.4;}
.empty-title{font-family:'Syne',sans-serif;font-size:15px;color:var(--text);margin-bottom:5px;}

/* ═══ MISC ═══ */
.separator{height:1px;background:var(--border);margin:14px 0;}
.tag{display:inline-flex;align-items:center;font-size:10px;padding:2px 7px;border-radius:4px;font-family:'DM Mono',monospace;}
.tag-blue{background:var(--accent-glow);color:var(--accent);border:1px solid rgba(79,142,247,0.2);}
.tag-green{background:var(--green-glow);color:var(--green);border:1px solid rgba(61,214,140,0.2);}
.tag-red{background:var(--danger-glow);color:var(--danger);border:1px solid rgba(247,95,95,0.2);}

/* ═══ FILTER BAR ═══ */
.filter-bar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:16px;}
.filter-bar input{background:var(--surface2);border:1px solid var(--border);color:var(--text);border-radius:var(--radius-sm);padding:7px 11px;font-size:12px;outline:none;}
.filter-bar input:focus{border-color:var(--accent);}
.filter-bar select{background:var(--surface2);border:1px solid var(--border);color:var(--text);border-radius:var(--radius-sm);padding:7px 11px;font-size:12px;outline:none;width:auto;min-width:120px;max-width:160px;}
.filter-bar select:focus{border-color:var(--accent);}

/* ═══ LOGIN SCREEN ═══ */
.login-screen{position:fixed;inset:0;background:var(--bg);z-index:10000;display:none;align-items:center;justify-content:center;flex-direction:column;}
.login-box{background:var(--surface);border:1px solid var(--border);border-radius:20px;padding:40px;width:100%;max-width:420px;box-shadow:0 24px 64px rgba(0,0,0,0.6);}
.login-logo{display:flex;align-items:center;gap:12px;margin-bottom:32px;justify-content:center;}
.login-logo-icon{width:44px;height:44px;background:linear-gradient(135deg,var(--accent),var(--accent2));border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:20px;font-weight:900;color:#fff;font-family:'Syne',sans-serif;box-shadow:0 0 20px rgba(79,142,247,0.35);}
.login-logo-title{font-family:'Syne',sans-serif;font-size:22px;font-weight:800;}
.login-logo-sub{font-family:'DM Mono',monospace;font-size:9px;color:var(--accent2);letter-spacing:2px;text-transform:uppercase;}
.login-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:700;margin-bottom:6px;text-align:center;}
.login-sub{font-size:12px;color:var(--text2);text-align:center;margin-bottom:28px;}
.login-form-group{margin-bottom:14px;}
.login-form-group label{display:block;font-size:10px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;font-family:'DM Mono',monospace;margin-bottom:6px;}
.login-form-group input,.login-form-group select{width:100%;background:var(--surface2);border:1px solid var(--border);color:var(--text);border-radius:var(--radius-sm);padding:11px 14px;font-size:14px;font-family:'DM Sans',sans-serif;outline:none;transition:border-color .2s;}
.login-form-group input:focus,.login-form-group select:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-glow);}
.login-btn{width:100%;padding:13px;background:var(--accent);color:#fff;border:none;border-radius:var(--radius-sm);font-size:15px;font-weight:600;font-family:'DM Sans',sans-serif;cursor:pointer;margin-top:8px;transition:all .2s;box-shadow:0 0 20px rgba(79,142,247,0.3);}
.login-btn:hover{background:#3d7ef0;transform:translateY(-1px);}
.login-error{background:var(--danger-glow);border:1px solid rgba(247,95,95,0.3);color:var(--danger);border-radius:var(--radius-sm);padding:10px 14px;font-size:13px;margin-bottom:14px;display:none;}
.login-error.show{display:block;}

/* ═══ ROLE BADGE ═══ */
.role-badge{font-size:9px;font-family:'DM Mono',monospace;padding:2px 7px;border-radius:4px;text-transform:uppercase;letter-spacing:1px;font-weight:600;}
.role-admin{background:rgba(167,139,250,0.15);color:var(--purple);border:1px solid rgba(167,139,250,0.25);}
.role-qa-manager{background:rgba(79,142,247,0.15);color:var(--accent);border:1px solid rgba(79,142,247,0.25);}
.role-qa-specialist{background:rgba(0,229,195,0.15);color:var(--accent2);border:1px solid rgba(0,229,195,0.25);}
.role-tl-cc{background:rgba(247,185,79,0.15);color:var(--accent3);border:1px solid rgba(247,185,79,0.25);}
.role-agent{background:rgba(61,214,140,0.15);color:var(--green);border:1px solid rgba(61,214,140,0.25);}

/* ═══ ACCESS DENIED ═══ */
.access-denied{display:flex;flex-direction:column;align-items:center;justify-content:center;padding:80px 24px;text-align:center;}
.access-denied-icon{font-size:48px;margin-bottom:16px;opacity:.5;}
.access-denied-title{font-family:'Syne',sans-serif;font-size:18px;font-weight:700;margin-bottom:8px;color:var(--text);}
.access-denied-sub{font-size:13px;color:var(--text2);}

/* ═══ ROLE MANAGER (Admin Page) ═══ */
.role-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:16px;margin-bottom:10px;display:flex;align-items:center;gap:14px;transition:border-color .2s;}
.role-card:hover{border-color:var(--border2);}
.role-avatar{width:40px;height:40px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:700;color:#fff;flex-shrink:0;}
/* ═══ WORKSPACE SYSTEM ═══ */
.ws-selector{position:relative;display:flex;align-items:center;}
.ws-btn{display:flex;align-items:center;gap:8px;padding:6px 12px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer;transition:all .2s;max-width:200px;}
.ws-btn:hover{border-color:var(--accent);}
.ws-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0;}
.ws-btn-name{font-size:12px;font-weight:600;color:var(--text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:120px;}
.ws-btn-arrow{font-size:10px;color:var(--text2);margin-left:2px;}
.ws-panel{position:absolute;top:calc(100% + 8px);left:0;width:300px;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);z-index:400;display:none;animation:fadeIn .2s ease;}
.ws-panel.open{display:block;}
.ws-panel-header{padding:14px 16px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center;}
.ws-panel-title{font-family:'Syne',sans-serif;font-size:13px;font-weight:700;}
.ws-list{max-height:260px;overflow-y:auto;}
.ws-item{display:flex;align-items:center;gap:10px;padding:11px 16px;cursor:pointer;transition:background .15s;border-bottom:1px solid rgba(30,45,66,0.4);}
.ws-item:hover{background:var(--surface2);}
.ws-item.active{background:var(--accent-glow);}
.ws-item-icon{width:34px;height:34px;border-radius:9px;display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0;}
.ws-item-name{font-size:13px;font-weight:600;color:var(--text);}
.ws-item-meta{font-size:10px;color:var(--text2);margin-top:1px;}
.ws-item-check{margin-left:auto;color:var(--accent);font-size:14px;}
.ws-panel-footer{padding:10px 12px;border-top:1px solid var(--border);}

/* Workspace management page */
.ws-card{background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:18px;display:flex;align-items:flex-start;gap:14px;transition:all .2s;cursor:default;}
.ws-card:hover{border-color:var(--border2);transform:translateY(-1px);box-shadow:var(--shadow-sm);}
.ws-card-icon{width:44px;height:44px;border-radius:12px;display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0;}
.ws-card-name{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;}
.ws-card-type{font-size:10px;font-family:'DM Mono',monospace;text-transform:uppercase;letter-spacing:1px;margin-top:2px;}
.ws-card-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px;}
.ws-card-stat{background:var(--surface);border:1px solid var(--border);border-radius:7px;padding:8px;text-align:center;}
.ws-card-stat-val{font-family:'Syne',sans-serif;font-size:16px;font-weight:800;}
.ws-card-stat-lbl{font-size:9px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-top:1px;}
.ws-overview-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:20px;}
@media(max-width:900px){.ws-overview-grid{grid-template-columns:repeat(2,1fr);}}
/* ═══ BULK UPLOAD ═══ */
#bulk-dropzone.drag-over{border-color:var(--accent);background:var(--accent-glow);}
#bulk-dropzone:hover{border-color:var(--border2);background:var(--surface2);}
.bulk-error-row td{color:var(--danger)!important;background:rgba(247,95,95,0.05);}
</style>
</head>
<body>

<!-- ═══════════════ LOGIN SCREEN ═══════════════ -->
<div class="login-screen" id="login-screen">
  <div class="login-box">
    <div class="login-logo">
      <div class="login-logo-icon">Q</div>
      <div>
        <div class="login-logo-title">QMS Pro</div>
        <div class="login-logo-sub">Quality Management</div>
      </div>
    </div>
    <div class="login-title">Welcome Back</div>
    <div class="login-sub">Sign in to your account</div>
    <div class="login-error" id="login-error">Invalid username or password</div>
    <div class="login-form-group">
      <label>Username or Email</label>
      <input type="text" id="login-user" placeholder="Enter your username or email" autocomplete="off"/>
    </div>
    <div class="login-form-group">
      <label>Password</label>
      <input type="password" id="login-pass" placeholder="Enter your password" onkeydown="if(event.key==='Enter')doLogin()"/>
    </div>
    <button class="login-btn" onclick="doLogin()">Sign In →</button>
  </div>
</div>

<!-- ═══════════════ SIDEBAR ═══════════════ -->
<aside class="sidebar">
  <div class="sidebar-logo">
    <div class="logo-mark">
      <div class="logo-icon">Q</div>
      <div>
        <div class="logo-title">QMS Pro</div>
        <div class="logo-sub">Quality Mgmt</div>
      </div>
    </div>
  </div>
  <div class="sidebar-kpis">
    <div class="sk"><div class="sk-val" id="sb-spec-count">0</div><div class="sk-lbl">QA Specs</div></div>
    <div class="sk"><div class="sk-val green" id="sb-agent-count">0</div><div class="sk-lbl">Agents</div></div>
  </div>
  <nav class="nav">
    <div class="nav-section">Overview</div>
    <div class="nav-item active" onclick="showPage('dashboard',this)"><span class="nav-icon">📊</span>Dashboard</div>

    <div class="nav-section">Evaluation</div>
    <div class="nav-item" onclick="showPage('scorecard',this)"><span class="nav-icon">📋</span>Scorecard</div>
    <div class="nav-item" onclick="showPage('sc-builder',this)"><span class="nav-icon">🛠️</span>Scorecard Builder</div>
    <div class="nav-item" onclick="showPage('tracker',this)"><span class="nav-icon">📈</span>QA Tracker<span class="nav-badge orange" id="tracker-badge">12</span></div>

    <div class="nav-section">Feedback</div>
    <div class="nav-item" onclick="showPage('feedback',this)"><span class="nav-icon">💬</span>Agent Feedback</div>
    <div class="nav-item" onclick="showPage('calibration',this)"><span class="nav-icon">⚖️</span>Calibration</div>
    <div class="nav-item" onclick="showPage('disputes',this)"><span class="nav-icon">🚩</span>Disputes<span class="nav-badge red" id="dispute-badge">3</span></div>

    <div class="nav-section">Team</div>
    <div class="nav-item" onclick="showPage('agents',this)"><span class="nav-icon">👥</span>Agents</div>
    <div class="nav-item" onclick="showPage('specialists',this)"><span class="nav-icon">🎯</span>QA Specialists</div>
    <div class="nav-item" onclick="showPage('team-mgmt',this)"><span class="nav-icon">⚙️</span>Team Management</div>

    <div class="nav-section">Insights</div>
    <div class="nav-item" onclick="showPage('reports',this)"><span class="nav-icon">📉</span>Reports & Analytics</div>

    <div class="nav-section">Admin</div>
    <div class="nav-item" onclick="showPage('workspaces',this)"><span class="nav-icon">🏢</span>Workspaces</div>
    <div class="nav-item" onclick="showPage('team-mgmt',this)"><span class="nav-icon">👤</span>Team Management</div>
    <div class="nav-item" onclick="showPage('role-mgmt',this)"><span class="nav-icon">🔐</span>Access & Roles</div>
  </nav>
  <div class="sidebar-footer">
    <div class="user-chip">
      <div class="user-avatar" id="sb-avatar">QM</div>
      <div style="flex:1;min-width:0;">
        <div class="user-name" id="sb-username">QA Manager</div>
        <div class="user-role" id="sb-role">Admin · All channels</div>
      </div>
    </div>
    <button onclick="doLogout()" style="width:100%;margin-top:10px;padding:9px;background:rgba(247,95,95,0.08);border:1px solid rgba(247,95,95,0.2);color:var(--danger);border-radius:var(--radius-sm);font-size:12px;font-weight:600;font-family:'DM Sans',sans-serif;cursor:pointer;transition:all .2s;display:flex;align-items:center;justify-content:center;gap:6px;">
      <span>⏻</span> Sign Out
    </button>
  </div>
</aside>

<!-- ═══════════════ MAIN ═══════════════ -->
<main class="main">
  <div class="topbar">
    <div class="page-title" id="page-title">Dashboard <span class="page-title-badge" id="page-title-badge">March 2026</span></div>
    <div class="topbar-actions">
      <!-- Workspace Switcher Dropdown in Topbar -->
      <div style="position:relative;" id="topbar-ws-wrap">
        <div style="display:flex;align-items:center;gap:4px;">
          <div id="topbar-ws-btn" onclick="toggleWsPanel()" style="display:flex;align-items:center;gap:8px;padding:6px 14px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer;transition:all .2s;min-width:160px;max-width:200px;" title="Switch workspace">
            <span id="topbar-ws-dot" style="width:8px;height:8px;border-radius:50%;background:var(--accent);flex-shrink:0;box-shadow:0 0 6px var(--accent);"></span>
            <span id="topbar-ws-name" style="font-size:12px;font-weight:600;color:var(--text);flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">All Workspaces</span>
            <span style="font-size:9px;color:var(--text2);">▼</span>
          </div>
          <button id="topbar-ws-edit-btn" onclick="editCurrentWorkspace()" style="display:none;padding:6px 10px;background:var(--surface2);border:1px solid rgba(79,142,247,0.3);border-radius:var(--radius-sm);color:var(--accent);font-size:12px;cursor:pointer;transition:all .2s;white-space:nowrap;align-items:center;gap:4px;" title="Edit current workspace">✏️ Edit</button>
        </div>
        <div id="ws-panel" class="ws-panel">
          <div class="ws-panel-header">
            <div class="ws-panel-title">🏢 Switch Workspace</div>
            <button class="btn btn-primary btn-xs" onclick="closeWsPanel();showPage('workspaces',null)">Manage All</button>
          </div>
          <div class="ws-list" id="ws-list"></div>
          <div class="ws-panel-footer">
            <button class="btn btn-ghost btn-sm" style="width:100%;margin-bottom:6px;" onclick="editCurrentWorkspace()" id="ws-edit-btn">✏️ Edit Current Workspace</button>
            <button class="btn btn-ghost btn-sm" style="width:100%;margin-bottom:6px;" onclick="closeWsPanel();showPage('workspaces',null)">🏢 Manage All Workspaces</button>
            <button class="btn btn-primary btn-sm" style="width:100%;" onclick="closeWsPanel();showPage('workspaces',null);setTimeout(()=>openAddWorkspace(),100)">+ New Workspace</button>
          </div>
        </div>
      </div>
      <span class="role-badge" id="topbar-role-badge">Admin</span>
      <div class="topbar-live"><span class="live-dot"></span>Live · Mar 29, 2026</div>
      <button id="topbar-logout-btn" onclick="doLogout()" style="padding:6px 10px;background:rgba(247,95,95,0.08);border:1px solid rgba(247,95,95,0.2);color:var(--danger);border-radius:var(--radius-sm);font-size:12px;font-weight:600;font-family:'DM Sans',sans-serif;cursor:pointer;display:flex;align-items:center;gap:5px;" title="Sign out">
        <span>⏻</span> Logout
      </button>
      <div style="position:relative;">
        <div class="theme-switcher-btn" onclick="toggleThemePanel()" title="Change theme">🎨</div>
        <div class="theme-panel" id="theme-panel">
          <div class="theme-panel-title">🎨 Choose Theme</div>
          <div class="theme-grid" id="theme-grid"></div>
        </div>
      </div>
      <div style="position:relative;">
        <div class="notif-btn" onclick="toggleNotif()" title="Notifications">🔔<span class="notif-count" id="notif-count">5</span></div>
        <div class="notif-panel" id="notif-panel">
          <div class="notif-header">
            <div class="notif-header-title">Notifications</div>
            <div class="notif-mark-all" onclick="markAllRead()">Mark all read</div>
          </div>
          <div class="notif-item unread" onclick="markOneRead(this)">
            <div class="notif-dot" style="background:var(--danger);"></div>
            <div><div class="notif-title">Ahmed Hassan — 3rd consecutive fail this month</div><div class="notif-meta">Score: 62% · Phone · 2h ago</div></div>
          </div>
          <div class="notif-item unread" onclick="markOneRead(this)">
            <div class="notif-dot" style="background:var(--orange);"></div>
            <div><div class="notif-title">Calibration variance alert: Omar Tarek ±9.8%</div><div class="notif-meta">Exceeds 5% threshold · 4h ago</div></div>
          </div>
          <div class="notif-item unread" onclick="markOneRead(this)">
            <div class="notif-dot" style="background:var(--accent);"></div>
            <div><div class="notif-title">New dispute filed: DISP-2026-003</div><div class="notif-meta">Ahmed Hassan · 6h ago</div></div>
          </div>
          <div class="notif-item unread" onclick="markOneRead(this)">
            <div class="notif-dot" style="background:var(--green);"></div>
            <div><div class="notif-title">Monthly target 95% achieved — 1,284 evals</div><div class="notif-meta">Well done team · Today</div></div>
          </div>
          <div class="notif-item unread" onclick="markOneRead(this)">
            <div class="notif-dot" style="background:var(--accent2);"></div>
            <div><div class="notif-title">Coaching follow-up due: Yara Mostafa</div><div class="notif-meta">PIP Review · Apr 5 · Overdue</div></div>
          </div>
        </div>
      </div>
      <button class="btn btn-ghost btn-sm" onclick="exportCSV()">↓ Export</button>
      <button class="btn btn-primary" onclick="openNewEval()">＋ New Evaluation</button>
    </div>
  </div>

  <div class="content">

    <!-- ═══ DASHBOARD ═══ -->
    <div id="page-dashboard" class="page active">

      <!-- Date Filter Bar -->
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:16px;flex-wrap:wrap;">
        <div style="display:flex;align-items:center;gap:6px;">
          <span style="font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;">FROM</span>
          <input type="date" id="dash-date-from" onchange="applyDashDateFilter()" style="font-size:12px;padding:5px 10px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);color:var(--text);"/>
        </div>
        <div style="display:flex;align-items:center;gap:6px;">
          <span style="font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;">TO</span>
          <input type="date" id="dash-date-to" onchange="applyDashDateFilter()" style="font-size:12px;padding:5px 10px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);color:var(--text);"/>
        </div>
        <div style="display:flex;gap:5px;flex-wrap:wrap;">
          <button class="btn btn-ghost btn-sm dash-preset active" onclick="setDashPreset('all',this)">All Time</button>
          <button class="btn btn-ghost btn-sm dash-preset" onclick="setDashPreset('today',this)">Today</button>
          <button class="btn btn-ghost btn-sm dash-preset" onclick="setDashPreset('7d',this)">Last 7D</button>
          <button class="btn btn-ghost btn-sm dash-preset" onclick="setDashPreset('30d',this)">Last 30D</button>
          <button class="btn btn-ghost btn-sm dash-preset" onclick="setDashPreset('month',this)">This Month</button>
        </div>
        <div id="dash-date-label" style="margin-left:auto;font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;"></div>
      </div>

      <div id="dashboard-content">
        <!-- Rendered dynamically by renderDashboard() -->
      </div>
    </div>
    <!-- ═══ SCORECARD ═══ -->
    <div id="page-scorecard" class="page">
      <div class="alert-strip alert-strip-info" style="justify-content:space-between;">
        <span>ℹ️ Complete all sections. Fatal items (★) cause automatic score deduction when marked. Scored items total 100 pts. Target: ≥ 80%</span>
        <button class="btn btn-ghost btn-sm" style="flex-shrink:0;margin-left:10px;" onclick="showPage('sc-builder',null)">🛠️ Edit Scorecard</button>
      </div>
      <div class="grid-2" style="align-items:start;">
        <div>
          <!-- Eval Info -->
          <div class="card">
            <div class="card-title" style="margin-bottom:14px;">Evaluation Info</div>
            <!-- Workspace + Agent selectors -->
            <div class="form-row cols-2" style="margin-bottom:4px;">
              <div class="form-group">
                <label>Workspace <span style="color:var(--danger);">*</span></label>
                <select id="sc-workspace" onchange="onScWsChange()" style="width:100%;">
                  <option value="">— Select workspace —</option>
                </select>
              </div>
              <div class="form-group">
                <label>Agent <span style="color:var(--danger);">*</span></label>
                <select id="sc-agent-select" onchange="onScAgentSelect()" style="width:100%;">
                  <option value="">— Select agent —</option>
                </select>
              </div>
            </div>
            <div class="form-row cols-2">
              <div class="form-group"><label>Agent Name</label><input type="text" id="sc-agent" placeholder="Agent name..." oninput="updateScore()"/></div>
              <div class="form-group"><label>Agent Email <span class="opt">📧 for notification</span></label><input type="email" id="sc-agent-email" placeholder="agent@company.com"/></div>
            </div>
            <div class="form-row cols-2">
              <div class="form-group"><label>QA Specialist</label>
                <select id="sc-specialist" onchange="updateScore()">
                  <option value="">Select specialist</option>
                  <option>Sara Ahmed</option><option>Mohamed Karim</option><option>Lina Rashid</option>
                  <option>Omar Tarek</option><option>Rania Fouad</option><option>Khaled Mostafa</option>
                </select>
              </div>
              <div class="form-group"><label>Date</label><input type="date" id="sc-date" oninput="updateScore()"/></div>
            </div>
            <div class="form-row cols-2">
              <div class="form-group"><label>Channel</label>
                <select id="sc-channel" onchange="updateScore()">
                  <option value="">Select channel</option>
                  <option>📞 Phone</option><option>💬 Live Chat</option><option>📧 Email</option>
                </select>
              </div>
              <div class="form-group"><label>Interaction ID</label><input type="text" id="sc-intid" placeholder="e.g. CALL-2026-..." oninput="updateScore()"/></div>
            </div>
          </div>

          <!-- SECTION 1: Soft Skills – Call Opening & Closure (20 pts) -->
          <div class="card">
            <div class="sc-section-title">
              <span>1. Soft Skills — Call Opening &amp; Closure</span>
              <span style="font-size:11px;color:var(--text2);background:var(--surface2);padding:2px 8px;border-radius:4px;">20 pts</span>
            </div>
            <div style="font-size:10px;color:var(--accent3);margin-bottom:10px;padding:6px 10px;background:rgba(247,185,79,0.06);border:1px solid rgba(247,185,79,0.15);border-radius:6px;">
              ⚠️ Deducting this item removes the full <strong>20 pts</strong> from the score
            </div>
            <div class="sc-item" data-key="consent">
              <div class="sc-item-label">Customer Consent, Opening Clarity &amp; Closure Structure<br/><span style="font-size:11px;color:var(--text2);">Agent confirms readiness, greets properly, introduces self &amp; page, closes professionally</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−20 pts</div>
              <div class="sc-item-score" id="btn-consent"></div>
            </div>
          </div>

          <!-- SECTION 2: Soft Skills – Attitude & Tone (25 pts) -->
          <div class="card">
            <div class="sc-section-title">
              <span>2. Soft Skills — Attitude &amp; Tone</span>
              <span style="font-size:11px;color:var(--text2);background:var(--surface2);padding:2px 8px;border-radius:4px;">25 pts</span>
            </div>
            <div style="font-size:10px;color:var(--accent3);margin-bottom:10px;padding:6px 10px;background:rgba(247,185,79,0.06);border:1px solid rgba(247,185,79,0.15);border-radius:6px;">
              ⚠️ Deducting <strong>any</strong> item below removes the full <strong>25 pts</strong> from the score
            </div>
            <div class="sc-item" data-key="listening">
              <div class="sc-item-label">Active Listening<br/><span style="font-size:11px;color:var(--text2);">Giving space to talk, no interruption</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−25 pts</div>
              <div class="sc-item-score" id="btn-listening"></div>
            </div>
            <div class="sc-item" data-key="professional">
              <div class="sc-item-label">Professional Language<br/><span style="font-size:11px;color:var(--text2);">No negative words, slangs, repetition, verbal nods, rephrasing</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−25 pts</div>
              <div class="sc-item-score" id="btn-professional"></div>
            </div>
            <div class="sc-item" data-key="empathy">
              <div class="sc-item-label">Empathy &amp; Tone Behavior<br/><span style="font-size:11px;color:var(--text2);">Showing empathy, not talking fast/sleepy/aggressive</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−25 pts</div>
              <div class="sc-item-score" id="btn-empathy"></div>
            </div>
            <div class="sc-item" data-key="concentration">
              <div class="sc-item-label">Concentration &amp; Dead Air<br/><span style="font-size:11px;color:var(--text2);">Focused on call, no silent moments</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−25 pts</div>
              <div class="sc-item-score" id="btn-concentration"></div>
            </div>
          </div>

          <!-- SECTION 3: Soft Skills – Hold Process (15 pts) -->
          <div class="card">
            <div class="sc-section-title">
              <span>3. Soft Skills — Hold Process</span>
              <span style="font-size:11px;color:var(--text2);background:var(--surface2);padding:2px 8px;border-radius:4px;">15 pts</span>
            </div>
            <div style="font-size:10px;color:var(--accent3);margin-bottom:10px;padding:6px 10px;background:rgba(247,185,79,0.06);border:1px solid rgba(247,185,79,0.15);border-radius:6px;">
              ⚠️ Deducting this item removes the full <strong>15 pts</strong> from the score
            </div>
            <div class="sc-item" data-key="hold">
              <div class="sc-item-label">Hold Process Followed<br/><span style="font-size:11px;color:var(--text2);">Agent followed correct hold procedure when placing customer on hold</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−15 pts</div>
              <div class="sc-item-score" id="btn-hold"></div>
            </div>
          </div>

          <!-- SECTION 4: Sales Expertise (Fatal) -->
          <div class="card">
            <div class="sc-section-title">
              <span>4. Sales Expertise</span>
              <span style="font-size:11px;background:rgba(247,95,95,0.12);color:var(--danger);padding:2px 8px;border-radius:4px;border:1px solid rgba(247,95,95,0.2);">★ Fatal Items</span>
            </div>
            <div style="font-size:10px;color:var(--text2);margin-bottom:10px;padding:6px 10px;background:rgba(247,95,95,0.05);border:1px solid rgba(247,95,95,0.1);border-radius:6px;line-height:1.6;">
              Any violation flagged here results in automatic fail. Mark <strong>YES ★</strong> only if the violation occurred.
            </div>
            <div class="sc-item" data-key="alt-suggestion" data-fatal="true">
              <div class="sc-item-label">Alternative Suggestion <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not suggest an alternative when the offered product was unsuitable</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-alt-suggestion"></div>
            </div>
            <div class="sc-item" data-key="save-attempt" data-fatal="true">
              <div class="sc-item-label">Save Attempt <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">No final attempt to retain the deal before it was lost</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-save-attempt"></div>
            </div>
            <div class="sc-item" data-key="relevance" data-fatal="true">
              <div class="sc-item-label">Relevance of Response <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Response to objection was inappropriate, illogical or off-topic</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-relevance"></div>
            </div>
            <div class="sc-item" data-key="value" data-fatal="true">
              <div class="sc-item-label">Value Explanation <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not focus on benefits/value — only listed features</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-value"></div>
            </div>
            <div class="sc-item" data-key="followup" data-fatal="true">
              <div class="sc-item-label">Follow-up Planning <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not handle WhatsApp/promise/follow-up requests correctly</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-followup"></div>
            </div>
            <div class="sc-item" data-key="policy" data-fatal="true">
              <div class="sc-item-label">Policy Knowledge <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not explain company policies clearly</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-policy"></div>
            </div>
            <div class="sc-item" data-key="push-agreement" data-fatal="true">
              <div class="sc-item-label">Pushing for Agreement <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Pressured the customer to confirm — mark if occurred</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-push-agreement"></div>
            </div>
            <div class="sc-item" data-key="warranty" data-fatal="true">
              <div class="sc-item-label">Warranty Not Mentioned <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not mention the warranty process at all</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-warranty"></div>
            </div>
          </div>

          <!-- SECTION 5: Call Performance (40 pts) -->
          <div class="card">
            <div class="sc-section-title">
              <span>5. Call Performance</span>
              <span style="font-size:11px;color:var(--text2);background:var(--surface2);padding:2px 8px;border-radius:4px;">40 pts</span>
            </div>
            <div style="font-size:10px;color:var(--accent3);margin-bottom:10px;padding:6px 10px;background:rgba(247,185,79,0.06);border:1px solid rgba(247,185,79,0.15);border-radius:6px;">
              ⚠️ Deducting <strong>any</strong> item below removes the full <strong>40 pts</strong> from the score
            </div>
            <div class="sc-item" data-key="alt-number">
              <div class="sc-item-label">Alternative Number<br/><span style="font-size:11px;color:var(--text2);">Requested additional number for easier communication</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-alt-number"></div>
            </div>
            <div class="sc-item" data-key="return-policy">
              <div class="sc-item-label">Return Policy Explained<br/><span style="font-size:11px;color:var(--text2);">Clearly explained the return policy to customer</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-return-policy"></div>
            </div>
            <div class="sc-item" data-key="item-desc">
              <div class="sc-item-label">Item Description Clarity<br/><span style="font-size:11px;color:var(--text2);">Explained color, name, quantity, price of the order</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-item-desc"></div>
            </div>
            <div class="sc-item" data-key="delivery">
              <div class="sc-item-label">Delivery Details Provided<br/><span style="font-size:11px;color:var(--text2);">Mentioned the delivery method and time</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-delivery"></div>
            </div>
            <div class="sc-item" data-key="address">
              <div class="sc-item-label">Address Obtained<br/><span style="font-size:11px;color:var(--text2);">Obtained the customer's address accurately</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-address"></div>
            </div>
            <div class="sc-item" data-key="bad-attitude">
              <div class="sc-item-label">Bad Attitude<br/><span style="font-size:11px;color:var(--text2);">Any inappropriate behavior — marked if occurred</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-bad-attitude"></div>
            </div>
            <div class="sc-item" data-key="correct-action">
              <div class="sc-item-label">Correct Action in System<br/><span style="font-size:11px;color:var(--text2);">Selected the correct interaction type in the system</span></div>
              <div class="sc-item-weight" style="color:var(--danger);font-size:10px;">−40 pts</div>
              <div class="sc-item-score" id="btn-correct-action"></div>
            </div>
          </div>

          <!-- SECTION 6: Documentation (0 pts — Fatal) -->
          <div class="card">
            <div class="sc-section-title">
              <span>6. Documentation</span>
              <span style="font-size:11px;background:rgba(247,95,95,0.12);color:var(--danger);padding:2px 8px;border-radius:4px;border:1px solid rgba(247,95,95,0.2);">★ Fatal Items</span>
            </div>
            <div style="font-size:10px;color:var(--text2);margin-bottom:10px;padding:6px 10px;background:rgba(247,95,95,0.05);border:1px solid rgba(247,95,95,0.1);border-radius:6px;">
              These items trigger automatic disqualification if flagged. Mark only if the violation occurred.
            </div>
            <div class="sc-item" data-key="notes" data-fatal="true">
              <div class="sc-item-label">Notes Quality <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Did not write clear notes (what happened – insights – comments)</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-notes"></div>
            </div>
            <div class="sc-item" data-key="fake-delivery" data-fatal="true">
              <div class="sc-item-label">Fake Delivery Time <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Provided incorrect/misleading delivery time or SLA</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-delivery"></div>
            </div>
          </div>

          <!-- SECTION 7: Fake Info & Actions (0 pts — Fatal) -->
          <div class="card">
            <div class="sc-section-title">
              <span>7. Fake Info &amp; Actions</span>
              <span style="font-size:11px;background:rgba(247,95,95,0.12);color:var(--danger);padding:2px 8px;border-radius:4px;border:1px solid rgba(247,95,95,0.2);">★ Fatal Items</span>
            </div>
            <div style="font-size:10px;color:var(--text2);margin-bottom:10px;padding:6px 10px;background:rgba(247,95,95,0.05);border:1px solid rgba(247,95,95,0.1);border-radius:6px;">
              Any of the below result in automatic fail regardless of total score.
            </div>
            <div class="sc-item" data-key="fake-confirm" data-fatal="true">
              <div class="sc-item-label">Fake Confirmation <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Confirmed order without proof (no call / no WhatsApp)</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-confirm"></div>
            </div>
            <div class="sc-item" data-key="fake-price" data-fatal="true">
              <div class="sc-item-label">Fake Price <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Gave wrong/inaccurate price not matching system or offer</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-price"></div>
            </div>
            <div class="sc-item" data-key="fake-specs" data-fatal="true">
              <div class="sc-item-label">Fake Specs <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Gave wrong/misleading product specifications or features</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-specs"></div>
            </div>
            <div class="sc-item" data-key="fake-product" data-fatal="true">
              <div class="sc-item-label">Fake Product <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Described a different product than the one actually offered</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-product"></div>
            </div>
            <div class="sc-item" data-key="fake-warranty" data-fatal="true">
              <div class="sc-item-label">Fake Warranty <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Gave incorrect information about warranty duration</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-warranty"></div>
            </div>
            <div class="sc-item" data-key="fake-agreement" data-fatal="true">
              <div class="sc-item-label">Fake Agreement <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Confirmed order without clear customer permission</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-agreement"></div>
            </div>
            <div class="sc-item" data-key="fake-offers" data-fatal="true">
              <div class="sc-item-label">Fake Offers <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Promised a gift/offer not actually included in the order</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-fake-offers"></div>
            </div>
            <div class="sc-item" data-key="invalid-lead" data-fatal="true">
              <div class="sc-item-label">Invalid Lead <span class="sc-critical">★ Fatal</span><br/><span style="font-size:11px;color:var(--text2);">Lead does not meet qualification criteria</span></div>
              <div class="sc-item-weight" style="color:var(--danger);">Fatal</div>
              <div class="sc-item-score" id="btn-invalid-lead"></div>
            </div>
          </div>

          <!-- Notes -->
          <div class="card">
            <div class="card-title" style="margin-bottom:12px;">Notes &amp; Coaching</div>
            <div class="form-group" style="margin-bottom:10px;"><label>Strengths Observed</label><textarea id="sc-strengths" placeholder="What did the agent do well?"></textarea></div>
            <div class="form-group" style="margin-bottom:10px;"><label>Areas for Improvement</label><textarea id="sc-improve" placeholder="Specific areas needing development..."></textarea></div>
            <div class="form-group" style="margin-bottom:14px;"><label>Action Required</label>
              <select id="sc-action">
                <option>No action needed</option><option>Coaching session required</option>
                <option>Formal warning</option><option>Escalate to manager</option><option>Performance improvement plan</option>
              </select>
            </div>
            <div style="display:flex;gap:10px;">
              <button class="btn btn-ghost" style="flex:1" onclick="resetScorecard()">↺ Reset</button>
              <button class="btn btn-primary" style="flex:2" onclick="submitScorecard()">✓ Submit &amp; Preview Email →</button>
            </div>
          </div>
        </div>

        <!-- RIGHT PANEL -->
        <div style="position:sticky;top:74px;">
          <div class="card">
            <div class="card-title" style="margin-bottom:12px;">Live Score</div>
            <div class="score-live-display">
              <div>
                <div class="score-live-num" id="score-num" style="color:var(--text2);">—</div>
                <div style="font-size:11px;color:var(--text2);margin-top:2px;">out of 100 pts</div>
              </div>
              <div id="score-verdict" class="score-live-verdict" style="background:var(--surface);color:var(--text2);">Not started</div>
            </div>
            <div style="margin-top:14px;">
              <div class="score-breakdown" style="grid-template-columns:1fr;">
                <div class="sb-item"><div class="sb-label"><span>Call Opening &amp; Closure</span><span class="sb-pct" id="sb-opening">0/20</span></div><div class="prog-bar"><div class="prog-fill" id="pb-opening" style="width:0%;background:var(--accent);"></div></div></div>
                <div class="sb-item"><div class="sb-label"><span>Attitude &amp; Tone</span><span class="sb-pct" id="sb-tone">0/25</span></div><div class="prog-bar"><div class="prog-fill" id="pb-tone" style="width:0%;background:var(--accent2);"></div></div></div>
                <div class="sb-item"><div class="sb-label"><span>Hold Process</span><span class="sb-pct" id="sb-hold">0/15</span></div><div class="prog-bar"><div class="prog-fill" id="pb-hold" style="width:0%;background:var(--purple);"></div></div></div>
                <div class="sb-item"><div class="sb-label"><span>Call Performance</span><span class="sb-pct" id="sb-perf">0/40</span></div><div class="prog-bar"><div class="prog-fill" id="pb-perf" style="width:0%;background:var(--green);"></div></div></div>
              </div>
            </div>
            <div style="margin-top:10px;padding:8px 10px;background:rgba(247,95,95,0.05);border:1px solid rgba(247,95,95,0.1);border-radius:6px;font-size:11px;color:var(--text2);">
              ★ Fatal items (Documentation &amp; Fake Info sections) cause immediate fail if flagged — score is set to 0.
            </div>
          </div>
          <div class="card">
            <div class="card-title" style="margin-bottom:10px;">Score Guide</div>
            <div style="display:flex;flex-direction:column;gap:6px;font-size:12px;">
              <div style="display:flex;justify-content:space-between;padding:6px 10px;background:rgba(61,214,140,0.07);border-radius:6px;"><span style="color:var(--green);">90–100%</span><span style="color:var(--text2);">🏆 Excellent</span></div>
              <div style="display:flex;justify-content:space-between;padding:6px 10px;background:rgba(79,142,247,0.07);border-radius:6px;"><span style="color:var(--accent);">80–89%</span><span style="color:var(--text2);">✅ Meets Standard</span></div>
              <div style="display:flex;justify-content:space-between;padding:6px 10px;background:rgba(247,185,79,0.07);border-radius:6px;"><span style="color:var(--accent3);">70–79%</span><span style="color:var(--text2);">⚠️ Needs Improvement</span></div>
              <div style="display:flex;justify-content:space-between;padding:6px 10px;background:rgba(247,95,95,0.07);border-radius:6px;"><span style="color:var(--danger);">&lt;70%</span><span style="color:var(--text2);">❌ Below Standard</span></div>
            </div>
          </div>
          <div class="card">
            <div class="card-title" style="margin-bottom:10px;">Fatal Error Flags</div>
            <div id="fatal-flags"><div style="padding:8px;background:var(--surface2);border-radius:6px;font-size:12px;color:var(--green);">✓ No fatal errors flagged</div></div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ QA TRACKER ═══ -->
    <div id="page-tracker" class="page">
      <div class="card">
        <div class="card-header">
          <div class="card-header-left">
            <div class="card-title">QA Evaluation Tracker</div>
            <div class="card-subtitle" id="tracker-count">Loading...</div>
          </div>
          <div class="card-actions">
            <button class="btn btn-ghost btn-sm" onclick="exportCSV()">↓ CSV</button>
          </div>
        </div>
        <div class="filter-bar">
          <div class="search-wrap" style="flex:1;max-width:240px;"><span class="search-icon">🔍</span><input type="text" id="tracker-search" placeholder="Search agent, ID..." oninput="renderTracker()"/></div>
          <select id="filter-channel" onchange="renderTracker()">
            <option value="">All Channels</option>
            <option value="Phone">📞 Phone</option>
            <option value="Chat">💬 Chat</option>
            <option value="Email">📧 Email</option>
          </select>
          <select id="filter-status" onchange="renderTracker()">
            <option value="">All Status</option>
            <option value="Pass">Pass</option>
            <option value="Fail">Fail</option>
            <option value="Pending">Pending</option>
          </select>
          <select id="filter-specialist" onchange="renderTracker()">
            <option value="">All Specialists</option>
            <option>Sara Ahmed</option><option>Mohamed Karim</option><option>Lina Rashid</option>
            <option>Omar Tarek</option><option>Rania Fouad</option><option>Khaled Mostafa</option>
          </select>
        </div>
        <div class="table-wrap">
          <table class="table-sortable">
            <thead><tr>
              <th onclick="sortTable('id')">Eval ID ↕</th>
              <th onclick="sortTable('agent')">Agent ↕</th>
              <th>Team</th>
              <th>Specialist</th>
              <th>Channel</th>
              <th onclick="sortTable('score')">Score ↕</th>
              <th>Status</th>
              <th onclick="sortTable('date')">Date ↕</th>
              <th></th>
            </tr></thead>
            <tbody id="tracker-tbody"></tbody>
          </table>
        </div>
        <div id="tracker-pagination" style="display:flex;justify-content:space-between;align-items:center;margin-top:12px;font-size:12px;color:var(--text2);padding-top:12px;border-top:1px solid var(--border);"></div>
      </div>
    </div>

    <!-- ═══ FEEDBACK ═══ -->
    <div id="page-feedback" class="page">
      <div class="grid-2" style="align-items:start;">
        <div class="card">
          <div class="card-title" style="margin-bottom:16px;">Send Agent Feedback</div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Agent</label><input type="text" id="fb-agent" placeholder="Agent name..."/></div>
            <div class="form-group"><label>Linked Eval ID</label><input type="text" id="fb-eval" placeholder="QA-2026-0042"/></div>
          </div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Channel</label><select id="fb-channel"><option>📞 Phone</option><option>💬 Live Chat</option><option>📧 Email</option></select></div>
            <div class="form-group"><label>Score Given</label><input type="number" id="fb-score" min="0" max="100" placeholder="0-100"/></div>
          </div>
          <div class="form-group" style="margin-bottom:10px;"><label>Feedback Type</label>
            <select id="fb-type">
              <option>Coaching & Guidance</option><option>Positive Recognition</option>
              <option>Formal Warning</option><option>Performance Improvement</option>
            </select>
          </div>
          <div class="form-group" style="margin-bottom:10px;"><label>Strengths</label><textarea id="fb-strengths" placeholder="What the agent did well..."></textarea></div>
          <div class="form-group" style="margin-bottom:10px;"><label>Development Areas</label><textarea id="fb-develop" placeholder="Specific areas to improve..."></textarea></div>
          <div class="form-group" style="margin-bottom:10px;"><label>Action Plan</label><textarea id="fb-action" placeholder="Steps with timeline..."></textarea></div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Follow-up Date</label><input type="date" id="fb-followup"/></div>
            <div class="form-group"><label>Delivery Method</label><select id="fb-delivery"><option>1-on-1 Session</option><option>Written (Email)</option><option>Team Meeting</option></select></div>
          </div>
          <div style="display:flex;gap:8px;margin-top:6px;">
            <button class="btn btn-ghost" style="flex:1">Preview</button>
            <button class="btn btn-primary" style="flex:2" onclick="submitFeedback()">Send Feedback</button>
          </div>
        </div>
        <div>
          <div class="card">
            <div class="card-header"><div class="card-title">Recent Feedback Sent</div><span class="tag tag-blue">This week</span></div>
            <div class="timeline">
              <div class="tl-item"><div class="tl-dot" style="background:var(--green);"></div><div class="tl-content"><div class="tl-title">Karim Wael — Positive Recognition</div><div class="tl-meta">Score: 91% · Phone · Sara Ahmed · Mar 25</div></div></div>
              <div class="tl-item"><div class="tl-dot" style="background:var(--orange);"></div><div class="tl-content"><div class="tl-title">Nour Saleh — Coaching Session</div><div class="tl-meta">Score: 71% · Chat · Mohamed Karim · Mar 24</div></div></div>
              <div class="tl-item"><div class="tl-dot" style="background:var(--danger);"></div><div class="tl-content"><div class="tl-title">Ahmed Hassan — Formal Warning</div><div class="tl-meta">Score: 62% · Phone · Manager · Mar 23</div></div></div>
              <div class="tl-item"><div class="tl-dot" style="background:var(--accent);"></div><div class="tl-content"><div class="tl-title">Fatima Al-Zahraa — Coaching & Guidance</div><div class="tl-meta">Score: 74% · Email · Lina Rashid · Mar 22</div></div></div>
            </div>
          </div>
          <div class="card">
            <div class="card-title" style="margin-bottom:12px;">Pending Follow-ups</div>
            <div class="table-wrap">
              <table>
                <thead><tr><th>Agent</th><th>Type</th><th>Due</th><th>Status</th></tr></thead>
                <tbody>
                  <tr><td>Ahmed Hassan</td><td>Coaching</td><td style="font-family:'DM Mono',monospace;font-size:11px;">Mar 30</td><td><span class="status-badge status-pending">Due Soon</span></td></tr>
                  <tr><td>Nour Saleh</td><td>Check-in</td><td style="font-family:'DM Mono',monospace;font-size:11px;">Apr 2</td><td><span class="status-badge status-pending">Scheduled</span></td></tr>
                  <tr><td>Yara Mostafa</td><td>PIP Review</td><td style="font-family:'DM Mono',monospace;font-size:11px;">Apr 5</td><td><span class="status-badge status-fail">Overdue</span></td></tr>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ CALIBRATION ═══ -->
    <div id="page-calibration" class="page">
      <div class="grid-2" style="align-items:start;">
        <div class="card">
          <div class="card-title" style="margin-bottom:16px;">Schedule Calibration Session</div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Session Title</label><input type="text" id="cal-title" placeholder="e.g. March Phone Calibration"/></div>
            <div class="form-group"><label>Session Date</label><input type="date" id="cal-date"/></div>
          </div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Channel Focus</label><select id="cal-channel"><option>📞 Phone</option><option>💬 Chat</option><option>📧 Email</option><option>All Channels</option></select></div>
            <div class="form-group"><label>Interaction to Review</label><input type="text" id="cal-int" placeholder="Interaction ID..."/></div>
          </div>
          <div class="form-group" style="margin-bottom:10px;"><label>Participating Specialists</label><select id="cal-specs" multiple style="height:90px;"><option>Sara Ahmed</option><option>Mohamed Karim</option><option>Lina Rashid</option><option>Omar Tarek</option><option>Rania Fouad</option><option>Khaled Mostafa</option></select></div>
          <div class="form-group" style="margin-bottom:10px;"><label>Focus Areas</label><textarea id="cal-focus" placeholder="Criteria being calibrated?"></textarea></div>
          <div class="form-group" style="margin-bottom:14px;"><label>Expected Outcome</label><textarea id="cal-outcome" placeholder="Alignment goals..."></textarea></div>
          <button class="btn btn-primary" style="width:100%;" onclick="submitCalibration()">Schedule Session</button>
        </div>
        <div>
          <div class="card">
            <div class="card-title" style="margin-bottom:12px;">Calibration History</div>
            <div class="table-wrap">
              <table>
                <thead><tr><th>Session</th><th>Channel</th><th>Date</th><th>Variance</th><th>Outcome</th></tr></thead>
                <tbody>
                  <tr><td>March Phone Cal.</td><td><span class="ch-badge ch-phone">📞 Phone</span></td><td style="font-family:'DM Mono',monospace;font-size:11px;">Mar 15</td><td><span style="color:var(--green);font-family:'DM Mono',monospace;font-size:11px;">±3.2%</span></td><td><span class="status-badge status-pass">Aligned</span></td></tr>
                  <tr><td>Feb Chat Review</td><td><span class="ch-badge ch-chat">💬 Chat</span></td><td style="font-family:'DM Mono',monospace;font-size:11px;">Feb 20</td><td><span style="color:var(--orange);font-family:'DM Mono',monospace;font-size:11px;">±7.8%</span></td><td><span class="status-badge status-pending">Re-calibrate</span></td></tr>
                  <tr><td>Feb Email Cal.</td><td><span class="ch-badge ch-email">📧 Email</span></td><td style="font-family:'DM Mono',monospace;font-size:11px;">Feb 10</td><td><span style="color:var(--green);font-family:'DM Mono',monospace;font-size:11px;">±2.1%</span></td><td><span class="status-badge status-pass">Aligned</span></td></tr>
                  <tr><td>Jan Full Cal.</td><td>All</td><td style="font-family:'DM Mono',monospace;font-size:11px;">Jan 25</td><td><span style="color:var(--danger);font-family:'DM Mono',monospace;font-size:11px;">±11.4%</span></td><td><span class="status-badge status-fail">Misaligned</span></td></tr>
                </tbody>
              </table>
            </div>
          </div>
          <div class="card">
            <div class="card-title" style="margin-bottom:12px;">Specialist Variance</div>
            <div style="display:flex;flex-direction:column;gap:10px;">
              <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:13px;"><span>Sara Ahmed</span><span style="color:var(--green);font-family:'DM Mono',monospace;font-size:11px;">±2.1%</span></div><div class="prog-bar"><div class="prog-fill" style="width:5%;background:var(--green);"></div></div></div>
              <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:13px;"><span>Mohamed Karim</span><span style="color:var(--green);font-family:'DM Mono',monospace;font-size:11px;">±3.4%</span></div><div class="prog-bar"><div class="prog-fill" style="width:10%;background:var(--green);"></div></div></div>
              <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:13px;"><span>Lina Rashid</span><span style="color:var(--accent3);font-family:'DM Mono',monospace;font-size:11px;">±6.2%</span></div><div class="prog-bar"><div class="prog-fill" style="width:22%;background:var(--accent3);"></div></div></div>
              <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:13px;"><span>Omar Tarek</span><span style="color:var(--danger);font-family:'DM Mono',monospace;font-size:11px;">±9.8%</span></div><div class="prog-bar"><div class="prog-fill" style="width:38%;background:var(--danger);"></div></div></div>
            </div>
            <div style="margin-top:10px;font-size:11px;color:var(--text2);">Target variance: ≤ 5% for consistent scoring</div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ DISPUTES ═══ -->
    <div id="page-disputes" class="page">
      <div id="disputes-submit-only" style="display:none;">
        <!-- CC Team Leader view: submit only -->
        <div class="alert-strip alert-strip-info">💬 Submit a dispute on behalf of an agent. The QA team will review and respond.</div>
        <div class="card" style="max-width:600px;">
          <div class="card-title" style="margin-bottom:16px;">Submit New Dispute</div>
          <div class="form-row cols-2">
            <div class="form-group">
              <label>Agent <span style="color:var(--danger);">*</span></label>
              <select id="disp-agent" onchange="onDisputeAgentChange()" style="width:100%;">
                <option value="">— Select agent —</option>
              </select>
            </div>
            <div class="form-group">
              <label>Eval ID <span style="color:var(--danger);">*</span></label>
              <select id="disp-eval" style="width:100%;">
                <option value="">— Select agent first —</option>
              </select>
            </div>
          </div>
          <div class="form-row cols-2">
            <div class="form-group"><label>Channel</label>
              <select id="disp-channel"><option>📞 Phone</option><option>💬 Chat</option><option>📧 Email</option></select>
            </div>
            <div class="form-group"><label>Score Received</label>
              <input type="number" id="disp-score" min="0" max="100" placeholder="e.g. 62" readonly style="background:var(--surface2);"/>
            </div>
          </div>
          <div class="form-group" style="margin-bottom:10px;"><label>Dispute Reason <span style="color:var(--danger);">*</span></label><textarea id="disp-reason-tl" placeholder="Describe the agent's dispute in detail..."></textarea></div>
          <div class="form-group" style="margin-bottom:16px;"><label>Supporting Evidence</label><input type="text" id="disp-evidence-tl" placeholder="e.g. Timestamp 02:34, email thread ID..."/></div>
          <button class="btn btn-primary" style="width:100%;" onclick="logDispute()">🚩 Submit Dispute</button>
        </div>

        <!-- TL also sees open disputes for their team -->
        <div class="card" style="margin-top:18px;">
          <div class="card-header"><div class="card-title">My Team's Open Disputes</div><span class="tag tag-red" id="tl-open-count">3 open</span></div>
          <div id="tl-disputes-list" style="display:flex;flex-direction:column;gap:10px;"></div>
        </div>
      </div>

      <div id="disputes-full" style="display:block;">
        <!-- QA Manager / Admin view: full control -->
        <div class="grid-2" style="align-items:start;">
          <div>
            <div class="alert-strip">🚩 <strong id="open-disputes-count">3 open disputes</strong> require your review</div>
            <div class="dispute-card" id="disp-003">
              <div class="dispute-header">
                <div><div class="dispute-id">DISP-2026-003</div><div style="font-size:14px;font-weight:600;margin-top:3px;">Ahmed Hassan — QA-2026-0198</div><div style="font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;">📞 Phone · Score: 62% · Mar 26</div></div>
                <span class="status-badge status-fail">Open</span>
              </div>
              <div class="dispute-body">"I believe the score for my empathy section is incorrect. I acknowledged the customer's frustration multiple times."</div>
              <div style="display:flex;gap:6px;margin-top:10px;">
                <button class="btn btn-success btn-sm" onclick="resolveDispute(this,'uphold')">✓ Uphold</button>
                <button class="btn btn-danger btn-sm" onclick="resolveDispute(this,'adjust')">↕ Adjust</button>
                <button class="btn btn-ghost btn-sm">🎙 Listen</button>
              </div>
            </div>
            <div class="dispute-card" id="disp-002">
              <div class="dispute-header">
                <div><div class="dispute-id">DISP-2026-002</div><div style="font-size:14px;font-weight:600;margin-top:3px;">Nour Saleh — QA-2026-0187</div><div style="font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;">💬 Chat · Score: 71% · Mar 24</div></div>
                <span class="status-badge status-fail">Open</span>
              </div>
              <div class="dispute-body">"The resolution section was marked incorrectly. The issue recurred due to a system bug, not agent error."</div>
              <div style="display:flex;gap:6px;margin-top:10px;">
                <button class="btn btn-success btn-sm" onclick="resolveDispute(this,'uphold')">✓ Uphold</button>
                <button class="btn btn-danger btn-sm" onclick="resolveDispute(this,'adjust')">↕ Adjust</button>
                <button class="btn btn-ghost btn-sm">💬 View Chat</button>
              </div>
            </div>
            <div class="dispute-card" id="disp-001">
              <div class="dispute-header">
                <div><div class="dispute-id">DISP-2026-001</div><div style="font-size:14px;font-weight:600;margin-top:3px;">Yara Mostafa — QA-2026-0175</div><div style="font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;">📧 Email · Score: 69% · Mar 22</div></div>
                <span class="status-badge status-pending">In Review</span>
              </div>
              <div class="dispute-body">"The template used was the approved one. The grammar issue flagged is part of the template."</div>
              <div style="display:flex;gap:6px;margin-top:10px;">
                <button class="btn btn-success btn-sm" onclick="resolveDispute(this,'uphold')">✓ Uphold</button>
                <button class="btn btn-danger btn-sm" onclick="resolveDispute(this,'adjust')">↕ Adjust</button>
                <button class="btn btn-ghost btn-sm">📧 View Email</button>
              </div>
            </div>
          </div>
          <div>
            <div class="card">
              <div class="card-title" style="margin-bottom:14px;">Log New Dispute</div>
              <div class="form-row cols-2">
                <div class="form-group">
                  <label>Agent <span style="color:var(--danger);">*</span></label>
                  <select id="disp-agent-mgr" onchange="onDisputeAgentChangeMgr()" style="width:100%;">
                    <option value="">— Select agent —</option>
                  </select>
                </div>
                <div class="form-group">
                  <label>Eval ID <span style="color:var(--danger);">*</span></label>
                  <select id="disp-eval-mgr" style="width:100%;">
                    <option value="">— Select agent first —</option>
                  </select>
                </div>
              </div>
              <div class="form-group" style="margin-bottom:10px;"><label>Dispute Reason <span style="color:var(--danger);">*</span></label><textarea id="disp-reason" placeholder="Describe the dispute in detail..."></textarea></div>
              <div class="form-group" style="margin-bottom:14px;"><label>Supporting Evidence</label><input type="text" id="disp-evidence" placeholder="e.g. Timestamp 02:34, email thread ID..."/></div>
              <button class="btn btn-primary" style="width:100%;" onclick="logDisputeMgr()">Submit Dispute</button>
            </div>
            <div class="card">
              <div class="card-title" style="margin-bottom:12px;">Dispute Stats</div>
              <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;text-align:center;">
                <div class="report-kpi"><div class="report-kpi-val" style="color:var(--danger);" id="dispute-stat-open">3</div><div class="report-kpi-lbl">Open</div></div>
                <div class="report-kpi"><div class="report-kpi-val" style="color:var(--green);">14</div><div class="report-kpi-lbl">Resolved</div></div>
                <div class="report-kpi"><div class="report-kpi-val" style="color:var(--accent);">28%</div><div class="report-kpi-lbl">Adjusted</div></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ AGENTS ═══ -->
    <div id="page-agents" class="page">
      <div class="card">
        <div class="card-header">
          <div class="card-header-left">
            <div class="card-title">Agent Performance Overview</div>
            <div class="card-subtitle">450 agents across all channels</div>
          </div>
          <div class="card-actions">
            <button class="btn btn-ghost btn-sm" onclick="showPage('team-mgmt',null)">⚙️ Manage</button>
            <button class="btn btn-primary btn-sm" onclick="openMemberModal('agent')">+ Add Agent</button>
          </div>
        </div>
        <div class="filter-bar">
          <div class="search-wrap" style="flex:1;max-width:240px;"><span class="search-icon">🔍</span><input type="text" id="agent-search" placeholder="Search agent..." oninput="renderAgents()"/></div>
          <select id="agent-filter-team" onchange="renderAgents()"><option value="">All Teams</option><option>Team A</option><option>Team B</option><option>Team C</option><option>Team D</option></select>
          <select id="agent-filter-channel" onchange="renderAgents()"><option value="">All Channels</option><option value="Phone">📞 Phone</option><option value="Chat">💬 Chat</option><option value="Email">📧 Email</option></select>
          <select id="agent-filter-status" onchange="renderAgents()"><option value="">All Status</option><option value="Pass">Pass</option><option value="Pending">Coaching</option><option value="Fail">Failing</option></select>
          <input type="date" id="agent-date-from" onchange="renderAgents()" placeholder="From" style="font-size:11px;padding:5px 8px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);color:var(--text);"/>
          <input type="date" id="agent-date-to" onchange="renderAgents()" placeholder="To" style="font-size:11px;padding:5px 8px;background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);color:var(--text);"/>
          <button class="btn btn-ghost btn-sm" onclick="document.getElementById('agent-date-from').value='';document.getElementById('agent-date-to').value='';renderAgents();">✕ Clear Dates</button>
        </div>
        <div class="table-wrap">
          <table>
            <thead><tr><th>Agent</th><th>Target Calls</th><th>Audits</th><th>Score</th><th>Sales Score</th><th>Sales Mistakes</th><th>Failed Audits</th><th>Status</th><th></th></tr></thead>
            <tbody id="agents-tbody"></tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- ═══ SPECIALISTS ═══ -->
    <div id="page-specialists" class="page">
      <div class="card">
        <div class="card-header">
          <div class="card-header-left">
            <div class="card-title">QA Specialist Performance</div>
            <div class="card-subtitle">20 specialists · Evaluation quality & consistency</div>
          </div>
        </div>
        <div id="specialists-grid" style="display:grid;grid-template-columns:repeat(2,1fr);gap:14px;"></div>
      </div>
    </div>

    <!-- ═══ REPORTS (NEW) ═══ -->
    <div id="page-reports" class="page">
      <div class="grid-3" style="margin-bottom:18px;">
        <div class="report-kpi" style="background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:18px;">
          <div class="report-kpi-val" style="color:var(--accent);font-size:28px;">84.3%</div>
          <div class="report-kpi-lbl">Overall Avg Score</div>
          <div style="font-size:11px;color:var(--green);margin-top:6px;">▲ +2.1% vs Feb</div>
        </div>
        <div class="report-kpi" style="background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:18px;">
          <div class="report-kpi-val" style="color:var(--green);font-size:28px;">1,284</div>
          <div class="report-kpi-lbl">Total Evaluations</div>
          <div style="font-size:11px;color:var(--text2);margin-top:6px;">95% of monthly target</div>
        </div>
        <div class="report-kpi" style="background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:18px;">
          <div class="report-kpi-val" style="color:var(--orange);font-size:28px;">8.4%</div>
          <div class="report-kpi-lbl">Agents Below Target</div>
          <div style="font-size:11px;color:var(--green);margin-top:6px;">▼ Improved from 11.2%</div>
        </div>
      </div>

      <div class="grid-2">
        <div class="card">
          <div class="card-header"><div class="card-header-left"><div class="card-title">Monthly Score Trend</div><div class="card-subtitle">Jan – Mar 2026 by channel</div></div></div>
          <canvas id="monthly-chart" height="150" style="width:100%;"></canvas>
          <div class="chart-legend" style="margin-top:10px;">
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--accent);"></div>Phone</div>
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--accent2);"></div>Chat</div>
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--accent3);"></div>Email</div>
          </div>
        </div>
        <div class="card">
          <div class="card-header"><div class="card-header-left"><div class="card-title">Score Distribution</div><div class="card-subtitle">All agents this month</div></div></div>
          <canvas id="dist-chart" height="150" style="width:100%;"></canvas>
          <div class="chart-legend" style="margin-top:10px;">
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--green);"></div>Excellent (90-100%)</div>
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--accent);"></div>Pass (80-89%)</div>
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--accent3);"></div>Needs Work (70-79%)</div>
            <div class="chart-legend-item"><div class="chart-legend-dot" style="background:var(--danger);"></div>Fail (&lt;70%)</div>
          </div>
        </div>
      </div>

      <div class="grid-2">
        <div class="card">
          <div class="card-header"><div class="card-title">Top Improving Agents</div><span class="tag tag-green">This Month</span></div>
          <div style="display:flex;flex-direction:column;gap:10px;">
            <div style="display:flex;align-items:center;gap:10px;padding:8px;background:var(--surface2);border-radius:8px;">
              <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--green),var(--accent2));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;">KW</div>
              <div style="flex:1;"><div style="font-size:13px;">Karim Wael</div><div class="prog-bar" style="margin-top:4px;"><div class="prog-fill" style="width:82%;background:var(--green);"></div></div></div>
              <span class="tag tag-green">+12%</span>
            </div>
            <div style="display:flex;align-items:center;gap:10px;padding:8px;background:var(--surface2);border-radius:8px;">
              <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--accent),var(--purple));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;">SI</div>
              <div style="flex:1;"><div style="font-size:13px;">Salma Ibrahim</div><div class="prog-bar" style="margin-top:4px;"><div class="prog-fill" style="width:88%;background:var(--accent);"></div></div></div>
              <span class="tag tag-green">+9%</span>
            </div>
            <div style="display:flex;align-items:center;gap:10px;padding:8px;background:var(--surface2);border-radius:8px;">
              <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--accent2),var(--accent));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;">RK</div>
              <div style="flex:1;"><div style="font-size:13px;">Reem Khalid</div><div class="prog-bar" style="margin-top:4px;"><div class="prog-fill" style="width:79%;background:var(--accent2);"></div></div></div>
              <span class="tag tag-green">+7%</span>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="card-header"><div class="card-title">Critical Criteria Performance</div><span class="tag tag-blue">Avg across all evals</span></div>
          <div style="display:flex;flex-direction:column;gap:10px;">
            <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;"><span>Issue Resolution</span><span style="color:var(--green);font-family:'DM Mono',monospace;">92%</span></div><div class="prog-bar"><div class="prog-fill" style="width:92%;background:var(--green);"></div></div></div>
            <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;"><span>Empathy & Listening</span><span style="color:var(--accent);font-family:'DM Mono',monospace;">81%</span></div><div class="prog-bar"><div class="prog-fill" style="width:81%;background:var(--accent);"></div></div></div>
            <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;"><span>Compliance</span><span style="color:var(--accent2);font-family:'DM Mono',monospace;">95%</span></div><div class="prog-bar"><div class="prog-fill" style="width:95%;background:var(--accent2);"></div></div></div>
            <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;"><span>Professional Closing</span><span style="color:var(--accent3);font-family:'DM Mono',monospace;">78%</span></div><div class="prog-bar"><div class="prog-fill" style="width:78%;background:var(--accent3);"></div></div></div>
            <div><div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;"><span>Product Knowledge</span><span style="color:var(--orange);font-family:'DM Mono',monospace;">74%</span></div><div class="prog-bar"><div class="prog-fill" style="width:74%;background:var(--orange);"></div></div></div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ SCORECARD BUILDER ═══ -->
    <div id="page-sc-builder" class="page">
      <div class="grid-2" style="align-items:start;">
        <!-- LEFT: Section & Item Builder -->
        <div>
          <div class="card">
            <div class="card-header">
              <div class="card-header-left">
                <div class="card-title">🛠️ Scorecard Builder</div>
                <div class="card-subtitle">Add, edit or remove sections and attributes</div>
              </div>
              <button class="btn btn-primary btn-sm" onclick="openAddSection()">+ Add Section</button>
            </div>
            <div id="sc-builder-list"></div>
          </div>
        </div>

        <!-- RIGHT: Live Preview -->
        <div style="position:sticky;top:74px;">
          <div class="card">
            <div class="card-header">
              <div class="card-header-left">
                <div class="card-title">Live Preview</div>
                <div class="card-subtitle">How the scorecard looks now</div>
              </div>
              <button class="btn btn-success btn-sm" onclick="applyScorecard()">✓ Apply to Scorecard</button>
            </div>
            <div id="sc-preview-list" style="display:flex;flex-direction:column;gap:8px;"></div>
            <div style="margin-top:14px;padding:12px;background:var(--surface2);border-radius:8px;display:flex;justify-content:space-between;align-items:center;">
              <span style="font-size:12px;color:var(--text2);">Total Scored Points</span>
              <span style="font-family:'Syne',sans-serif;font-size:20px;font-weight:800;color:var(--accent);" id="sc-total-pts">100</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ TEAM MANAGEMENT ═══ -->
    <div id="page-team-mgmt" class="page">
      <div class="tabs">
        <div class="tab-btn active" onclick="switchTeamTab('agents',this)">👥 CC Agents</div>
        <div class="tab-btn" onclick="switchTeamTab('specialists',this)">🎯 QA Specialists</div>
      </div>

      <!-- CC Agents Tab -->
      <div id="team-tab-agents">
        <div class="card">
          <div class="card-header">
            <div class="card-header-left">
              <div class="card-title">Customer Care Agents</div>
              <div class="card-subtitle" id="agent-count-label">0 agents</div>
            </div>
            <button class="btn btn-primary btn-sm" onclick="openMemberModal('agent')">+ Add Agent</button>
          </div>
          <div class="filter-bar">
            <div class="search-wrap" style="flex:1;max-width:240px;"><span class="search-icon">🔍</span><input type="text" id="tm-agent-search" placeholder="Search agent..." oninput="renderTeamAgents()"/></div>
            <select id="tm-agent-team" onchange="renderTeamAgents()">
              <option value="">All Teams</option>
              <option>Team A</option><option>Team B</option><option>Team C</option><option>Team D</option>
            </select>
            <select id="tm-agent-channel" onchange="renderTeamAgents()">
              <option value="">All Channels</option>
              <option>📞 Phone</option><option>💬 Chat</option><option>📧 Email</option>
            </select>
          </div>
          <div class="table-wrap">
            <table>
              <thead><tr><th>Emp ID</th><th>Name</th><th>Target Calls</th><th>Audits</th><th>Score</th><th>Sales Score</th><th>Sales Mistakes</th><th>Failed Audits</th><th>Status</th><th>Actions</th></tr></thead>
              <tbody id="tm-agents-tbody"></tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- QA Specialists Tab -->
      <div id="team-tab-specialists" style="display:none;">
        <div class="card">
          <div class="card-header">
            <div class="card-header-left">
              <div class="card-title">QA Specialists</div>
              <div class="card-subtitle" id="spec-count-label">0 specialists</div>
            </div>
            <button class="btn btn-primary btn-sm" onclick="openMemberModal('specialist')">+ Add Specialist</button>
          </div>
          <div class="filter-bar">
            <div class="search-wrap" style="flex:1;max-width:240px;"><span class="search-icon">🔍</span><input type="text" id="tm-spec-search" placeholder="Search specialist..." oninput="renderTeamSpecs()"/></div>
          </div>
          <div class="table-wrap">
            <table>
              <thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Evals/Month</th><th>Avg Score</th><th>Assigned Workspaces</th><th>Status</th><th>Actions</th></tr></thead>
              <tbody id="tm-specs-tbody"></tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ ADMIN CONTROL CENTER ═══ -->
    <div id="page-role-mgmt" class="page">
      <div class="tabs" id="admin-tabs">
        <div class="tab-btn active" onclick="switchAdminTab('users',this)">👤 Users</div>
        <div class="tab-btn" onclick="switchAdminTab('roles',this)">🔐 Roles & Access</div>
        <div class="tab-btn" onclick="switchAdminTab('scorecard-cfg',this)">📋 Scorecard</div>
        <div class="tab-btn" onclick="switchAdminTab('app-settings',this)">⚙️ App Settings</div>
      </div>

      <!-- ── USERS TAB ── -->
      <div id="admin-tab-users">
        <div class="card">
          <div class="card-header">
            <div class="card-header-left">
              <div class="card-title">System Users</div>
              <div class="card-subtitle">Add, edit, delete users and assign roles</div>
            </div>
            <button class="btn btn-primary btn-sm" onclick="openUserModal()">+ Add User</button>
          </div>
          <div class="table-wrap">
            <table>
              <thead><tr><th>Name</th><th>Username</th><th>Role</th><th>Status</th><th>Last Login</th><th>Actions</th></tr></thead>
              <tbody id="admin-users-tbody"></tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- ── ROLES TAB ── -->
      <div id="admin-tab-roles" style="display:none;">
        <div class="grid-2" style="align-items:start;">
          <div class="card">
            <div class="card-header">
              <div class="card-header-left">
                <div class="card-title">Role Permissions</div>
                <div class="card-subtitle">Control what each role can access</div>
              </div>
            </div>
            <div id="role-permissions-editor"></div>
          </div>
          <div class="card">
            <div class="card-header">
              <div class="card-header-left">
                <div class="card-title">Page Access Matrix</div>
                <div class="card-subtitle">Toggle page access per role</div>
              </div>
            </div>
            <div id="page-access-matrix"></div>
          </div>
        </div>
      </div>

      <!-- ── SCORECARD CONFIG TAB ── -->
      <div id="admin-tab-scorecard-cfg" style="display:none;">
        <div class="alert-strip alert-strip-info">ℹ️ Changes here update the live scorecard immediately. Total scored pts should equal 100.</div>
        <div class="grid-2" style="align-items:start;">
          <div>
            <div class="card">
              <div class="card-header">
                <div class="card-header-left"><div class="card-title">Scorecard Sections</div><div class="card-subtitle" id="sc-cfg-total"></div></div>
                <button class="btn btn-primary btn-sm" onclick="openAddSection()">+ Add Section</button>
              </div>
              <div id="sc-cfg-list"></div>
            </div>
          </div>
          <div style="position:sticky;top:74px;">
            <div class="card">
              <div class="card-header"><div class="card-title">Live Preview</div></div>
              <div id="sc-cfg-preview"></div>
              <div style="margin-top:12px;padding:10px 14px;background:var(--surface2);border-radius:8px;display:flex;justify-content:space-between;">
                <span style="font-size:12px;color:var(--text2);">Total Scored Points</span>
                <span style="font-family:'Syne',sans-serif;font-size:20px;font-weight:800;color:var(--accent);" id="sc-cfg-total-num">100</span>
              </div>
              <button class="btn btn-success" style="width:100%;margin-top:10px;" onclick="applyScorecard()">✓ Save & Apply to Scorecard</button>
            </div>
          </div>
        </div>
      </div>

      <!-- ── APP SETTINGS TAB ── -->
      <div id="admin-tab-app-settings" style="display:none;">
        <div class="grid-2" style="align-items:start;">
          <div>
            <div class="card">
              <div class="card-header"><div class="card-title">🏢 Organization Info</div></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Company Name</label><input type="text" id="cfg-company" placeholder="e.g. QMS Pro" value="QMS Pro"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Department</label><input type="text" id="cfg-dept" placeholder="e.g. Quality Assurance" value="Quality Assurance"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>QA Target Score (%)</label><input type="number" id="cfg-target" min="0" max="100" value="80"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Monthly Eval Target</label><input type="number" id="cfg-monthly-target" min="0" value="1350"/></div>
              <button class="btn btn-primary" style="width:100%;" onclick="saveAppSettings()">Save Settings</button>
            </div>
            <div class="card">
              <div class="card-header"><div class="card-title">🎨 Default Theme</div></div>
              <div id="admin-theme-grid" style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;"></div>
            </div>
          </div>
          <div>
            <div class="card">
              <div class="card-header"><div class="card-title">📋 Evaluation Settings</div></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Pass Score Threshold (%)</label><input type="number" id="cfg-pass" value="80" min="0" max="100"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Excellent Score Threshold (%)</label><input type="number" id="cfg-excellent" value="90" min="0" max="100"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Dispute Window (hours)</label><input type="number" id="cfg-dispute-window" value="48" min="1"/></div>
              <div class="form-group" style="margin-bottom:12px;"><label>Coaching Below Score (%)</label><input type="number" id="cfg-coaching-threshold" value="75" min="0" max="100"/></div>
              <button class="btn btn-primary" style="width:100%;" onclick="saveAppSettings()">Save Settings</button>
            </div>
            <div class="card">
              <div class="card-header"><div class="card-title">🚨 Danger Zone</div></div>
              <div style="display:flex;flex-direction:column;gap:8px;">
                <div style="padding:12px;background:rgba(79,142,247,0.08);border:1px solid rgba(79,142,247,0.2);border-radius:var(--radius-sm);margin-bottom:4px;">
                  <div style="font-size:13px;font-weight:700;color:var(--accent);margin-bottom:4px;">🔥 Firebase — Upload All Data</div>
                  <div style="font-size:11px;color:var(--text2);margin-bottom:10px;">Uploads all agents, specialists, workspaces, scorecard and config to Firebase. Do this once after connecting.</div>
                  <button class="btn btn-ghost" style="width:100%;margin-bottom:8px;" onclick="checkFirebaseConnection()">Check Firebase Connection</button>
                  <button class="btn btn-primary" style="width:100%;" onclick="runSeedFirestore()">🔥 Upload Data to Firebase</button>
                  <div id="firebase-status" style="font-size:11px;color:var(--text2);margin-top:8px;text-align:center;">Firebase: checking...</div>
                  <div id="seed-status" style="font-size:11px;color:var(--text2);margin-top:8px;text-align:center;display:none;"></div>
                </div>
                <button class="btn btn-danger" style="width:100%;" onclick="if(confirm('Reset all evaluations? This cannot be undone.'))showToast('All evaluations cleared','error')">🗑 Clear All Evaluations</button>
                <button class="btn btn-danger" style="width:100%;" onclick="if(confirm('Reset scorecard to default?'))showToast('Scorecard reset to default')">↺ Reset Scorecard to Default</button>
                <button class="btn btn-danger" style="width:100%;" onclick="if(confirm('Reset all users to default?'))showToast('Users reset to default')">👤 Reset All Users</button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ WORKSPACES ═══ -->
    <div id="page-workspaces" class="page">
      <div class="card" style="margin-bottom:18px;">
        <div class="card-header">
          <div class="card-header-left">
            <div class="card-title">🏢 Workspaces</div>
            <div class="card-subtitle">Manage departments, regions and teams — each with its own data and dashboard</div>
          </div>
          <div class="card-actions">
            <button class="btn btn-ghost btn-sm" onclick="showPage('ws-overview',null)">📊 Cross-Workspace View</button>
            <button class="btn btn-primary btn-sm" onclick="openAddWorkspace()">+ New Workspace</button>
          </div>
        </div>
        <!-- Filter bar -->
        <div class="filter-bar" style="margin-bottom:16px;">
          <div class="search-wrap" style="flex:1;max-width:240px;"><span class="search-icon">🔍</span><input type="text" id="ws-search" placeholder="Search workspaces..." oninput="renderWorkspacesPage()"/></div>
          <select id="ws-filter-type" onchange="renderWorkspacesPage()">
            <option value="">All Types</option>
            <option value="department">Department</option>
            <option value="region">Region</option>
            <option value="team">Team</option>
            <option value="channel">Channel</option>
          </select>
        </div>
        <div id="ws-cards-grid" class="ws-overview-grid"></div>
      </div>
    </div>

    <!-- ═══ CROSS-WORKSPACE OVERVIEW ═══ -->
    <div id="page-ws-overview" class="page">
      <div class="kpi-grid" id="ws-overview-kpis"></div>
      <div class="grid-2">
        <div class="card">
          <div class="card-header"><div class="card-title">📊 Score by Workspace</div></div>
          <div id="ws-score-bars" style="display:flex;flex-direction:column;gap:12px;"></div>
        </div>
        <div class="card">
          <div class="card-header"><div class="card-title">📈 Evaluations by Workspace</div></div>
          <canvas id="ws-evals-chart" height="180" style="width:100%;"></canvas>
        </div>
      </div>
      <div class="card">
        <div class="card-header"><div class="card-title">🏆 Workspace Leaderboard</div></div>
        <div class="table-wrap">
          <table>
            <thead><tr><th>Workspace</th><th>Type</th><th>Agents</th><th>Evals</th><th>Avg Score</th><th>Pass Rate</th><th>Disputes</th><th>Status</th></tr></thead>
            <tbody id="ws-leaderboard-tbody"></tbody>
          </table>
        </div>
      </div>
    </div>

  </div>
</main>

<!-- ═══ ADD/EDIT WORKSPACE MODAL ═══ -->
<div class="modal-overlay" id="ws-modal" onclick="if(event.target===this)closeWsModal()">
  <div class="modal" style="max-width:580px;">
    <span class="modal-close-x" onclick="closeWsModal()">×</span>
    <div class="modal-title" id="ws-modal-title">New Workspace</div>
    <div class="form-row cols-2">
      <div class="form-group">
        <label>Workspace Name</label>
        <input type="text" id="wm-name" placeholder="e.g. Cairo Sales Team"/>
      </div>
      <div class="form-group">
        <label>Type</label>
        <select id="wm-type">
          <option value="telesales">📞 Telesales</option>
          <option value="confirmation">✅ Confirmation</option>
          <option value="whatsapp">💬 WhatsApp</option>
        </select>
      </div>
    </div>
    <div class="form-row cols-2">
      <div class="form-group">
        <label>Icon (emoji)</label>
        <input type="text" id="wm-icon" placeholder="e.g. 🏢" maxlength="4"/>
      </div>
      <div class="form-group">
        <label>Color</label>
        <select id="wm-color">
          <option value="var(--accent)">🔵 Blue</option>
          <option value="var(--green)">🟢 Green</option>
          <option value="var(--purple)">🟣 Purple</option>
          <option value="var(--accent2)">🩵 Teal</option>
          <option value="var(--accent3)">🟡 Gold</option>
          <option value="var(--danger)">🔴 Red</option>
          <option value="var(--orange)">🟠 Orange</option>
        </select>
      </div>
    </div>
    <div class="form-group" style="margin-bottom:12px;">
      <label>Description</label>
      <input type="text" id="wm-desc" placeholder="Brief description of this workspace"/>
    </div>
    <div class="form-row cols-2">
      <div class="form-group">
        <label>Region / Location</label>
        <input type="text" id="wm-region" placeholder="e.g. Cairo, Egypt"/>
      </div>
      <div class="form-group">
        <label>Manager Name</label>
        <input type="text" id="wm-manager" placeholder="Workspace manager"/>
      </div>
    </div>
    <div class="form-row cols-3">
      <div class="form-group">
        <label>QA Target (%)</label>
        <input type="number" id="wm-target" value="80" min="0" max="100"/>
      </div>
      <div class="form-group">
        <label>Monthly Eval Target</label>
        <input type="number" id="wm-eval-target" value="200" min="0"/>
      </div>
      <div class="form-group">
        <label>Status</label>
        <select id="wm-status"><option value="active">Active</option><option value="inactive">Inactive</option></select>
      </div>
    </div>
    <div class="separator"></div>
    <div style="font-size:12px;color:var(--text2);margin-bottom:10px;">Assign existing members to this workspace:</div>
    <div class="form-row cols-2">
      <div class="form-group">
        <label>Assign Agents <span class="opt">hold Ctrl/Cmd to multi-select</span></label>
        <select id="wm-agents" multiple style="height:110px;"></select>
      </div>
      <div class="form-group">
        <label>Assign QA Specialists <span class="opt">hold Ctrl/Cmd to multi-select</span></label>
        <select id="wm-specs" multiple style="height:110px;"></select>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeWsModal()">Cancel</button>
      <button class="btn btn-primary" onclick="saveWorkspace()">Save Workspace</button>
    </div>
  </div>
</div>

<!-- ═══ ADD/EDIT USER MODAL ═══ -->
<div class="modal-overlay" id="user-modal" onclick="if(event.target===this)closeUserModal()">
  <div class="modal" style="max-width:520px;">
    <span class="modal-close-x" onclick="closeUserModal()">×</span>
    <div class="modal-title" id="user-modal-title">Add User</div>
    <div class="form-row cols-2">
      <div class="form-group"><label>Full Name</label><input type="text" id="um-name" placeholder="Full name"/></div>
      <div class="form-group"><label>Username</label><input type="text" id="um-username" placeholder="e.g. sara.ahmed"/></div>
    </div>
    <div class="form-row cols-2">
      <div class="form-group"><label>Password</label><input type="password" id="um-password" placeholder="Password"/></div>
      <div class="form-group"><label>Role</label>
        <select id="um-role">
          <option value="admin">Full Admin</option>
          <option value="qa-manager">QA Manager</option>
          <option value="qa-specialist">QA Specialist</option>
          <option value="tl-cc">CC Team Leader</option>
          <option value="agent">CC Agent</option>
        </select>
      </div>
    </div>
    <div class="form-row cols-2">
      <div class="form-group"><label>Initials</label><input type="text" id="um-initials" placeholder="e.g. SA" maxlength="2"/></div>
      <div class="form-group"><label>Status</label>
        <select id="um-status"><option value="Active">Active</option><option value="Inactive">Inactive</option></select>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeUserModal()">Cancel</button>
      <button class="btn btn-primary" onclick="saveUser()">Save User</button>
    </div>
  </div>
</div>

<!-- ═══ ADD/EDIT SECTION MODAL ═══ -->
<div class="modal-overlay" id="section-modal" onclick="if(event.target===this)closeSectionModal()">
  <div class="modal" style="max-width:500px;">
    <span class="modal-close-x" onclick="closeSectionModal()">×</span>
    <div class="modal-title" id="section-modal-title">Add Section</div>
    <div class="form-group" style="margin-bottom:12px;">
      <label>Section Name</label>
      <input type="text" id="sm-name" placeholder="e.g. Call Opening & Closure"/>
    </div>
    <div class="form-row cols-2">
      <div class="form-group">
        <label>Points</label>
        <input type="number" id="sm-pts" placeholder="e.g. 20" min="0"/>
      </div>
      <div class="form-group">
        <label>Type</label>
        <select id="sm-type">
          <option value="scored">Scored (deduct full section)</option>
          <option value="fatal">Fatal (any deduct = fail)</option>
        </select>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeSectionModal()">Cancel</button>
      <button class="btn btn-primary" onclick="saveSection()">Save Section</button>
    </div>
  </div>
</div>

<!-- ═══ ADD/EDIT ITEM MODAL ═══ -->
<div class="modal-overlay" id="item-modal" onclick="if(event.target===this)closeItemModal()">
  <div class="modal" style="max-width:500px;">
    <span class="modal-close-x" onclick="closeItemModal()">×</span>
    <div class="modal-title" id="item-modal-title">Add Attribute</div>
    <div class="form-group" style="margin-bottom:12px;">
      <label>Attribute Name</label>
      <input type="text" id="im-name" placeholder="e.g. Active Listening"/>
    </div>
    <div class="form-group" style="margin-bottom:12px;">
      <label>Description</label>
      <input type="text" id="im-desc" placeholder="Short explanation..."/>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeItemModal()">Cancel</button>
      <button class="btn btn-primary" onclick="saveItem()">Save Attribute</button>
    </div>
  </div>
</div>

<!-- ═══ ADD/EDIT MEMBER MODAL ═══ -->
<div class="modal-overlay" id="member-modal" onclick="if(event.target===this)closeMemberModal()">
  <div class="modal" style="max-width:600px;position:relative;">
    <span class="modal-close-x" onclick="closeMemberModal()">×</span>
    <div class="modal-title" id="member-modal-title">Add Agent</div>

    <!-- Tabs — only show for agents -->
    <div id="member-modal-tabs" class="tabs" style="margin-bottom:18px;">
      <div class="tab-btn active" onclick="switchMemberTab('single',this)">👤 Single</div>
      <div class="tab-btn" onclick="switchMemberTab('bulk',this)">📤 Bulk Upload</div>
    </div>

    <!-- SINGLE TAB -->
    <div id="member-tab-single">
      <div class="form-row cols-2">
        <div class="form-group"><label>Full Name</label><input type="text" id="mm-name" placeholder="Full name"/></div>
        <div class="form-group"><label>Email</label><input type="email" id="mm-email" placeholder="email@company.com"/></div>
      </div>
      <div class="form-row cols-2">
        <div class="form-group"><label>Phone</label><input type="text" id="mm-phone" placeholder="+20 10..."/></div>
        <div class="form-group"><label>Status</label>
          <select id="mm-status"><option value="Active">Active</option><option value="Inactive">Inactive</option></select>
        </div>
      </div>
      <div id="mm-agent-fields">
        <div class="form-row cols-2">
          <div class="form-group"><label>Team</label>
            <select id="mm-team"><option>Team A</option><option>Team B</option><option>Team C</option><option>Team D</option></select>
          </div>
          <div class="form-group"><label>Channel</label>
            <select id="mm-channel"><option>📞 Phone</option><option>💬 Chat</option><option>📧 Email</option></select>
          </div>
        </div>
      </div>

      <!-- Workspace assignment — specialists only -->
      <div id="mm-specialist-fields" style="display:none;">
        <div style="margin-top:4px;padding-top:16px;border-top:1px solid var(--border);">
          <div style="font-size:10px;color:var(--accent2);font-family:'DM Mono',monospace;letter-spacing:2px;text-transform:uppercase;margin-bottom:4px;">Assigned Workspaces <span style="color:var(--danger);">*</span></div>
          <div style="font-size:12px;color:var(--text2);margin-bottom:10px;">Select every workspace this specialist is allowed to evaluate. Their name will appear in the scorecard dropdown for those workspaces only.</div>
          <div id="mm-ws-grid" style="display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:10px;"></div>
          <div style="display:flex;gap:8px;">
            <button type="button" onclick="mmSelectAllWs(true)" style="padding:4px 12px;background:rgba(79,142,247,0.1);border:1px solid rgba(79,142,247,0.3);color:var(--accent);border-radius:var(--radius-xs);font-size:11px;font-weight:600;cursor:pointer;font-family:'DM Sans',sans-serif;">✓ All</button>
            <button type="button" onclick="mmSelectAllWs(false)" style="padding:4px 12px;background:var(--surface2);border:1px solid var(--border);color:var(--text2);border-radius:var(--radius-xs);font-size:11px;cursor:pointer;font-family:'DM Sans',sans-serif;">✕ None</button>
          </div>
        </div>
      </div>

      <div class="modal-footer">
        <button class="btn btn-ghost" onclick="closeMemberModal()">Cancel</button>
        <button class="btn btn-primary" onclick="saveMember()">Save</button>
      </div>
    </div>

    <!-- BULK TAB -->
    <div id="member-tab-bulk" style="display:none;">
      <!-- Download template -->
      <div style="padding:12px 14px;background:var(--accent-glow);border:1px solid rgba(79,142,247,0.2);border-radius:var(--radius-sm);margin-bottom:14px;display:flex;align-items:center;justify-content:space-between;">
        <div>
          <div style="font-size:13px;font-weight:600;color:var(--accent);">📥 Download CSV Template</div>
          <div style="font-size:11px;color:var(--text2);margin-top:2px;">Fill in the template then upload it below</div>
        </div>
        <button class="btn btn-ghost btn-sm" onclick="downloadBulkTemplate()">↓ Template</button>
      </div>

      <!-- CSV format guide -->
      <div style="padding:10px 12px;background:var(--surface2);border-radius:var(--radius-sm);margin-bottom:14px;font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;line-height:1.8;">
        <div style="color:var(--text);font-weight:600;margin-bottom:4px;font-family:'DM Sans',sans-serif;font-size:12px;">CSV Format:</div>
        name, email, phone, team, channel, status<br/>
        <span style="color:var(--accent2);">Ahmed Hassan, <a href="/cdn-cgi/l/email-protection" class="__cf_email__" data-cfemail="b6d7dedbd3d2f6d5d998d5d9db">[email&#160;protected]</a>, +201000000001, Team A, Phone, Active</span><br/>
        <span style="color:var(--accent2);">Nour Saleh, <a href="/cdn-cgi/l/email-protection" class="__cf_email__" data-cfemail="8ae4e5fff8cae9e5a4e9e5e7">[email&#160;protected]</a>, +201000000002, Team B, Chat, Active</span>
      </div>

      <!-- Drop zone -->
      <div id="bulk-dropzone"
        ondragover="event.preventDefault();this.classList.add('drag-over')"
        ondragleave="this.classList.remove('drag-over')"
        ondrop="handleBulkDrop(event)"
        onclick="document.getElementById('bulk-file-input').click()"
        style="border:2px dashed var(--border);border-radius:var(--radius-sm);padding:32px 20px;text-align:center;cursor:pointer;transition:all .2s;margin-bottom:14px;">
        <div style="font-size:32px;margin-bottom:8px;">📂</div>
        <div style="font-size:14px;font-weight:600;color:var(--text);">Drop CSV file here</div>
        <div style="font-size:12px;color:var(--text2);margin-top:4px;">or click to browse</div>
        <input type="file" id="bulk-file-input" accept=".csv" style="display:none;" onchange="handleBulkFile(this)"/>
      </div>

      <!-- Preview table -->
      <div id="bulk-preview" style="display:none;">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;">
          <div style="font-size:13px;font-weight:600;" id="bulk-preview-count"></div>
          <button class="btn btn-ghost btn-xs" onclick="clearBulkPreview()">✕ Clear</button>
        </div>
        <div style="max-height:200px;overflow-y:auto;border:1px solid var(--border);border-radius:var(--radius-sm);">
          <table style="width:100%;font-size:11px;">
            <thead><tr id="bulk-preview-head"></tr></thead>
            <tbody id="bulk-preview-tbody"></tbody>
          </table>
        </div>
        <div id="bulk-errors" style="margin-top:8px;"></div>
      </div>

      <div class="modal-footer">
        <button class="btn btn-ghost" onclick="closeMemberModal()">Cancel</button>
        <button class="btn btn-primary" id="bulk-submit-btn" onclick="submitBulkAgents()" style="display:none;">✓ Import Agents</button>
      </div>
    </div>
  </div>
</div>

<!-- ═══ AGENT DRILL-DOWN MODAL ═══ -->
<div class="modal-overlay" id="agent-modal" onclick="if(event.target===this)closeAgentModal()">
  <div class="modal agent-modal">
    <span class="modal-close-x" onclick="closeAgentModal()">×</span>
    <div class="agent-modal-header">
      <div class="agent-modal-avatar" id="am-avatar" style="background:linear-gradient(135deg,var(--accent),var(--accent2));"></div>
      <div>
        <div class="agent-modal-name" id="am-name">—</div>
        <div class="agent-modal-meta" id="am-meta">—</div>
      </div>
      <div style="margin-left:auto;text-align:right;">
        <div id="am-score-badge"></div>
      </div>
    </div>
    <div class="agent-kpi-row">
      <div class="agent-kpi"><div class="agent-kpi-val" id="am-score" style="color:var(--accent);">—</div><div class="agent-kpi-lbl">Avg Score</div></div>
      <div class="agent-kpi"><div class="agent-kpi-val" id="am-evals" style="color:var(--green);">—</div><div class="agent-kpi-lbl">Total Evals</div></div>
      <div class="agent-kpi"><div class="agent-kpi-val" id="am-trend-v" style="color:var(--accent2);">—</div><div class="agent-kpi-lbl">Trend</div></div>
      <div class="agent-kpi"><div class="agent-kpi-val" id="am-last" style="color:var(--text2);font-size:13px;">—</div><div class="agent-kpi-lbl">Last Eval</div></div>
    </div>
    <div class="card" style="margin-bottom:12px;">
      <div class="card-title" style="margin-bottom:10px;">Score Breakdown</div>
      <div style="display:flex;flex-direction:column;gap:8px;" id="am-breakdown"></div>
    </div>
    <div style="display:flex;gap:8px;">
      <button class="btn btn-ghost" style="flex:1" onclick="closeAgentModal()">Close</button>
      <button class="btn btn-teal" style="flex:1" onclick="closeAgentModal();showPage('feedback',null)">Send Feedback</button>
      <button class="btn btn-primary" style="flex:1" onclick="closeAgentModal();showPage('scorecard',null)">New Eval</button>
    </div>
  </div>
</div>

<!-- ═══ EMAIL MODAL ═══ -->
<div class="email-modal-overlay" id="email-modal">
  <div class="email-modal">
    <div class="email-modal-topbar">
      <div class="email-modal-topbar-title">📧 Agent Score Notification — Preview</div>
      <span class="email-modal-close" onclick="document.getElementById('email-modal').classList.remove('open')">×</span>
    </div>
    <div class="email-preview-wrap">
      <div class="ep-header">
        <div class="ep-company">QMS Pro · Quality Management</div>
        <div class="ep-score-row">
          <div class="ep-score-num" id="ep-score-num">84%</div>
          <div class="ep-score-right">
            <div class="ep-score-label">Quality Score</div>
            <div id="ep-badge" class="ep-badge ep-badge-pass">✅ Pass</div>
          </div>
        </div>
        <div class="ep-meta">
          <div class="ep-meta-item">Agent: <strong id="ep-agent-name">—</strong></div>
          <div class="ep-meta-item">Channel: <strong id="ep-channel">—</strong></div>
          <div class="ep-meta-item">Evaluated by: <strong id="ep-specialist">—</strong></div>
          <div class="ep-meta-item">Date: <strong id="ep-date">—</strong></div>
        </div>
      </div>
      <div class="ep-body">
        <div class="ep-greeting" id="ep-greeting">Dear Agent,</div>
        <div class="ep-intro" id="ep-intro">Your quality evaluation has been completed.</div>
        <div class="ep-section-label">Score Breakdown</div>
        <div class="ep-breakdown">
          <div class="ep-bitem"><div class="ep-bitem-label">Call Opening &amp; Closure</div><div><span class="ep-bitem-score" id="ep-b-opening">0</span><span class="ep-bitem-max"> / 20 pts</span></div><div class="ep-bitem-bar"><div class="ep-bitem-fill" id="ep-bf-opening" style="width:0%;background:#4f8ef7;"></div></div></div>
          <div class="ep-bitem"><div class="ep-bitem-label">Attitude &amp; Tone</div><div><span class="ep-bitem-score" id="ep-b-res">0</span><span class="ep-bitem-max"> / 25 pts</span></div><div class="ep-bitem-bar"><div class="ep-bitem-fill" id="ep-bf-res" style="width:0%;background:#00e5c3;"></div></div></div>
          <div class="ep-bitem"><div class="ep-bitem-label">Hold + Sales Expertise</div><div><span class="ep-bitem-score" id="ep-b-comms">0</span><span class="ep-bitem-max"> / 55 pts</span></div><div class="ep-bitem-bar"><div class="ep-bitem-fill" id="ep-bf-comms" style="width:0%;background:#f7b94f;"></div></div></div>
          <div class="ep-bitem"><div class="ep-bitem-label">Call Performance</div><div><span class="ep-bitem-score" id="ep-b-closing">0</span><span class="ep-bitem-max"> / 40 pts</span></div><div class="ep-bitem-bar"><div class="ep-bitem-fill" id="ep-bf-closing" style="width:0%;background:#3dd68c;"></div></div></div>
        </div>
        <div class="ep-notes"><div class="ep-notes-label">Strengths</div><div class="ep-notes-text" id="ep-strengths-text">—</div></div>
        <div class="ep-notes"><div class="ep-notes-label">Areas for Improvement</div><div class="ep-notes-text" id="ep-improve-text">—</div></div>
        <div class="ep-action" id="ep-action-box"><div class="ep-action-label" id="ep-action-label">Action Required</div><div class="ep-action-text" id="ep-action-text">—</div></div>
        <div class="ep-footer">This evaluation was conducted as part of our Quality Management Program. If you have questions about your score, please speak with your QA Specialist or Team Leader. You may raise a formal dispute within 48 hours.<br><br>QMS Pro · Quality Assurance Team</div>
      </div>
    </div>
    <div class="email-modal-actions">
      <div class="email-modal-hint" id="email-modal-hint">Sending to: <strong id="ep-to-email"><a href="/cdn-cgi/l/email-protection" class="__cf_email__" data-cfemail="e9888e8c879da98a868499888790c78a8684">[email&#160;protected]</a></strong></div>
      <button class="btn btn-ghost btn-sm" onclick="copyEmailBody()">📋 Copy</button>
      <button class="btn btn-outlook btn-sm" onclick="openOutlook()">Outlook ↗</button>
      <button class="btn btn-gmail btn-sm" onclick="openGmail()">Gmail ↗</button>
    </div>
  </div>
</div>

<!-- ═══ QUICK EVAL MODAL ═══ -->
<div class="modal-overlay" id="eval-modal" onclick="if(event.target===this)document.getElementById('eval-modal').classList.remove('open')">
  <div class="modal" style="max-width:560px;">
    <span class="modal-close-x" onclick="document.getElementById('eval-modal').classList.remove('open')">×</span>
    <div class="modal-title">➕ New Evaluation</div>

    <!-- Step 1: Workspace -->
    <div class="form-group" style="margin-bottom:12px;">
      <label style="font-size:12px;font-weight:700;color:var(--accent);">1️⃣ Workspace</label>
      <select id="eval-workspace" onchange="onEvalWsChange()" style="width:100%;"></select>
    </div>

    <!-- Step 2: Agent -->
    <div class="form-group" style="margin-bottom:12px;">
      <label style="font-size:12px;font-weight:700;color:var(--accent);">2️⃣ Agent</label>
      <select id="eval-agent" style="width:100%;" onchange="onEvalAgentChange()">
        <option value="">— Select workspace first —</option>
      </select>
    </div>

    <!-- Step 3: Details row -->
    <div class="form-row cols-3" style="margin-bottom:12px;">
      <div class="form-group">
        <label>Channel</label>
        <select id="eval-channel">
          <option value="Phone">📞 Phone</option>
          <option value="Chat">💬 Chat</option>
          <option value="Email">📧 Email</option>
        </select>
      </div>
      <div class="form-group">
        <label>QA Specialist</label>
        <select id="eval-specialist"></select>
      </div>
      <div class="form-group">
        <label>Date</label>
        <input type="date" id="eval-date"/>
      </div>
    </div>

    <!-- Quick Score (for quick submit only) -->
    <div id="eval-score-row" style="margin-bottom:12px;padding:10px 14px;background:var(--surface2);border-radius:var(--radius-sm);border:1px solid var(--border);">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;">
        <label style="font-size:12px;font-weight:600;">⚡ Quick Score</label>
        <span id="eval-score-display" style="font-family:'DM Mono',monospace;font-size:16px;font-weight:700;color:var(--accent);">80</span>
      </div>
      <input type="range" id="eval-score" min="0" max="100" value="80" oninput="updateEvalScore(this.value)" style="width:100%;"/>
      <div style="display:flex;justify-content:space-between;font-size:10px;color:var(--text2);margin-top:4px;">
        <span>0 — Fail</span><span>70 — Coaching</span><span>80 — Pass</span><span>90+ — Excellent</span>
      </div>
    </div>

    <!-- Workspace preview badge -->
    <div id="eval-ws-preview" style="display:none;padding:8px 12px;background:var(--surface2);border-radius:var(--radius-sm);border:1px solid var(--border);margin-bottom:12px;display:flex;align-items:center;gap:10px;">
      <div id="eval-ws-dot" style="width:8px;height:8px;border-radius:50%;background:var(--accent);flex-shrink:0;"></div>
      <div>
        <div style="font-size:11px;color:var(--text2);">Saving to workspace:</div>
        <div id="eval-ws-preview-name" style="font-size:13px;font-weight:700;color:var(--accent);"></div>
      </div>
      <div id="eval-agent-info" style="margin-left:auto;text-align:right;font-size:11px;color:var(--text2);"></div>
    </div>

    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="document.getElementById('eval-modal').classList.remove('open')">Cancel</button>
      <button class="btn btn-ghost" onclick="goFullScorecard()">📋 Full Scorecard</button>
      <button class="btn btn-primary" onclick="quickSubmit()">⚡ Submit</button>
    </div>
  </div>
</div>

<!-- ═══ TOAST ═══ -->
<div class="toast" id="toast"><span id="toast-msg">Action completed</span></div>

<!-- ═══ FIREBASE SDK (compat — works with onclick handlers) ═══ -->
<script src="https://www.gstatic.com/firebasejs/10.8.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.8.0/firebase-auth-compat.js"></script>
<script>
// Firebase compat SDK — global functions

// ══════════════════════════════════════════════
// PASTE YOUR FIREBASE CONFIG HERE  ↓↓↓
// ══════════════════════════════════════════════
const firebaseConfig = {
  apiKey:            "AIzaSyBSbyGxP6P6j2NnhMD0uyAiEVtU-eccI0s",
  authDomain:        "taager-qms.firebaseapp.com",
  projectId:         "taager-qms",
  storageBucket:     "taager-qms.firebasestorage.app",
  messagingSenderId: "1011634206606",
  appId:             "1:1011634206606:web:58cb5cb5475195dacb16df",
  measurementId:     "G-LDKJD8Z8LF"
};
// ══════════════════════════════════════════════

// ── Init Firebase ──
const _fbApp  = firebase.initializeApp(firebaseConfig);
const _fbDb   = firebase.firestore();
const _fbAuth = firebase.auth();
let _firebaseConnected = false;
const AUTH_TOKEN_STORAGE_KEY = 'qms-firebase-auth-token';

async function storeAuthToken(authUser) {
  if (!authUser) {
    clearStoredAuthToken();
    return null;
  }
  try {
    const tokenResult = await authUser.getIdTokenResult();
    const tokenPayload = {
      token: tokenResult.token,
      uid: authUser.uid,
      email: authUser.email || '',
      refreshToken: authUser.refreshToken || '',
      expiresAt: tokenResult.expirationTime || '',
      issuedAt: tokenResult.issuedAtTime || '',
      savedAt: new Date().toISOString()
    };
    localStorage.setItem(AUTH_TOKEN_STORAGE_KEY, JSON.stringify(tokenPayload));
    return tokenPayload;
  } catch (e) {
    console.warn('Failed to store Firebase auth token:', e.message || e);
    return null;
  }
}

function getStoredAuthToken() {
  try {
    const raw = localStorage.getItem(AUTH_TOKEN_STORAGE_KEY);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch (e) {
    console.warn('Failed to read stored auth token:', e.message || e);
    return null;
  }
}

function clearStoredAuthToken() {
  try {
    localStorage.removeItem(AUTH_TOKEN_STORAGE_KEY);
  } catch (e) {
    console.warn('Failed to clear stored auth token:', e.message || e);
  }
}

window.getSavedAuthToken = function() {
  return getStoredAuthToken();
};

function setFirebaseStatus(text, tone) {
  const el = document.getElementById('firebase-status');
  if (!el) return;
  el.textContent = text;
  if (tone === 'ok') el.style.color = 'var(--green)';
  else if (tone === 'error') el.style.color = 'var(--danger)';
  else el.style.color = 'var(--text2)';
}

async function checkFirebaseConnection() {
  setFirebaseStatus('Firebase: checking connection...', 'info');
  try {
    const timeoutMs = 8000;
    const probe = getDoc(doc(_fbDb, 'config', 'app'));
    await Promise.race([
      probe,
      new Promise((_, reject) => setTimeout(() => reject(new Error('Connection timed out')), timeoutMs))
    ]);
    _firebaseConnected = true;
    setFirebaseStatus('Firebase: connected', 'ok');
    console.log('Firebase connectivity check passed');
    return true;
  } catch (e) {
    _firebaseConnected = false;
    setFirebaseStatus('Firebase: not reachable (' + (e.message || 'unknown error') + ')', 'error');
    console.warn('Firebase connectivity check failed:', e.message || e);
    return false;
  }
}

// Compat wrappers so existing DataLayer code works unchanged
function collection(db,name){return db.collection(name);}
function doc(db,...args){if(args.length===1)return db.doc(args[0]);return db.collection(args[0]).doc(args[1]);}
function getDoc(ref){return ref.get().then(s=>({exists:()=>s.exists,data:()=>s.data(),id:s.id}));}
function getDocs(q){return q.get().then(s=>({docs:s.docs.map(d=>({id:d.id,data:()=>d.data()})),empty:s.empty}));}
function addDoc(colRef,data){return colRef.add(data);}
function setDoc(ref,data,opts){return opts&&opts.merge?ref.set(data,{merge:true}):ref.set(data);}
function updateDoc(ref,data){return ref.update(data);}
function deleteDoc(ref){return ref.delete();}
function onSnapshot(q,cb){return q.onSnapshot(s=>{cb({docs:s.docs.map(d=>({id:d.id,data:()=>d.data()}))});});}
function where(f,op,v){return {type:'where',f,op,v};}
function orderBy(f,d){return {type:'orderBy',f,d};}
function limit(n){return {type:'limit',n};}
function query(ref,...cs){let q=ref;cs.forEach(c=>{if(!c)return;if(c.type==='where')q=q.where(c.f,c.op,c.v);else if(c.type==='orderBy')q=q.orderBy(c.f,c.d||'asc');else if(c.type==='limit')q=q.limit(c.n);});return q;}
function signInWithEmailAndPassword(auth,email,pass){return auth.signInWithEmailAndPassword(email,pass);}

// ── Override DataLayer with Firebase after app loads ──
// Called once DOM + local data is ready
function connectFirebase() {
  checkFirebaseConnection();

  function usernameFromEmail(email) {
    const e = String(email || '').trim().toLowerCase();
    if (!e.includes('@')) return '';
    return e.split('@')[0];
  }

  async function enrichUserProfileDocument(docId, data) {
    try {
      await setDoc(doc(_fbDb, 'users', docId), data, {merge:true});
    } catch (e) {
      console.warn('Unable to enrich Firebase users profile:', e.message || e);
    }
  }

  async function createProfileFromAuthUser(authUser) {
    if(!authUser || !authUser.uid) return null;
    const email = String(authUser.email || '').trim().toLowerCase();
    const username = usernameFromEmail(email) || authUser.uid.slice(0,8);
    const name = authUser.displayName || username || 'User';
    const initials = name.split(' ').map(w=>w[0]||'').join('').slice(0,2).toUpperCase() || 'US';
    const profile = {
      uid: authUser.uid,
      email,
      username,
      name,
      initials,
      role: 'agent',
      status: 'Active',
      autoProvisioned: true,
      createdAt: new Date().toISOString()
    };
    await setDoc(doc(_fbDb, 'users', authUser.uid), profile, {merge:true});
    return { id: authUser.uid, ...profile };
  }

  async function loadUserProfileFromFirestore(authUser, loginId) {
    let profileDoc = null;

    const byUid = await getDocs(query(collection(_fbDb,'users'), where('uid','==',authUser.uid), limit(1)));
    if(!byUid.empty) profileDoc = byUid.docs[0];

    if(!profileDoc && authUser.email) {
      const byEmail = await getDocs(query(collection(_fbDb,'users'), where('email','==',authUser.email.toLowerCase()), limit(1)));
      if(!byEmail.empty) profileDoc = byEmail.docs[0];
    }

    if(!profileDoc && authUser.email) {
      const byEmailExact = await getDocs(query(collection(_fbDb,'users'), where('email','==',authUser.email), limit(1)));
      if(!byEmailExact.empty) profileDoc = byEmailExact.docs[0];
    }

    if(!profileDoc && loginId && !String(loginId).includes('@')) {
      const byUsername = await getDocs(query(collection(_fbDb,'users'), where('username','==',String(loginId).toLowerCase()), limit(1)));
      if(!byUsername.empty) profileDoc = byUsername.docs[0];
    }

    if(!profileDoc && loginId && !String(loginId).includes('@')) {
      const byUsernameExact = await getDocs(query(collection(_fbDb,'users'), where('username','==',String(loginId)), limit(1)));
      if(!byUsernameExact.empty) profileDoc = byUsernameExact.docs[0];
    }

    if(!profileDoc && authUser.email) {
      const byEmailLocalPart = await getDocs(query(collection(_fbDb,'users'), where('username','==',usernameFromEmail(authUser.email)), limit(1)));
      if(!byEmailLocalPart.empty) profileDoc = byEmailLocalPart.docs[0];
    }

    if(!profileDoc) return null;
    const profile = profileDoc.data() || {};
    if(!profile.role || !ROLES[profile.role]) return null;

    const name = profile.name || authUser.displayName || profile.email || authUser.email || 'User';
    const initials = profile.initials || name.split(' ').map(w=>w[0]||'').join('').slice(0,2).toUpperCase() || 'US';
    await enrichUserProfileDocument(profileDoc.id, {
      uid: authUser.uid,
      email: profile.email || authUser.email || '',
      username: profile.username || usernameFromEmail(authUser.email) || ''
    });
    return {
      id: profileDoc.id,
      ...profile,
      uid: authUser.uid,
      email: profile.email || authUser.email || '',
      name,
      initials
    };
  }

  // Replace DataLayer methods with Firebase versions
  DataLayer.findUser = async function(loginId, password) {
    if(!_fbAuth || !_fbDb) throw new Error('Firebase Auth is not initialized.');
    const id = String(loginId || '').trim().toLowerCase();
    if(!id || !password) return null;

    let email = id;
    let profileDoc = null;

    // Username login: resolve email from Firestore user profile first.
    if(!id.includes('@')) {
      let snap = await getDocs(query(collection(_fbDb,'users'), where('username','==',id), limit(1)));
      if(snap.empty) {
        snap = await getDocs(query(collection(_fbDb,'users'), where('username','==',String(loginId || '').trim()), limit(1)));
      }
      if(snap.empty) return null;
      profileDoc = snap.docs[0];
      const profile = profileDoc.data() || {};
      email = String(profile.email || '').trim();
      if(!email) throw new Error('Missing email in Firebase users profile.');
    }

    const authResult = await signInWithEmailAndPassword(_fbAuth, email, password);
    const authUser = authResult && authResult.user ? authResult.user : null;
    if(!authUser) return null;

    if(profileDoc) {
      const profile = profileDoc.data() || {};
      if(!profile.role || !ROLES[profile.role]) throw new Error('User role is missing in Firebase users profile.');
      const name = profile.name || authUser.displayName || profile.email || authUser.email || 'User';
      const initials = profile.initials || name.split(' ').map(w=>w[0]||'').join('').slice(0,2).toUpperCase() || 'US';
      return {
        id: profileDoc.id,
        ...profile,
        uid: authUser.uid,
        email: profile.email || authUser.email || '',
        name,
        initials
      };
    }

    // Email login: user must still exist in Firestore users collection with role.
    let profile = await loadUserProfileFromFirestore(authUser, id);
    if(!profile) {
      profile = await createProfileFromAuthUser(authUser);
      if(profile) {
        showToast('First login detected. Profile created with Agent role.');
      }
    }
    if(!profile) throw new Error('No matching Firebase users profile was found for this account.');
    return profile;
  };

  _fbAuth.onAuthStateChanged(async function(authUser){
    if(!authUser){
      clearStoredAuthToken();
      currentUser = null;
      return;
    }
    await storeAuthToken(authUser);
    if(currentUser) return;
    try {
      let profile = await loadUserProfileFromFirestore(authUser, authUser.email || '');
      if(!profile) profile = await createProfileFromAuthUser(authUser);
      if(!profile) return;
      currentUser = profile;
      const userWs = DB.workspaces.find(ws=>!ws.isGlobal && ws.users && ws.users.includes(profile.username));
      activeWorkspaceId = (userWs && profile.role !== 'admin' && profile.role !== 'qa-manager') ? userWs.id : 'ws-global';
      document.getElementById('login-screen').style.display='none';
      document.querySelector('.sidebar').style.display='flex';
      document.querySelector('.main').style.display='flex';
      applyRoleUI();
      const ws = getActiveWs();
      document.getElementById('topbar-ws-name').textContent = ws.name;
      document.getElementById('topbar-ws-dot').style.background = ws.color;
      renderWsList();
      const role = ROLES[profile.role];
      const firstPage = role && role.pages[0] ? role.pages[0] : 'dashboard';
      showPage(firstPage,null);
    } catch(e) {
      console.warn('Firebase auth session restore failed:', e.message || e);
    }
  });

  DataLayer.getUsers = async function() {
    const snap = await getDocs(collection(_fbDb,'users'));
    return snap.docs.map(d=>({id:d.id,...d.data()}));
  };

  DataLayer.getAgents = async function() {
    const snap = await getDocs(query(collection(_fbDb,'agents'), orderBy('name')));
    return snap.docs.map(d=>({id:d.id,...d.data()}));
  };

  DataLayer.addAgent = async function(agent) {
    const ref = await addDoc(collection(_fbDb,'agents'), agent);
    return {...agent, id:ref.id};
  };

  DataLayer.updateAgent = async function(id, data) {
    await updateDoc(doc(_fbDb,'agents',id), data);
  };

  DataLayer.deleteAgent = async function(id) {
    await deleteDoc(doc(_fbDb,'agents',id));
  };

  DataLayer.getSpecialists = async function() {
    const snap = await getDocs(collection(_fbDb,'specialists'));
    return snap.docs.map(d=>({id:d.id,...d.data()}));
  };

  DataLayer.addSpecialist = async function(spec) {
    const ref = await addDoc(collection(_fbDb,'specialists'), spec);
    return {...spec, id:ref.id};
  };

  DataLayer.deleteSpecialist = async function(id) {
    await deleteDoc(doc(_fbDb,'specialists',id));
  };

  DataLayer.getEvals = async function(workspaceId) {
    let q = workspaceId && workspaceId !== 'ws-global'
      ? query(collection(_fbDb,'evaluations'), where('workspace','==',workspaceId), orderBy('createdAt','desc'), limit(200))
      : query(collection(_fbDb,'evaluations'), orderBy('createdAt','desc'), limit(200));
    const snap = await getDocs(q);
    return snap.docs.map(d=>({id:d.id,...d.data()}));
  };

  DataLayer.addEval = async function(evalData) {
    const data = {...evalData, createdAt: new Date().toISOString(), createdBy: _fbAuth.currentUser?.uid||'unknown'};
    const ref = await addDoc(collection(_fbDb,'evaluations'), data);
    return {...data, id:ref.id};
  };

  // Real-time eval listener — updates UI on every new eval
  DataLayer.listenEvals = function(workspaceId, callback) {
    let q = workspaceId && workspaceId !== 'ws-global'
      ? query(collection(_fbDb,'evaluations'), where('workspace','==',workspaceId), orderBy('createdAt','desc'), limit(200))
      : query(collection(_fbDb,'evaluations'), orderBy('createdAt','desc'), limit(200));
    return onSnapshot(q, snap => callback(snap.docs.map(d=>({id:d.id,...d.data()}))));
  };

  DataLayer.addDispute = async function(d) {
    const data = {...d, createdAt: new Date().toISOString()};
    const ref = await addDoc(collection(_fbDb,'disputes'), data);
    return {...data, id:ref.id};
  };

  DataLayer.updateDispute = async function(id, data) {
    await updateDoc(doc(_fbDb,'disputes',id), data);
  };

  DataLayer.addFeedback = async function(fb) {
    const data = {...fb, createdAt: new Date().toISOString()};
    const ref = await addDoc(collection(_fbDb,'feedback'), data);
    return {...data, id:ref.id};
  };

  DataLayer.saveScorecard = async function(sections, workspaceId) {
    const id = workspaceId || 'global';
    await setDoc(doc(_fbDb,'scorecard',id), {sections, updatedAt: new Date().toISOString()});
  };

  DataLayer.saveWorkspace = async function(ws) {
    const {evaluations, ...wsData} = ws;
    await setDoc(doc(_fbDb,'workspaces',ws.id), wsData, {merge:true});
  };

  DataLayer.saveConfig = async function(data) {
    await setDoc(doc(_fbDb,'config','app'), data, {merge:true});
  };

  // ── Load live data into DB ──
  Promise.all([
    getDocs(query(collection(_fbDb,'agents'), orderBy('name'))),
    getDocs(collection(_fbDb,'specialists')),
  ]).then(([agSnap, spSnap]) => {
    if (agSnap.docs.length > 0) {
      DB.agents = agSnap.docs.map(d=>({id:d.id,...d.data()}));
      teamAgents = DB.agents;
      renderTeamAgents();
      renderAgents();
      console.log('✅ Firebase agents loaded:', DB.agents.length);
      // Refresh dispute agent dropdowns if they are visible
      const d1 = document.getElementById('disp-agent');
      const d2 = document.getElementById('disp-agent-mgr');
      if(d1 || d2) initDisputeForm();
    }
    if (spSnap.docs.length > 0) {
      DB.specialists = spSnap.docs.map(d=>({id:d.id,...d.data()}));
      teamSpecs = DB.specialists;
      renderTeamSpecs();
      console.log('✅ Firebase specialists loaded:', DB.specialists.length);
      // Refresh specialist dropdown in eval modal if it is open
      const evalModal = document.getElementById('eval-modal');
      if(evalModal && evalModal.classList.contains('open')){
        const specSel = document.getElementById('eval-specialist');
        if(specSel && specSel.options.length <= 1){
          specSel.innerHTML = '<option value="">— Select specialist —</option>' +
            DB.specialists.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
        }
      }
    }
  }).catch(e => console.warn('Firebase data load failed, using local data:', e.message));

  // ── Real-time eval listener — re-subscribable on workspace switch ──
  let _unsubEvals = null;
  function _subscribeEvals(wsId) {
    if(_unsubEvals){ try{_unsubEvals();}catch(e){} _unsubEvals=null; }
    _unsubEvals = DataLayer.listenEvals(wsId, (liveEvals) => {
      DB.evaluations = liveEvals;
      renderRecentEvals();
      renderTracker();
      console.log('🔴 Live evals for', wsId, ':', liveEvals.length);
    });
  }
  _subscribeEvals(activeWorkspaceId);
  window._fbSubscribeEvals = _subscribeEvals; // expose globally

  console.log('🔥 Firebase connected and listening for real-time updates!');
}

// ── IN-APP SEED BUTTON ──
async function runSeedFirestore() {
  const btn = document.querySelector('button[onclick="runSeedFirestore()"]');
  const status = document.getElementById('seed-status');
  if (!status) return;

  const isConnected = await checkFirebaseConnection();
  if (!isConnected) {
    showToast('Firebase not connected yet - check your config or rules', 'error');
    return;
  }

  btn.disabled = true;
  btn.textContent = '⏳ Uploading...';
  status.style.display = 'block';

  const steps = [
    { label: 'Uploading agents...', fn: async () => {
      for (const a of DB.agents) await setDoc(doc(_fbDb,'agents',a.id), a);
      return DB.agents.length + ' agents';
    }},
    { label: 'Uploading specialists...', fn: async () => {
      for (const s of DB.specialists) await setDoc(doc(_fbDb,'specialists',s.id), s);
      return DB.specialists.length + ' specialists';
    }},
    { label: 'Uploading users...', fn: async () => {
      for (const u of DB.users) await setDoc(doc(_fbDb,'users',u.id), u);
      return DB.users.length + ' users';
    }},
    { label: 'Uploading roles...', fn: async () => {
      for (const [k,r] of Object.entries(DB.roles)) await setDoc(doc(_fbDb,'roles',k), r);
      return 'roles';
    }},
    { label: 'Uploading workspaces...', fn: async () => {
      for (const ws of DB.workspaces) {
        const {evaluations,...wsData} = ws;
        await setDoc(doc(_fbDb,'workspaces',ws.id), wsData);
      }
      return DB.workspaces.length + ' workspaces';
    }},
    { label: 'Uploading scorecard...', fn: async () => {
      await setDoc(doc(_fbDb,'scorecard','global'), {sections: DB.scorecard, updatedAt: new Date().toISOString()});
      return 'scorecard';
    }},
    { label: 'Uploading config...', fn: async () => {
      await setDoc(doc(_fbDb,'config','app'), DB.config);
      return 'config';
    }},
  ];

  try {
    for (const step of steps) {
      status.innerHTML = `⏳ ${step.label}`;
      const result = await step.fn();
      status.innerHTML = `✅ Uploaded: ${result}`;
      await new Promise(r => setTimeout(r, 300));
    }
    status.innerHTML = '🎉 All data uploaded to Firebase successfully!';
    status.style.color = 'var(--green)';
    btn.textContent = '✅ Upload Complete';
    btn.style.background = 'var(--green)';
    showToast('🎉 Firebase seeding complete!');
  } catch(e) {
    status.innerHTML = '❌ Error: ' + e.message;
    status.style.color = 'var(--danger)';
    btn.disabled = false;
    btn.textContent = '🔄 Retry Upload';
    showToast('Upload failed: ' + e.message, 'error');
  }
}

// Expose seedFirestore globally for console use
window.seedFirestore = async function() {
  console.log('🔥 Seeding Firestore...');
  const batch = [];
  for (const agent of DB.agents) {
    batch.push(setDoc(doc(_fbDb,'agents',agent.id), agent));
  }
  for (const spec of DB.specialists) {
    batch.push(setDoc(doc(_fbDb,'specialists',spec.id), spec));
  }
  for (const user of DB.users) {
    batch.push(setDoc(doc(_fbDb,'users',user.id), user));
  }
  for (const ws of DB.workspaces) {
    const {evaluations,...wsData}=ws;
    batch.push(setDoc(doc(_fbDb,'workspaces',ws.id), wsData));
  }
  for (const [key,role] of Object.entries(DB.roles)) {
    batch.push(setDoc(doc(_fbDb,'roles',key), role));
  }
  batch.push(setDoc(doc(_fbDb,'config','app'), DB.config));
  batch.push(setDoc(doc(_fbDb,'scorecard','global'), {sections:DB.scorecard, updatedAt:new Date().toISOString()}));
  await Promise.all(batch);
  console.log('🎉 Seeding complete! Refresh the page.');
};
// ╔══════════════════════════════════════════════════════════════╗
// ║               QMS PRO — CENTRAL DATA STORE                  ║
// ║  All app data lives here. Firebase will replace DataLayer.  ║
// ╚══════════════════════════════════════════════════════════════╝

// ── HELPERS ──
function randInt(a,b){return Math.floor(Math.random()*(b-a+1))+a;}
function randItem(arr){return arr[Math.floor(Math.random()*arr.length)];}
function genId(){return 'x'+Math.random().toString(36).slice(2,9);}
function scoreColor(s){return s>=90?'var(--green)':s>=80?'var(--accent)':s>=70?'var(--accent3)':'var(--danger)';}
function scoreClass(s){return s>=80?'score-high':s>=70?'score-mid':'score-low';}
function statusBadge(st){
  if(st==='Pass') return '<span class="status-badge status-pass">Pass</span>';
  if(st==='Fail') return '<span class="status-badge status-fail">Fail</span>';
  return '<span class="status-badge status-pending">Pending</span>';
}
function chBadge(ch){
  if(ch==='Phone') return '<span class="ch-badge ch-phone">📞 Phone</span>';
  if(ch==='Chat') return '<span class="ch-badge ch-chat">💬 Chat</span>';
  return '<span class="ch-badge ch-email">📧 Email</span>';
}

// ── SEED DATA ──
const _channels=['Phone','Chat','Email'];
const _agentNames=['Shrouk Talaat Abas','Dalia Khaled Rabie','Bassant Ragab Abdelghafar','Karim Mohamed Mabrouk','Abdelrahman Ahmed Mousa Ahmed','Moamen Ahmed Sayed','Elham Moustafa Mohamed','Ahmed Mohyee Mohamed Saad','Nourhan Adel Emam','Rawan Yasser','Mohamed Ahmed Mohamed Hassan','Walaa Gamal Mohamed','Shahd Samir','Abdelrahman Alaa Abdelrahman','Ziad Eid Fathy Mohamed','Ali Esam Housny','Mohamed Hassan Mohamed Mohamed','Mai Yasser El Sayed Amer Hussain','Aya Elsayed Ibrahim','Salma Ragab Radwan','Norhan Nasser Mahmoud','Hager Yasser Saied','Belal Magdy Abdelfattah Abdelhalim','Mohamed Moustafa','Omar Ahmed','Islam Osama Abdelgalil','Nayera Abdallah','Esraa Ayman Mohamed Sayed','Sondos Ahmed','Malak Magdy','Sara Mohamed','Rawan Sayed Ahmed','Samia Mohamed Ali Mohamed','Mohamed Hany Saber Ahmed','Sandy Essam Nasry','Nadia Abdelfattah','Hager Abdelhady','Islam Nazeeh Gad'];
const _teams=['Team A','Team B','Team C','Team D'];
const _specialists=['Sara Ahmed','Mohamed Karim','Lina Rashid','Omar Tarek','Rania Fouad','Khaled Mostafa'];

// ╔══════════════════════════════════════════════════════════════╗
// ║                     CENTRAL DB OBJECT                       ║
// ║  Replace DataLayer methods with Firebase calls to go live   ║
// ╚══════════════════════════════════════════════════════════════╝
const DB = {
  // ── App Config ──
  config: {
    company: 'QMS Pro',
    dept: 'Quality Assurance',
    passThreshold: 80,
    excellentThreshold: 90,
    monthlyTarget: 1350,
    disputeWindow: 48,
    coachingThreshold: 75,
    defaultTheme: 'default',
  },

  // ── Users ──
  users: [
    {id:'u1', username:'admin',       password:'admin123',  role:'admin',        name:'Karim Hassan',    initials:'KH', email:'karimhassanps@gmail.com'},
    {id:'u2', username:'qa.manager',  password:'qa123',     role:'qa-manager',   name:'QA Manager',      initials:'QM'},
    {id:'u3', username:'sara.ahmed',  password:'spec123',   role:'qa-specialist',name:'Sara Ahmed',      initials:'SA'},
    {id:'u4', username:'mk',          password:'spec123',   role:'qa-specialist',name:'Mohamed Karim',   initials:'MK'},
    {id:'u5', username:'tl.cc',       password:'tl123',     role:'tl-cc',        name:'CC Team Leader',  initials:'TL'},
    {id:'u6', username:'ahmed.hassan',password:'agent123',  role:'agent',        name:'Ahmed Hassan',    initials:'AH'},
    {id:'u7', username:'nour.saleh',  password:'agent123',  role:'agent',        name:'Nour Saleh',      initials:'NS'},
  ],

  // ── Roles & Permissions ──
  roles: {
    'admin':        {label:'Full Admin',     badge:'role-admin',       color:'var(--purple)', canNewEval:true,  canDispute:true,  canExport:true,  canAdmin:true,  pages:['dashboard','scorecard','tracker','feedback','calibration','disputes','agents','specialists','reports','sc-builder','team-mgmt','role-mgmt','workspaces','ws-overview'], desc:'Full access to everything'},
    'qa-manager':   {label:'QA Manager',     badge:'role-qa-manager',  color:'var(--accent)', canNewEval:true,  canDispute:true,  canExport:true,  canAdmin:false, pages:['dashboard','scorecard','tracker','feedback','calibration','disputes','agents','specialists','reports','sc-builder','team-mgmt','workspaces','ws-overview'], desc:'Full QA access — monitor everything'},
    'qa-specialist':{label:'QA Specialist',  badge:'role-qa-specialist',color:'var(--accent2)',canNewEval:true,  canDispute:false, canExport:false, canAdmin:false, pages:['dashboard','scorecard','tracker','feedback','calibration','agents','specialists','sc-builder'], desc:'Can evaluate, send feedback, view dashboard'},
    'tl-cc':        {label:'CC Team Leader', badge:'role-tl-cc',       color:'var(--accent3)',canNewEval:false, canDispute:true,  canExport:false, canAdmin:false, pages:['dashboard','disputes','agents','specialists','tracker','team-mgmt'], desc:'Submit disputes, view agent performance'},
    'agent':        {label:'CC Agent',       badge:'role-agent',       color:'var(--green)',  canNewEval:false, canDispute:false, canExport:false, canAdmin:false, pages:['dashboard'], desc:'View own scores and evaluations only'},
  },

  // ── CC Agents (Team Members) ──
  agents: [
    // ── KSA Agents — Mar 1–30, 2026 ──
    {id:'a1', empId:'1784110', name:'Shrouk Talaat Abas',            email:'shrouk.talaat@co.com',    phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:22, score:92, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a2', empId:'1718843', name:'Dalia Khaled Rabie',            email:'dalia.khaled@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:24, audits:22, score:84, salesScore:'81.82%',  salesMistakes:4,  failedAudits:3},
    {id:'a3', empId:'1717557', name:'Bassant Ragab Abdelghafar',     email:'bassant.ragab@co.com',    phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:22, audits:21, score:67, salesScore:'66.67%',  salesMistakes:7,  failedAudits:7},
    {id:'a4', empId:'1786017', name:'Karim Mohamed Mabrouk',         email:'karim.mabrouk@co.com',   phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:21, score:83, salesScore:'80.95%',  salesMistakes:4,  failedAudits:3},
    {id:'a5', empId:'1799764', name:'Abdelrahman Ahmed Mousa Ahmed', email:'abdelrahman.mousa@co.com',phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:21, audits:19, score:46, salesScore:'73.68%',  salesMistakes:5,  failedAudits:9},
    {id:'a6', empId:'1717542', name:'Moamen Ahmed Sayed',            email:'moamen.ahmed@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:21, score:90, salesScore:'90.48%',  salesMistakes:2,  failedAudits:2},
    {id:'a7', empId:'181200',  name:'Elham Moustafa Mohamed',        email:'elham.moustafa@co.com',   phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:11, audits:11, score:96, salesScore:'100.00%', salesMistakes:0,  failedAudits:0},
    {id:'a8', empId:'1806929', name:'Ahmed Mohyee Mohamed Saad',     email:'ahmed.mohyee@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:25, audits:22, score:66, salesScore:'90.91%',  salesMistakes:2,  failedAudits:6},
    {id:'a9', empId:'1695272', name:'Nourhan Adel Emam',             email:'nourhan.adel@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:24, audits:24, score:94, salesScore:'91.67%',  salesMistakes:2,  failedAudits:1},
    {id:'a10',empId:'1789030', name:'Rawan Yasser',                  email:'rawan.yasser@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:30, audits:29, score:92, salesScore:'79.31%',  salesMistakes:6,  failedAudits:2},
    {id:'a11',empId:'1920879', name:'Mohamed Ahmed Mohamed Hassan',  email:'mohamed.ahmed.h@co.com',  phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:26, audits:22, score:89, salesScore:'90.91%',  salesMistakes:2,  failedAudits:2},
    {id:'a12',empId:'1743074', name:'Walaa Gamal Mohamed',           email:'walaa.gamal@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:19, audits:18, score:90, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a13',empId:'1941212', name:'Shahd Samir',                   email:'shahd.samir@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:18, audits:18, score:91, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a14',empId:'1767755', name:'Abdelrahman Alaa Abdelrahman',  email:'abdelrahman.alaa@co.com', phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:28, audits:26, score:36, salesScore:'-23.08%', salesMistakes:32, failedAudits:16},
    {id:'a15',empId:'1799749', name:'Ziad Eid Fathy Mohamed',        email:'ziad.eid@co.com',         phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:19, audits:14, score:46, salesScore:'50.00%',  salesMistakes:7,  failedAudits:7},
    {id:'a16',empId:'1762846', name:'Ali Esam Housny',               email:'ali.esam@co.com',         phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:25, audits:18, score:92, salesScore:'94.44%',  salesMistakes:1,  failedAudits:1},
    {id:'a17',empId:'1879862', name:'Mohamed Hassan Mohamed Mohamed',email:'mhm.mohamed@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:29, audits:27, score:86, salesScore:'96.30%',  salesMistakes:1,  failedAudits:3},
    {id:'a18',empId:'1879938', name:'Mai Yasser El Sayed Amer Hussain',email:'mai.yasser@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:24, audits:22, score:74, salesScore:'72.73%',  salesMistakes:6,  failedAudits:5},
    {id:'a19',empId:'1784145', name:'Aya Elsayed Ibrahim',           email:'aya.elsayed@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:18, audits:16, score:94, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a20',empId:'1762079', name:'Salma Ragab Radwan',            email:'salma.ragab@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:27, audits:24, score:94, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a21',empId:'1699478', name:'Norhan Nasser Mahmoud',         email:'norhan.nasser@co.com',    phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:24, audits:22, score:99, salesScore:'100.00%', salesMistakes:0,  failedAudits:0},
    {id:'a22',empId:'1695243', name:'Hager Yasser Saied',            email:'hager.yasser@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:25, audits:23, score:86, salesScore:'100.00%', salesMistakes:0,  failedAudits:3},
    // ── Image 2 ──
    {id:'a23',empId:'1799748', name:'Belal Magdy Abdelfattah Abdelhalim',email:'belal.magdy@co.com', phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:21, audits:18, score:21, salesScore:'-22.22%', salesMistakes:22, failedAudits:14},
    {id:'a24',empId:'1879851', name:'Mohamed Moustafa',              email:'mohamed.moustafa@co.com', phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:22, audits:19, score:89, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a25',empId:'1941280', name:'Omar Ahmed',                    email:'omar.ahmed@co.com',       phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:17, audits:15, score:85, salesScore:'93.33%',  salesMistakes:1,  failedAudits:1},
    {id:'a26',empId:'1980819', name:'Islam Osama Abdelgalil',        email:'islam.osama@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:22, score:69, salesScore:'95.45%',  salesMistakes:1,  failedAudits:6},
    {id:'a27',empId:'1164237', name:'Nayera Abdallah',               email:'nayera.abdallah@co.com',  phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:21, audits:21, score:84, salesScore:'100.00%', salesMistakes:0,  failedAudits:2},
    {id:'a28',empId:'1814858', name:'Esraa Ayman Mohamed Sayed',     email:'esraa.ayman@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:26, audits:22, score:83, salesScore:'81.82%',  salesMistakes:4,  failedAudits:3},
    {id:'a29',empId:'1941897', name:'Sondos Ahmed',                  email:'sondos.ahmed@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:21, audits:20, score:90, salesScore:'100.00%', salesMistakes:0,  failedAudits:1},
    {id:'a30',empId:'1941221', name:'Malak Magdy',                   email:'malak.magdy@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:22, audits:18, score:83, salesScore:'83.33%',  salesMistakes:3,  failedAudits:3},
    {id:'a31',empId:'1879991', name:'Sara Mohamed',                  email:'sara.mohamed@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:25, audits:23, score:51, salesScore:'60.87%',  salesMistakes:9,  failedAudits:9},
    {id:'a32',empId:'1980811', name:'Rawan Sayed Ahmed',             email:'rawan.sayed@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:22, audits:21, score:88, salesScore:'85.71%',  salesMistakes:3,  failedAudits:2},
    {id:'a33',empId:'1840630', name:'Samia Mohamed Ali Mohamed',     email:'samia.ali@co.com',        phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:22, audits:21, score:65, salesScore:'66.67%',  salesMistakes:7,  failedAudits:5},
    {id:'a34',empId:'1980820', name:'Mohamed Hany Saber Ahmed',      email:'mohamed.hany@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:18, score:86, salesScore:'94.44%',  salesMistakes:1,  failedAudits:2},
    {id:'a35',empId:'1980803', name:'Sandy Essam Nasry',             email:'sandy.essam@co.com',      phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:22, score:69, salesScore:'90.91%',  salesMistakes:2,  failedAudits:4},
    {id:'a36',empId:'1941234', name:'Nadia Abdelfattah',             email:'nadia.abd@co.com',        phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:20, audits:20, score:63, salesScore:'85.00%',  salesMistakes:3,  failedAudits:7},
    {id:'a37',empId:'1980982', name:'Hager Abdelhady',               email:'hager.abd@co.com',        phone:'', team:'KSA Team', channel:'📞 Phone', status:'Inactive',targetCalls:4,  audits:3,  score:58, salesScore:'N/A',     salesMistakes:0,  failedAudits:1},
    {id:'a38',empId:'1593317', name:'Islam Nazeeh Gad',              email:'islam.nazeeh@co.com',     phone:'', team:'KSA Team', channel:'📞 Phone', status:'Active',  targetCalls:23, audits:20, score:69, salesScore:'95.00%',  salesMistakes:1,  failedAudits:4},
  ],

  // ── QA Specialists ──
  specialists: [
    {id:'sp1',name:'Sara Ahmed',    email:'sara@co.com', phone:'+20 100 000 0101',evals:284,avgScore:97,status:'Active'},
    {id:'sp2',name:'Mohamed Karim', email:'mk@co.com',   phone:'+20 100 000 0102',evals:271,avgScore:94,status:'Active'},
    {id:'sp3',name:'Lina Rashid',   email:'lina@co.com', phone:'+20 100 000 0103',evals:263,avgScore:91,status:'Active'},
    {id:'sp4',name:'Omar Tarek',    email:'omar@co.com', phone:'+20 100 000 0104',evals:258,avgScore:89,status:'Active'},
    {id:'sp5',name:'Rania Fouad',   email:'rania@co.com',phone:'+20 100 000 0105',evals:247,avgScore:87,status:'Active'},
    {id:'sp6',name:'Khaled Mostafa',email:'km@co.com',   phone:'+20 100 000 0106',evals:239,avgScore:85,status:'Active'},
  ],

  // ── Evaluations ──
  evaluations: (()=>{
    const list=[];
    for(let i=0;i<80;i++){
      const score=randInt(55,100);
      const ch=randItem(_channels);
      const st=score>=80?'Pass':score>=70?(Math.random()>.5?'Pending':'Fail'):'Fail';
      list.push({id:`QA-2026-${String(i+1).padStart(4,'0')}`,agent:randItem(_agentNames),team:randItem(_teams),specialist:randItem(_specialists),channel:ch,score,status:st,date:`Mar ${randInt(1,28)}, 2026`});
    }
    return list;
  })(),

  // ── Disputes ──
  disputes: [
    {id:'DISP-2026-003',agent:'Ahmed Hassan',evalId:'QA-2026-0198',channel:'📞 Phone',score:62,reason:'I believe the empathy section score is incorrect.',evidence:'Timestamp 02:34',status:'Open',date:'Mar 26'},
    {id:'DISP-2026-002',agent:'Nour Saleh',  evalId:'QA-2026-0187',channel:'💬 Chat', score:71,reason:'Issue recurred due to a system bug, not agent error.',evidence:'Chat log #4421',status:'Open',date:'Mar 24'},
    {id:'DISP-2026-001',agent:'Yara Mostafa',evalId:'QA-2026-0175',channel:'📧 Email',score:69,reason:'Grammar flagged is part of the approved template.',evidence:'Template v3.2',status:'Review',date:'Mar 22'},
  ],

  // ── Scorecard Sections ──
  scorecard: [
    {id:'s1',name:'Call Opening & Closure',pts:20,type:'scored',items:[
      {id:'i1',name:'Customer Consent, Opening Clarity & Closure',desc:'Agent confirms readiness, greets properly, introduces self & page, closes professionally'},
    ]},
    {id:'s2',name:'Attitude & Tone',pts:25,type:'scored',items:[
      {id:'i2',name:'Active Listening',desc:'Giving space to talk, no interruption'},
      {id:'i3',name:'Professional Language',desc:'No negative words, slangs, repetition'},
      {id:'i4',name:'Empathy & Tone Behavior',desc:'Showing empathy, not fast/sleepy/aggressive'},
      {id:'i5',name:'Concentration & Dead Air',desc:'Focused on call, no silent moments'},
    ]},
    {id:'s3',name:'Hold Process',pts:15,type:'scored',items:[
      {id:'i6',name:'Hold Process Followed',desc:'Correct hold procedure used'},
    ]},
    {id:'s4',name:'Sales Expertise',pts:0,type:'fatal',items:[
      {id:'i7', name:'Alternative Suggestion',desc:'Suggested alternative when product not suitable'},
      {id:'i8', name:'Save Attempt',desc:'Final attempt to retain deal'},
      {id:'i9', name:'Relevance of Response',desc:'Objection handled logically'},
      {id:'i10',name:'Value Explanation',desc:'Focused on benefits not features'},
      {id:'i11',name:'Follow-up Planning',desc:'Handled WhatsApp/follow-up correctly'},
      {id:'i12',name:'Policy Knowledge',desc:'Explained policies clearly'},
      {id:'i13',name:'Pushing for Agreement',desc:'Did not pressure customer'},
      {id:'i14',name:'Warranty Mentioned',desc:'Mentioned warranty process'},
    ]},
    {id:'s5',name:'Call Performance',pts:40,type:'scored',items:[
      {id:'i15',name:'Alternative Number',desc:'Requested extra contact number'},
      {id:'i16',name:'Return Policy Explained',desc:'Explained return policy clearly'},
      {id:'i17',name:'Item Description Clarity',desc:'Explained color, name, quantity, price'},
      {id:'i18',name:'Delivery Details Provided',desc:'Mentioned delivery method and time'},
      {id:'i19',name:'Address Obtained',desc:'Got customer address accurately'},
      {id:'i20',name:'Bad Attitude',desc:'No inappropriate behavior'},
      {id:'i21',name:'Correct Action in System',desc:'Selected correct interaction type'},
    ]},
    {id:'s6',name:'Documentation',pts:0,type:'fatal',items:[
      {id:'i22',name:'Notes Quality',desc:'Wrote clear notes'},
      {id:'i23',name:'Fake Delivery Time',desc:'Did not give false delivery info'},
    ]},
    {id:'s7',name:'Fake Info & Actions',pts:0,type:'fatal',items:[
      {id:'i24',name:'Fake Confirmation',desc:'Did not confirm without proof'},
      {id:'i25',name:'Fake Price',desc:'Did not give wrong price'},
      {id:'i26',name:'Fake Specs',desc:'Did not give wrong specs'},
      {id:'i27',name:'Fake Product',desc:'Did not describe wrong product'},
      {id:'i28',name:'Fake Warranty',desc:'Did not give wrong warranty info'},
      {id:'i29',name:'Fake Agreement',desc:'Did not confirm without permission'},
      {id:'i30',name:'Fake Offers',desc:'Did not promise non-existent offers'},
      {id:'i31',name:'Invalid Lead',desc:'Lead meets qualification criteria'},
    ]},
  ],

  // ── Calibration Sessions ──
  calibrations: [
    {id:'c1',title:'March Phone Cal.',channel:'Phone',date:'Mar 15',variance:3.2,outcome:'Aligned'},
    {id:'c2',title:'Feb Chat Review', channel:'Chat', date:'Feb 20',variance:7.8,outcome:'Re-calibrate'},
    {id:'c3',title:'Feb Email Cal.',  channel:'Email',date:'Feb 10',variance:2.1,outcome:'Aligned'},
    {id:'c4',title:'Jan Full Cal.',   channel:'All',  date:'Jan 25',variance:11.4,outcome:'Misaligned'},
  ],

  // ── Feedback ──
  feedback: [
    {id:'f1',agent:'Karim Wael',   type:'Positive Recognition',score:91,channel:'Phone',specialist:'Sara Ahmed',   date:'Mar 25'},
    {id:'f2',agent:'Nour Saleh',   type:'Coaching Session',    score:71,channel:'Chat', specialist:'Mohamed Karim',date:'Mar 24'},
    {id:'f3',agent:'Ahmed Hassan', type:'Formal Warning',      score:62,channel:'Phone',specialist:'Manager',      date:'Mar 23'},
    {id:'f4',agent:'Fatima Al-Zahraa',type:'Coaching & Guidance',score:74,channel:'Email',specialist:'Lina Rashid',date:'Mar 22'},
  ],
};

// ╔══════════════════════════════════════════════════════════════╗
// ║                      DATA LAYER                             ║
// ║  Swap these functions with Firebase calls for live data     ║
// ╚══════════════════════════════════════════════════════════════╝
const DataLayer = {
  // CONFIG
  getConfig:      ()=> DB.config,
  saveConfig:     (data)=>{ Object.assign(DB.config, data); },

  // USERS
  getUsers:       ()=> DB.users,
  addUser:        (u)=>{ DB.users.push(u); },
  updateUser:     (id,data)=>{ const u=DB.users.find(x=>x.id===id); if(u) Object.assign(u,data); },
  deleteUser:     (id)=>{ DB.users=DB.users.filter(x=>x.id!==id); },
  findUser:       (username,password)=> DB.users.find(u=>u.username===username&&u.password===password),

  // ROLES
  getRoles:       ()=> DB.roles,
  updateRole:     (key,data)=>{ if(DB.roles[key]) Object.assign(DB.roles[key],data); },

  // AGENTS
  getAgents:      ()=> DB.agents,
  addAgent:       (a)=>{ DB.agents.push(a); },
  updateAgent:    (id,data)=>{ const a=DB.agents.find(x=>x.id===id); if(a) Object.assign(a,data); },
  deleteAgent:    (id)=>{ DB.agents=DB.agents.filter(x=>x.id!==id); },

  // SPECIALISTS
  getSpecialists: ()=> DB.specialists,
  addSpecialist:  (s)=>{ DB.specialists.push(s); },
  updateSpecialist:(id,data)=>{ const s=DB.specialists.find(x=>x.id===id); if(s) Object.assign(s,data); },
  deleteSpecialist:(id)=>{ DB.specialists=DB.specialists.filter(x=>x.id!==id); },

  // EVALUATIONS
  getEvals:       ()=> DB.evaluations,
  addEval:        (e)=>{ DB.evaluations.unshift(e); },
  updateEval:     (id,data)=>{ const e=DB.evaluations.find(x=>x.id===id); if(e) Object.assign(e,data); },

  // DISPUTES
  getDisputes:    ()=> DB.disputes,
  addDispute:     (d)=>{ DB.disputes.unshift(d); },
  updateDispute:  (id,data)=>{ const d=DB.disputes.find(x=>x.id===id); if(d) Object.assign(d,data); },

  // SCORECARD
  getScorecard:   ()=> DB.scorecard,
  saveScorecard:  (sections)=>{ DB.scorecard=sections; },

  // CALIBRATIONS
  getCalibrations:()=> DB.calibrations,
  addCalibration: (c)=>{ DB.calibrations.unshift(c); },

  // FEEDBACK
  getFeedback:    ()=> DB.feedback,
  addFeedback:    (f)=>{ DB.feedback.unshift(f); },
};

// ── CONVENIENCE ALIASES (used throughout the app) ──
// These let the rest of the code stay unchanged
const ROLES  = DB.roles;
const USERS  = DB.users;
let appConfig= DB.config;
let scSections= DB.scorecard;
let teamAgents= DB.agents;
let teamSpecs = DB.specialists;
const evals  = DB.evaluations;
const channels=_channels;
const teams  =_teams;

// agents & agentData for performance page (generated from DB.agents)
const agents = _agentNames;
const agentData=Array.from({length:30},(_,i)=>({
  name:_agentNames[i%_agentNames.length]+(_agentNames[i%_agentNames.length]&&i>=_agentNames.length?' '+String.fromCharCode(65+Math.floor(i/_agentNames.length)):''),
  team:randItem(_teams),channel:randItem(_channels),score:randInt(60,99),
  evals:randInt(5,40),lastEval:`Mar ${randInt(1,28)}`,trend:randItem(['▲','▼','→']),
  opening:randInt(15,20),resolution:randInt(20,35),comms:randInt(15,25),closing:randInt(9,15)
}));

// ═══ SCORECARD LOGIC ═══
// Each item belongs to a section. Deducting ANY item in a section = lose ALL section pts.
// Fatal sections: deducting any item = immediate FAIL (score = 0)
const allItems={
  // Section 1 — Call Opening & Closure (20 pts)
  'consent':        {pts:20, label:'Customer Consent, Opening Clarity & Closure', section:'opening'},
  // Section 2 — Attitude & Tone (25 pts)
  'listening':      {pts:25, label:'Active Listening',                            section:'tone'},
  'professional':   {pts:25, label:'Professional Language',                       section:'tone'},
  'empathy':        {pts:25, label:'Empathy & Tone Behavior',                     section:'tone'},
  'concentration':  {pts:25, label:'Concentration & Dead Air',                    section:'tone'},
  // Section 3 — Hold Process (15 pts)
  'hold':           {pts:15, label:'Hold Process Followed',                       section:'hold'},
  // Section 4 — Sales Expertise (fatal)
  'alt-suggestion': {pts:0,  label:'Alternative Suggestion',  section:'sales', fatal:true},
  'save-attempt':   {pts:0,  label:'Save Attempt',            section:'sales', fatal:true},
  'relevance':      {pts:0,  label:'Relevance of Response',   section:'sales', fatal:true},
  'value':          {pts:0,  label:'Value Explanation',       section:'sales', fatal:true},
  'followup':       {pts:0,  label:'Follow-up Planning',      section:'sales', fatal:true},
  'policy':         {pts:0,  label:'Policy Knowledge',        section:'sales', fatal:true},
  'push-agreement': {pts:0,  label:'Pushing for Agreement',   section:'sales', fatal:true},
  'warranty':       {pts:0,  label:'Warranty Not Mentioned',  section:'sales', fatal:true},
  // Section 5 — Call Performance (40 pts)
  'alt-number':     {pts:40, label:'Alternative Number',         section:'perf'},
  'return-policy':  {pts:40, label:'Return Policy Explained',    section:'perf'},
  'item-desc':      {pts:40, label:'Item Description Clarity',   section:'perf'},
  'delivery':       {pts:40, label:'Delivery Details Provided',  section:'perf'},
  'address':        {pts:40, label:'Address Obtained',           section:'perf'},
  'bad-attitude':   {pts:40, label:'Bad Attitude',               section:'perf'},
  'correct-action': {pts:40, label:'Correct Action in System',   section:'perf'},
  // Section 6 — Documentation (fatal)
  'notes':          {pts:0,  label:'Notes Quality',       section:'doc', fatal:true},
  'fake-delivery':  {pts:0,  label:'Fake Delivery Time',  section:'doc', fatal:true},
  // Section 7 — Fake Info & Actions (fatal)
  'fake-confirm':   {pts:0,  label:'Fake Confirmation',  section:'fake', fatal:true},
  'fake-price':     {pts:0,  label:'Fake Price',         section:'fake', fatal:true},
  'fake-specs':     {pts:0,  label:'Fake Specs',         section:'fake', fatal:true},
  'fake-product':   {pts:0,  label:'Fake Product',       section:'fake', fatal:true},
  'fake-warranty':  {pts:0,  label:'Fake Warranty',      section:'fake', fatal:true},
  'fake-agreement': {pts:0,  label:'Fake Agreement',     section:'fake', fatal:true},
  'fake-offers':    {pts:0,  label:'Fake Offers',        section:'fake', fatal:true},
  'invalid-lead':   {pts:0,  label:'Invalid Lead',       section:'fake', fatal:true},
};

// Section full points
const sectionMax={opening:20, tone:25, hold:15, perf:40};

// State: key → true if deducted
const deducted={};

function initScorecard(){
  Object.keys(allItems).forEach(key=>{
    const container=document.getElementById('btn-'+key);
    if(!container)return;
    container.innerHTML='';
    const btn=document.createElement('div');
    btn.className='sc-radio-btn';
    btn.textContent='Deduct';
    btn.style.cssText='width:82px;font-size:11px;';
    btn.onclick=()=>{
      deducted[key]=!deducted[key];
      btn.className='sc-radio-btn'+(deducted[key]?' selected-0':'');
      btn.textContent=deducted[key]?'★ Deducted':'Deduct';
      updateScore();
    };
    container.appendChild(btn);
  });
}

function updateScore(){
  // Find which sections have at least one deduction
  const sectionDeducted={opening:false, tone:false, hold:false, perf:false};
  let hasFatal=false;
  const deductedLabels=[];

  Object.keys(allItems).forEach(key=>{
    if(!deducted[key])return;
    const item=allItems[key];
    deductedLabels.push(item.label);
    if(item.fatal){
      hasFatal=true;
    } else {
      sectionDeducted[item.section]=true;
    }
  });

  // Section scores: full pts if not deducted, 0 if any item in section deducted
  const sc={};
  Object.keys(sectionMax).forEach(s=>{ sc[s]=sectionDeducted[s]?0:sectionMax[s]; });

  const total=sc.opening+sc.tone+sc.hold+sc.perf;
  const pct=hasFatal?0:total;
  const anyInteraction=Object.keys(deducted).some(k=>deducted[k]);

  const el=document.getElementById('score-num');
  el.textContent=anyInteraction?(hasFatal?'FAIL':pct+'%'):'—';
  el.style.color=anyInteraction?(hasFatal?'var(--danger)':scoreColor(pct)):'var(--text2)';

  const verdict=document.getElementById('score-verdict');
  if(!anyInteraction){verdict.textContent='Not started';verdict.style.cssText='background:var(--surface);color:var(--text2);';}
  else if(hasFatal){verdict.textContent='❌ Fatal Fail';verdict.style.cssText='background:rgba(247,95,95,.15);color:var(--danger);';}
  else if(pct>=90){verdict.textContent='🏆 Excellent';verdict.style.cssText='background:rgba(61,214,140,.15);color:var(--green);';}
  else if(pct>=80){verdict.textContent='✅ Pass';verdict.style.cssText='background:rgba(79,142,247,.15);color:var(--accent);';}
  else if(pct>=70){verdict.textContent='⚠️ Needs Work';verdict.style.cssText='background:rgba(247,185,79,.15);color:var(--accent3);';}
  else{verdict.textContent='❌ Fail';verdict.style.cssText='background:rgba(247,95,95,.15);color:var(--danger);';}

  document.getElementById('sb-opening').textContent=`${sc.opening}/20`;
  document.getElementById('sb-tone').textContent=`${sc.tone}/25`;
  document.getElementById('sb-hold').textContent=`${sc.hold}/15`;
  document.getElementById('sb-perf').textContent=`${sc.perf}/40`;
  document.getElementById('pb-opening').style.width=`${(sc.opening/20)*100}%`;
  document.getElementById('pb-tone').style.width=`${(sc.tone/25)*100}%`;
  document.getElementById('pb-hold').style.width=`${(sc.hold/15)*100}%`;
  document.getElementById('pb-perf').style.width=`${(sc.perf/40)*100}%`;

  const ff=document.getElementById('fatal-flags');
  if(deductedLabels.length>0){
    ff.innerHTML=deductedLabels.map(l=>`<div style="padding:6px 10px;background:rgba(247,95,95,.08);border:1px solid rgba(247,95,95,.2);border-radius:6px;color:var(--danger);font-size:12px;margin-bottom:4px;">★ ${l}</div>`).join('');
  } else {
    ff.innerHTML='<div style="padding:7px 10px;background:var(--surface2);border-radius:6px;color:var(--green);font-size:12px;">✓ No deductions applied</div>';
  }
}

function submitScorecard(){
  const agent=document.getElementById('sc-agent').value.trim();
  const spec=document.getElementById('sc-specialist').value;
  if(!agent){showToast('⚠️ Please enter the Agent Name','error');return;}
  if(!spec){showToast('⚠️ Please select a QA Specialist','error');return;}
  const agentEmail=document.getElementById('sc-agent-email').value.trim();
  const channel=document.getElementById('sc-channel').value||'—';
  const date=document.getElementById('sc-date').value||new Date().toLocaleDateString();
  const strengths=document.getElementById('sc-strengths').value.trim()||'No specific strengths noted.';
  const improve=document.getElementById('sc-improve').value.trim()||'No specific areas noted.';
  const action=document.getElementById('sc-action').value||'No action needed';
  const sectionDeducted={opening:false,tone:false,hold:false,perf:false};
  let isFatal=false;
  Object.keys(allItems).forEach(key=>{
    if(!deducted[key])return;
    const item=allItems[key];
    if(item.fatal){isFatal=true;}
    else{sectionDeducted[item.section]=true;}
  });
  const sc={opening:sectionDeducted.opening?0:20, tone:sectionDeducted.tone?0:25, hold:sectionDeducted.hold?0:15, perf:sectionDeducted.perf?0:40};
  const total=sc.opening+sc.tone+sc.hold+sc.perf;
  const pct=isFatal?0:total;
  let verdict,badgeClass,badgeText;
  if(pct>=90){verdict='Excellent';badgeClass='ep-badge-pass';badgeText='🏆 Excellent';}
  else if(pct>=80){verdict='Pass';badgeClass='ep-badge-pass';badgeText='✅ Pass';}
  else if(pct>=70){verdict='Needs Improvement';badgeClass='ep-badge-warn';badgeText='⚠️ Needs Improvement';}
  else{verdict='Below Standard';badgeClass='ep-badge-fail';badgeText='❌ Below Standard';}
  document.getElementById('ep-score-num').textContent=pct+'%';
  document.getElementById('ep-badge').className='ep-badge '+badgeClass;
  document.getElementById('ep-badge').textContent=badgeText;
  document.getElementById('ep-agent-name').textContent=agent;
  document.getElementById('ep-channel').textContent=channel;
  document.getElementById('ep-specialist').textContent=spec;
  document.getElementById('ep-date').textContent=date;
  document.getElementById('ep-greeting').textContent=`Dear ${agent},`;
  document.getElementById('ep-intro').textContent=`Your quality evaluation for a ${channel} interaction has been completed by ${spec}. Your overall score is ${pct}% (${total}/100 points) — ${verdict}.`;
  document.getElementById('ep-b-opening').textContent=sc.opening;
  document.getElementById('ep-b-res').textContent=sc.tone;
  document.getElementById('ep-b-comms').textContent=sc.hold;
  document.getElementById('ep-b-closing').textContent=sc.perf;
  document.getElementById('ep-bf-opening').style.width=`${(sc.opening/20)*100}%`;
  document.getElementById('ep-bf-res').style.width=`${(sc.tone/25)*100}%`;
  document.getElementById('ep-bf-comms').style.width=`${(sc.hold/15)*100}%`;
  document.getElementById('ep-bf-closing').style.width=`${(sc.perf/40)*100}%`;
  document.getElementById('ep-strengths-text').textContent=strengths;
  document.getElementById('ep-improve-text').textContent=improve;
  const abox=document.getElementById('ep-action-box');
  const alabel=document.getElementById('ep-action-label');
  const atext=document.getElementById('ep-action-text');
  abox.className='ep-action';
  if(action==='No action needed'){abox.classList.add('ep-action-none');alabel.textContent='✅ No Action Required';atext.textContent='Keep up the great work!';}
  else if(action==='Coaching session required'||action==='Performance improvement plan'){abox.classList.add('ep-action-coach');alabel.textContent='📅 Action: '+action;atext.textContent=`Your QA Specialist will schedule a ${action.toLowerCase()}.`;}
  else{abox.classList.add('ep-action-warn');alabel.textContent='⚠️ Action: '+action;atext.textContent=`This evaluation has triggered a ${action.toLowerCase()}.`;}
  const toEmail=agentEmail||'agent@company.com';
  document.getElementById('ep-to-email').textContent=toEmail;
  document.getElementById('email-modal-hint').innerHTML=agentEmail?`Sending to: <strong>${toEmail}</strong>`:`<span style="color:var(--accent3);">⚠️ No email entered</span>`;
  window._emailData={agent,agentEmail:toEmail,spec,channel,date,pct,total,opening:sc.opening,tone:sc.tone,hold:sc.hold,perf:sc.perf,verdict,strengths,improve,action};
  document.getElementById('email-modal').classList.add('open');

  // ── Save eval to active workspace ──
  const newEval = {
    id:`QA-2026-${String(evals.length+1).padStart(4,'0')}`,
    agent, specialist:spec,
    channel:channel.replace(/[📞💬📧]\s*/,''),
    score:pct,
    status:pct>=80?'Pass':pct>=70?'Pending':'Fail',
    date:new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}),
    workspace: activeWorkspaceId,
    createdAt: new Date().toISOString(),
  };
  // Add to global evals
  evals.unshift(newEval);
  DB.evaluations.unshift(newEval);
  // Auto-save to Firebase
  try{ DataLayer.addEval(newEval); }catch(e){}
  // Add to workspace evals too
  const activeWs = getActiveWs();
  if(activeWs && !activeWs.isGlobal){
    activeWs.evaluations.unshift(newEval);
    activeWs.stats.evals = (activeWs.stats.evals||0) + 1;
    const scores = activeWs.evaluations.map(e=>e.score);
    activeWs.stats.avgScore = parseFloat((scores.reduce((a,b)=>a+b,0)/scores.length).toFixed(1));
    activeWs.stats.passRate = Math.round((activeWs.evaluations.filter(e=>e.status==='Pass').length/activeWs.evaluations.length)*100);
  }
  renderTracker(); renderRecentEvals();
  showToast(`✅ Evaluation saved to ${activeWs.isGlobal?'Global':activeWs.name}! 🔥`);
}

function buildEmailBody(){
  const d=window._emailData;if(!d)return'';
  return`Subject: Your QA Score — ${d.pct}% (${d.verdict})\n\nDear ${d.agent},\n\nYour quality evaluation for a ${d.channel} interaction has been completed by ${d.spec} on ${d.date}.\n\nOVERALL SCORE: ${d.pct}% — ${d.verdict}\n\nSCORE BREAKDOWN:\n• Opening: ${d.opening}/25\n• Resolution: ${d.res}/35\n• Communication: ${d.comms}/25\n• Closing: ${d.closing}/15\n• TOTAL: ${d.total}/100\n\nSTRENGTHS:\n${d.strengths}\n\nAREAS FOR IMPROVEMENT:\n${d.improve}\n\nACTION: ${d.action}\n\nQMS Pro · Quality Assurance Team`;
}
function openGmail(){const d=window._emailData;if(!d)return;window.open(`https://mail.google.com/mail/?view=cm&fs=1&to=${encodeURIComponent(d.agentEmail)}&su=${encodeURIComponent('Your QA Score — '+d.pct+'% ('+d.verdict+')')}&body=${encodeURIComponent(buildEmailBody())}`,'_blank');}
function openOutlook(){const d=window._emailData;if(!d)return;window.open(`https://outlook.office.com/mail/deeplink/compose?to=${encodeURIComponent(d.agentEmail)}&subject=${encodeURIComponent('Your QA Score — '+d.pct+'% ('+d.verdict+')')}&body=${encodeURIComponent(buildEmailBody())}`,'_blank');}
function copyEmailBody(){const text=buildEmailBody();navigator.clipboard.writeText(text).then(()=>showToast('Email copied!')).catch(()=>{const ta=document.createElement('textarea');ta.value=text;document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);showToast('Copied!');});}

function onScWsChange(){
  const wsId = document.getElementById('sc-workspace').value;
  const agSel = document.getElementById('sc-agent-select');
  const specSel = document.getElementById('sc-specialist');

  if(!wsId){
    agSel.innerHTML='<option value="">— Select agent —</option>';
    specSel.innerHTML='<option value="">Select specialist</option>' +
      DB.specialists.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
    return;
  }

  const ws = getWsById(wsId);
  if(!ws) return;
  activeWorkspaceId = wsId;

  // ── Agents: live from DB filtered by workspace ──
  let wsAgents = [];
  if(ws.agents && ws.agents.length > 0){
    wsAgents = DB.agents.filter(a => ws.agents.includes(a.name));
  }
  if(wsAgents.length === 0) wsAgents = DB.agents;

  agSel.innerHTML = '<option value="">— Select agent —</option>' +
    wsAgents.map(a=>`<option value="${a.name}" data-email="${a.email||''}" data-emp="${a.empId||''}">${a.name}${a.empId?' ('+a.empId+')':''}</option>`).join('');

  // ── Specialists: filtered by workspace AND current user role ──
  let wsSpecs = [];
  if(ws.specialists && ws.specialists.length > 0){
    wsSpecs = DB.specialists.filter(s => ws.specialists.includes(s.name));
  }
  if(wsSpecs.length === 0) wsSpecs = DB.specialists; // fallback

  // If logged-in user is a QA Specialist, only show themselves
  if(currentUser && currentUser.role === 'qa-specialist'){
    const selfMatch = wsSpecs.find(s => s.name === currentUser.name);
    if(selfMatch) wsSpecs = [selfMatch];
  }

  const curSpec = specSel ? specSel.value : '';
  specSel.innerHTML = '<option value="">Select specialist</option>' +
    wsSpecs.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');

  // Auto-select: single option, restore previous, or current user
  if(wsSpecs.length === 1){ specSel.value = wsSpecs[0].name; }
  else if(wsSpecs.find(s=>s.name===curSpec)){ specSel.value = curSpec; }
  if(currentUser && currentUser.role==='qa-specialist'){
    const match = wsSpecs.find(s=>s.name===currentUser.name);
    if(match && specSel) specSel.value = match.name;
  }
}

function onScAgentSelect(){
  const sel = document.getElementById('sc-agent-select');
  const opt = sel.options[sel.selectedIndex];
  if(!opt || !opt.value) return;
  const nameEl = document.getElementById('sc-agent');
  const emailEl = document.getElementById('sc-agent-email');
  if(nameEl) nameEl.value = opt.value;
  if(emailEl) emailEl.value = opt.getAttribute('data-email') || '';
}

function initScorecardPage(){
  const wsSel = document.getElementById('sc-workspace');
  if(!wsSel) return;

  // ── Always rebuild workspace list from live DB ──
  const allWs = DB.workspaces.filter(w=>!w.isGlobal && w.status==='active');
  wsSel.innerHTML = '<option value="">— Select workspace —</option>' +
    allWs.map(ws=>
      `<option value="${ws.id}" ${activeWorkspaceId===ws.id&&activeWorkspaceId!=='ws-global'?'selected':''}>${ws.icon} ${ws.name} · ${ws.region}</option>`
    ).join('');

  // ── Pre-populate agents if workspace already selected ──
  if(activeWorkspaceId && activeWorkspaceId !== 'ws-global'){
    wsSel.value = activeWorkspaceId;
    onScWsChange();
  } else {
    // No workspace selected — show all specialists and empty agents
    const specSel = document.getElementById('sc-specialist');
    if(specSel){
      specSel.innerHTML = '<option value="">Select specialist</option>' +
        DB.specialists.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
    }
    const agSel = document.getElementById('sc-agent-select');
    if(agSel) agSel.innerHTML = '<option value="">— Select workspace first —</option>';
  }

  // ── Set today's date ──
  const dateEl = document.getElementById('sc-date');
  if(dateEl && !dateEl.value) dateEl.value = new Date().toISOString().split('T')[0];
}

function resetScorecard(){
  Object.keys(deducted).forEach(k=>delete deducted[k]);
  initScorecard();
  updateScore();
  ['sc-agent','sc-agent-email','sc-intid','sc-strengths','sc-improve'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
  const agSel=document.getElementById('sc-agent-select');
  if(agSel) agSel.value='';
  document.getElementById('email-modal').classList.remove('open');
}

// ═══ TRACKER ═══
let sortKey='id',sortAsc=true,trackerPage=1;
const pageSize=15;

function sortTable(key){
  if(sortKey===key)sortAsc=!sortAsc;else{sortKey=key;sortAsc=true;}
  trackerPage=1;renderTracker();
}

function renderTracker(){
  const ch=document.getElementById('filter-channel').value;
  const st=document.getElementById('filter-status').value;
  const sp=document.getElementById('filter-specialist').value;
  const q=(document.getElementById('tracker-search')||{value:''}).value.toLowerCase();
  // Use workspace-filtered evals
  const wsEvals = typeof getActiveEvals === 'function' ? getActiveEvals() : evals;
  let filtered=wsEvals.filter(e=>
    (!ch||e.channel===ch)&&(!st||e.status===st)&&(!sp||e.specialist===sp)&&
    (!q||e.agent.toLowerCase().includes(q)||e.id.toLowerCase().includes(q))
  );
  filtered.sort((a,b)=>{
    let av=a[sortKey],bv=b[sortKey];
    if(typeof av==='string')av=av.toLowerCase(),bv=bv.toLowerCase();
    return sortAsc?(av>bv?1:av<bv?-1:0):(av<bv?1:av>bv?-1:0);
  });
  const total=filtered.length;
  const pages=Math.ceil(total/pageSize)||1;
  if(trackerPage>pages)trackerPage=1;
  const slice=filtered.slice((trackerPage-1)*pageSize,trackerPage*pageSize);
  const tbody=document.getElementById('tracker-tbody');
  if(!tbody) return;
  tbody.innerHTML=slice.map(e=>`
    <tr>
      <td style="font-family:'DM Mono',monospace;font-size:10px;color:var(--accent);">${e.id}</td>
      <td style="font-weight:500;">${e.agent}</td>
      <td style="color:var(--text2);font-size:11px;">${e.team||getActiveWs().name}</td>
      <td style="font-size:11px;color:var(--text2);">${e.specialist||'—'}</td>
      <td>${chBadge(e.channel?.replace('📞 ','').replace('💬 ','').replace('📧 ','')||'Phone')}</td>
      <td><span class="score-badge ${scoreClass(e.score)}">${e.score}%</span></td>
      <td>${statusBadge(e.status)}</td>
      <td style="font-size:10px;color:var(--text2);font-family:'DM Mono',monospace;white-space:nowrap;">${e.date}</td>
      <td><button class="btn btn-ghost btn-xs" onclick="showToast('Opening ${e.id}...')">View</button></td>
    </tr>`).join('') || '<tr><td colspan="9" style="text-align:center;padding:20px;color:var(--text2);">No evaluations in this workspace yet</td></tr>';
  const countEl=document.getElementById('tracker-count');
  if(countEl) countEl.textContent=`${total} evaluations`;
  const badge=document.getElementById('tracker-badge');
  if(badge) badge.textContent=filtered.filter(e=>e.status==='Pending').length;
  const pag=document.getElementById('tracker-pagination');
  if(pag) pag.innerHTML=`<span>Showing ${total?((trackerPage-1)*pageSize+1):0}–${Math.min(trackerPage*pageSize,total)} of ${total}</span>
    <div style="display:flex;gap:4px;">
      <button class="btn btn-ghost btn-xs" onclick="trackerPage=Math.max(1,trackerPage-1);renderTracker()" ${trackerPage===1?'disabled':''}>←</button>
      <span style="padding:3px 10px;background:var(--surface2);border-radius:4px;font-size:11px;">${trackerPage}/${pages}</span>
      <button class="btn btn-ghost btn-xs" onclick="trackerPage=Math.min(${pages},trackerPage+1);renderTracker()" ${trackerPage>=pages?'disabled':''}>→</button>
    </div>`;
}

// ═══ DATE FILTER STATE ═══
let dashDateFrom = null;
let dashDateTo   = null;

function setDashPreset(preset, btn){
  const now = new Date();
  const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  document.querySelectorAll('.dash-preset').forEach(b=>b.classList.remove('active'));
  if(btn) btn.classList.add('active');

  const fromEl = document.getElementById('dash-date-from');
  const toEl   = document.getElementById('dash-date-to');

  if(preset==='all'){
    dashDateFrom = null; dashDateTo = null;
    if(fromEl) fromEl.value = '';
    if(toEl)   toEl.value   = '';
  } else if(preset==='today'){
    dashDateFrom = today;
    dashDateTo   = today;
    if(fromEl) fromEl.value = fmtDate(today);
    if(toEl)   toEl.value   = fmtDate(today);
  } else if(preset==='7d'){
    dashDateFrom = new Date(today); dashDateFrom.setDate(today.getDate()-6);
    dashDateTo   = today;
    if(fromEl) fromEl.value = fmtDate(dashDateFrom);
    if(toEl)   toEl.value   = fmtDate(today);
  } else if(preset==='30d'){
    dashDateFrom = new Date(today); dashDateFrom.setDate(today.getDate()-29);
    dashDateTo   = today;
    if(fromEl) fromEl.value = fmtDate(dashDateFrom);
    if(toEl)   toEl.value   = fmtDate(today);
  } else if(preset==='month'){
    dashDateFrom = new Date(today.getFullYear(), today.getMonth(), 1);
    dashDateTo   = today;
    if(fromEl) fromEl.value = fmtDate(dashDateFrom);
    if(toEl)   toEl.value   = fmtDate(today);
  }
  updateDashLabel();
  renderDashboard();
}

function applyDashDateFilter(){
  const fromEl = document.getElementById('dash-date-from');
  const toEl   = document.getElementById('dash-date-to');
  dashDateFrom = fromEl && fromEl.value ? new Date(fromEl.value) : null;
  dashDateTo   = toEl   && toEl.value   ? new Date(toEl.value)   : null;
  // Clear preset active
  document.querySelectorAll('.dash-preset').forEach(b=>b.classList.remove('active'));
  updateDashLabel();
  renderDashboard();
}

function updateDashLabel(){
  const el = document.getElementById('dash-date-label');
  if(!el) return;
  if(!dashDateFrom && !dashDateTo){ el.textContent = 'All time'; return; }
  const f = dashDateFrom ? dashDateFrom.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}) : '...';
  const t = dashDateTo   ? dashDateTo.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})   : '...';
  el.textContent = f + ' → ' + t;
}

function fmtDate(d){
  return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');
}

function filterEvalsByDate(evalsArr){
  if(!dashDateFrom && !dashDateTo) return evalsArr;
  return evalsArr.filter(function(e){
    if(!e.createdAt && !e.date) return true;
    const d = e.createdAt ? new Date(e.createdAt) : new Date(e.date);
    if(isNaN(d)) return true;
    const day = new Date(d.getFullYear(), d.getMonth(), d.getDate());
    if(dashDateFrom && day < dashDateFrom) return false;
    if(dashDateTo   && day > dashDateTo)   return false;
    return true;
  });
}

// Filter agents by their evals date range
function filterAgentsByDate(agentsArr, evalsArr){
  if(!dashDateFrom && !dashDateTo) return agentsArr;
  const filtered = filterEvalsByDate(evalsArr);
  const agentNames = new Set(filtered.map(e=>e.agent));
  return agentsArr.map(function(a){
    // Recalculate stats from filtered evals only
    const agEvals = filtered.filter(e=>e.agent===a.name);
    if(!agEvals.length){
      // Agent has no evals in range — show with zeroed stats
      return Object.assign({}, a, {
        audits: 0, score: 0, salesMistakes: 0, failedAudits: 0,
        salesScore: '0%', _filtered: true
      });
    }
    const avgSc = Math.round(agEvals.reduce((s,e)=>s+e.score,0)/agEvals.length);
    const failed = agEvals.filter(e=>e.status==='Fail').length;
    return Object.assign({}, a, {
      audits: agEvals.length,
      score: avgSc,
      failedAudits: failed,
      _filtered: true
    });
  });
}

// ═══ DYNAMIC DASHBOARD ═══
function renderDashboard(){
  const el = document.getElementById('dashboard-content');
  if(!el) return;

  try {
  const ws       = getActiveWs();
  const rawEvals = getActiveEvals();
  const allEvals = filterEvalsByDate(rawEvals);
  const allAgents= getActiveAgents();
  const wsLabel  = ws.isGlobal ? 'All Workspaces' : ws.icon+' '+ws.name;
  const target   = (ws.config&&ws.config.evalTarget)||DB.config.monthlyTarget||1350;
  const passT    = (ws.config&&ws.config.passThreshold)||DB.config.passThreshold||80;

  // ── Core KPIs from filtered evals ──
  const totalEvals  = allEvals.length;
  const failedEvals = allEvals.filter(function(e){return e.status==='Fail';}).length;
  const passedEvals = allEvals.filter(function(e){return e.status==='Pass';}).length;
  const avgScore    = totalEvals ? Math.round(allEvals.reduce(function(s,e){return s+e.score;},0)/totalEvals*10)/10 : 0;
  const failRate    = totalEvals ? Math.round(failedEvals/totalEvals*100) : 0;
  const targetPct   = Math.min(Math.round(totalEvals/target*100),100);
  const remaining   = Math.max(0, target-totalEvals);
  const scoreColor  = avgScore>=90?'var(--green)':avgScore>=80?'var(--accent)':avgScore>=70?'var(--accent3)':'var(--danger)';
  const totalAudits = totalEvals;
  const totalFailedAu = failedEvals;

  // ── Category scores from eval fields ──
  var softArr=[], salesArr=[], docArr=[], perfArr=[], salesMisTotal=0;
  allEvals.forEach(function(e){
    if(e.softScore>0)  softArr.push(e.softScore);
    if(e.salesScore>0) salesArr.push(e.salesScore);
    if(e.docScore>0)   docArr.push(e.docScore);
    if(e.perfScore>0)  perfArr.push(e.perfScore);
    salesMisTotal += (e.salesMistakes||0);
  });
  const avgSoft  = softArr.length  ? Math.round(softArr.reduce(function(a,b){return a+b;},0)/softArr.length)   : 0;
  const avgSales = salesArr.length ? Math.round(salesArr.reduce(function(a,b){return a+b;},0)/salesArr.length) : 0;
  const avgDoc   = docArr.length   ? Math.round(docArr.reduce(function(a,b){return a+b;},0)/docArr.length)     : 0;
  const avgPerf  = perfArr.length  ? Math.round(perfArr.reduce(function(a,b){return a+b;},0)/perfArr.length)   : 0;
  const totalSalesMis = salesMisTotal;

  // ── Per-agent map from filtered evals ──
  var agentEvalsMap = {};
  allEvals.forEach(function(e){
    if(!e.agent) return;
    if(!agentEvalsMap[e.agent]) agentEvalsMap[e.agent]={evals:[],failed:0,passed:0};
    agentEvalsMap[e.agent].evals.push(e);
    if(e.status==='Fail') agentEvalsMap[e.agent].failed++;
    if(e.status==='Pass') agentEvalsMap[e.agent].passed++;
  });
  const activeAgents  = Object.keys(agentEvalsMap).length;
  const totalAgents   = allAgents.length;
  const failingAgents = Object.keys(agentEvalsMap).filter(function(name){
    var ag=agentEvalsMap[name];
    var sc=ag.evals.length?Math.round(ag.evals.reduce(function(s,e){return s+e.score;},0)/ag.evals.length):0;
    return sc>0&&sc<passT;
  }).length;

  // ── Error breakdown ──
  const errSales = totalEvals ? Math.round(totalEvals*(100-Math.max(avgSales,0))/100) : 0;
  const errSoft  = totalEvals ? Math.round(totalEvals*(100-Math.max(avgSoft,0))/100)  : 0;
  const errDoc   = totalEvals ? Math.round(totalEvals*(100-Math.max(avgDoc,0))/100)   : 0;
  const errPerf  = totalEvals ? Math.round(totalEvals*(100-Math.max(avgPerf,0))/100)  : 0;
  const errTotal = Math.max(errSales+errSoft+errDoc+errPerf, 1);

  // ── Team breakdown from filtered evals ──
  var teamMap = {};
  allEvals.forEach(function(e){
    var agent = allAgents.find(function(a){return a.name===e.agent;});
    var team  = (agent&&agent.team)||'KSA Team';
    if(!teamMap[team]) teamMap[team]={scores:[],count:0,salesMis:0};
    teamMap[team].scores.push(e.score);
    teamMap[team].count++;
    teamMap[team].salesMis += (e.salesMistakes||0);
  });
  var teamRows='';
  Object.keys(teamMap).slice(0,5).forEach(function(team){
    var td=teamMap[team];
    var sc=td.scores.length?Math.round(td.scores.reduce(function(a,b){return a+b;},0)/td.scores.length):0;
    teamRows+='<tr>'
      +'<td>'+team+'</td>'
      +'<td><span class="score-badge '+scoreClass(sc)+'">'+sc+'%</span></td>'
      +'<td style="font-family:\'DM Mono\',monospace;font-size:11px;">'+td.salesMis+'</td>'
      +'<td style="font-family:\'DM Mono\',monospace;font-size:11px;">'+td.count+'</td>'
      +'<td><span class="score-badge '+scoreClass(sc)+'">'+sc+'%</span></td>'
      +'</tr>';
  });
  if(!teamRows) teamRows='<tr><td colspan="5" style="text-align:center;color:var(--text2);">No evaluations in selected period</td></tr>';

  // ── Recent 5 evals ──
  var recentRows='';
  allEvals.slice(0,5).forEach(function(e){
    recentRows+='<tr>'
      +'<td style="font-family:\'DM Mono\',monospace;font-size:10px;color:var(--accent);">'+e.id+'</td>'
      +'<td style="font-weight:500;font-size:12px;">'+e.agent+'</td>'
      +'<td style="font-size:11px;color:var(--text2);">'+(e.specialist||'—')+'</td>'
      +'<td>'+chBadge((e.channel||'Phone').replace(/[📞💬📧]\s*/,''))+'</td>'
      +'<td><span class="score-badge '+scoreClass(e.score)+'">'+e.score+'%</span></td>'
      +'<td>'+statusBadge(e.status)+'</td>'
      +'<td style="font-size:10px;color:var(--text2);">'+e.date+'</td>'
      +'</tr>';
  });
  if(!recentRows) recentRows='<tr><td colspan="7" style="text-align:center;padding:20px;color:var(--text2);">No evaluations in selected period</td></tr>';

  // ── Top agents from eval map ──
  const topColors=['var(--green)','var(--accent)','var(--accent2)','var(--accent3)','var(--purple)'];
  var topAgentsHTML='';
  var agentScoreList=Object.keys(agentEvalsMap).map(function(name){
    var ag=agentEvalsMap[name];
    return {name:name,score:Math.round(ag.evals.reduce(function(s,e){return s+e.score;},0)/ag.evals.length),audits:ag.evals.length};
  }).sort(function(a,b){return b.score-a.score;}).slice(0,5);
  agentScoreList.forEach(function(a,i){
    var c=topColors[i%topColors.length];
    var initials=a.name.split(' ').map(function(w){return w[0]||'';}).join('').slice(0,2).toUpperCase();
    topAgentsHTML+='<div style="display:flex;align-items:center;gap:10px;">'
      +'<div style="width:30px;height:30px;background:'+c+';border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;flex-shrink:0;">'+initials+'</div>'
      +'<div style="flex:1;"><div style="font-size:12px;margin-bottom:2px;">'+a.name+'</div>'
      +'<div style="font-size:10px;color:var(--text2);margin-bottom:3px;">'+a.audits+' evals</div>'
      +'<div class="prog-bar"><div class="prog-fill" style="width:'+a.score+'%;background:'+c+';"></div></div></div>'
      +'<div style="font-family:\'DM Mono\',monospace;font-size:12px;color:'+c+';">'+a.score+'%</div></div>';
  });
  if(!topAgentsHTML) topAgentsHTML='<div style="color:var(--text2);font-size:12px;text-align:center;padding:20px;">No evaluations in selected period</div>';

  // ── Workspace bars from filtered evals ──
  var wsBarList=ws.isGlobal?DB.workspaces.filter(function(w){return !w.isGlobal;}).slice(0,6):[ws];
  var wsBarsHTML='';
  wsBarList.forEach(function(w){
    var wEvals=filterEvalsByDate(w.evaluations||[]);
    var wScore=wEvals.length?Math.round(wEvals.reduce(function(s,e){return s+e.score;},0)/wEvals.length):0;
    wsBarsHTML+='<div>'
      +'<div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;">'
      +'<span>'+w.icon+' '+w.name+'</span>'
      +'<span style="font-family:\'DM Mono\',monospace;color:'+w.color+';">'+wScore+'%'
      +'<span style="font-size:10px;color:var(--text2);"> ('+wEvals.length+' evals)</span></span></div>'
      +'<div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+wScore+'%;background:'+w.color+';"></div></div>'
      +'</div>';
  });

  // ── Error rows ──
  var errItems=[
    ['💰 Sales Expertise',errSales,'var(--danger)'],
    ['🧠 Soft Skills',errSoft,'var(--accent3)'],
    ['📋 Documentation',errDoc,'var(--accent)'],
    ['📞 Call Performance',errPerf,'var(--green)'],
  ];
  var errRowsHTML='';
  errItems.forEach(function(item){
    var pct=Math.round(item[1]/errTotal*100);
    errRowsHTML+='<div>'
      +'<div style="display:flex;justify-content:space-between;margin-bottom:4px;font-size:12px;">'
      +'<span>'+item[0]+'</span>'
      +'<span style="font-family:\'DM Mono\',monospace;">'
      +'<span style="color:'+item[2]+';">'+item[1]+'</span> '
      +'<span style="color:var(--accent3);">'+pct+'%</span></span></div>'
      +'<div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+pct+'%;background:'+item[2]+';"></div></div>'
      +'</div>';
  });

  // ── Update sidebar KPIs ──
  var sbSpec=document.getElementById('sb-spec-count');
  var sbAgent=document.getElementById('sb-agent-count');
  if(sbSpec)  sbSpec.textContent=ws.isGlobal?DB.specialists.length:((ws.specialists&&ws.specialists.length)||DB.specialists.length);
  if(sbAgent) sbAgent.textContent=activeAgents||totalAgents;

  // ── RENDER ──
  el.innerHTML=''

  // Row 1: 4 KPI cards
  +'<div class="kpi-grid">'
  +'<div class="kpi-card blue"><span class="kpi-icon">⭐</span><div class="kpi-label">Avg Quality Score</div>'
  +'<div class="kpi-value" style="color:'+scoreColor+'">'+avgScore+'<span style="font-size:14px">%</span></div>'
  +'<div class="kpi-sub">'+wsLabel+'</div>'
  +'<div class="kpi-delta '+(avgScore>=passT?'delta-up':'delta-down')+'">'+(avgScore>=passT?'▲ Above ':'▼ Below ')+passT+'% target</div></div>'

  +'<div class="kpi-card green"><span class="kpi-icon">🎧</span><div class="kpi-label">Total Audits</div>'
  +'<div class="kpi-value">'+totalAudits.toLocaleString()+'</div>'
  +'<div class="kpi-sub">Target: '+target.toLocaleString()+' · '+targetPct+'% achieved</div>'
  +'<div class="kpi-delta '+(remaining>0?'delta-down':'delta-up')+'">'+(remaining>0?'▼ '+remaining.toLocaleString()+' remaining':'▲ Target reached!')+'</div></div>'

  +'<div class="kpi-card orange"><span class="kpi-icon">❌</span><div class="kpi-label">Failed Audits</div>'
  +'<div class="kpi-value">'+totalFailedAu.toLocaleString()+'</div>'
  +'<div class="kpi-sub">Out of '+totalAudits.toLocaleString()+' total</div>'
  +'<div class="kpi-delta delta-down">▼ '+failRate+'% fail rate</div></div>'

  +'<div class="kpi-card teal"><span class="kpi-icon">⚠️</span><div class="kpi-label">Sales Mistakes</div>'
  +'<div class="kpi-value">'+totalSalesMis.toLocaleString()+'</div>'
  +'<div class="kpi-sub">'+activeAgents+' agents with evals</div>'
  +'<div class="kpi-delta delta-down">▼ '+failingAgents+' agents below target</div></div>'
  +'</div>'

  // Row 2: Category KPIs
  +'<div class="grid-3" style="margin-bottom:18px;">'
  +'<div class="kpi-card" style="border-top:2px solid var(--accent2);">'
  +'<span class="kpi-icon">🧠</span><div class="kpi-label">Soft Skills Score</div>'
  +'<div class="kpi-value" style="color:var(--accent2);">'+avgSoft+'<span style="font-size:14px">%</span></div>'
  +'<div class="kpi-sub">Active Listening · Tone · Empathy</div>'
  +'<div class="kpi-delta '+(avgSoft>=passT?'delta-up':'delta-down')+'">'+(avgSoft>=passT?'▲ Above target':'▼ Below target')+'</div></div>'

  +'<div class="kpi-card" style="border-top:2px solid var(--orange);">'
  +'<span class="kpi-icon">💰</span><div class="kpi-label">Sales Expertise Score</div>'
  +'<div class="kpi-value" style="color:var(--orange);">'+avgSales+'<span style="font-size:14px">%</span></div>'
  +'<div class="kpi-sub">'+totalSalesMis+' sales mistakes</div>'
  +'<div class="kpi-delta '+(avgSales>=passT?'delta-up':'delta-down')+'">'+(avgSales>=passT?'▲ Above target':'▼ Below target')+'</div></div>'

  +'<div class="kpi-card" style="border-top:2px solid var(--green);">'
  +'<span class="kpi-icon">📞</span><div class="kpi-label">Call Performance</div>'
  +'<div class="kpi-value" style="color:'+(avgPerf>=90?'var(--green)':avgPerf>=80?'var(--accent)':'var(--accent3)')+'">'+avgPerf+'<span style="font-size:14px">%</span></div>'
  +'<div class="kpi-sub">'+errPerf+' below-target evals</div>'
  +'<div class="kpi-delta '+(avgPerf>=passT?'delta-up':'delta-down')+'">'+(avgPerf>=passT?'▲ Above target':'▼ Below target')+'</div></div>'
  +'</div>'

  // Charts row
  +'<div class="grid-2">'
  +'<div class="card"><div class="card-header"><div class="card-header-left">'
  +'<div class="card-title">Score by Category</div>'
  +'<div class="card-subtitle">'+wsLabel+' · '+totalEvals+' evals</div></div></div>'
  +'<div style="display:flex;flex-direction:column;gap:13px;">'
  +'<div><div style="display:flex;justify-content:space-between;margin-bottom:5px;font-size:13px;"><span>🧠 Soft Skills</span><span style="font-family:\'DM Mono\',monospace;font-size:12px;color:var(--accent2);">'+avgSoft+'%</span></div><div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+avgSoft+'%;background:linear-gradient(90deg,var(--accent2),var(--green));"></div></div></div>'
  +'<div><div style="display:flex;justify-content:space-between;margin-bottom:5px;font-size:13px;"><span>💰 Sales Expertise</span><span style="font-family:\'DM Mono\',monospace;font-size:12px;color:var(--orange);">'+avgSales+'%</span></div><div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+avgSales+'%;background:linear-gradient(90deg,var(--orange),var(--danger));"></div></div></div>'
  +'<div><div style="display:flex;justify-content:space-between;margin-bottom:5px;font-size:13px;"><span>📋 Documentation</span><span style="font-family:\'DM Mono\',monospace;font-size:12px;color:var(--accent);">'+avgDoc+'%</span></div><div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+avgDoc+'%;background:linear-gradient(90deg,var(--accent),var(--purple));"></div></div></div>'
  +'<div><div style="display:flex;justify-content:space-between;margin-bottom:5px;font-size:13px;"><span>📞 Call Performance</span><span style="font-family:\'DM Mono\',monospace;font-size:12px;color:var(--green);">'+avgPerf+'%</span></div><div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:'+avgPerf+'%;background:linear-gradient(90deg,var(--green),var(--accent2));"></div></div></div>'
  +'</div>'
  +'<div class="mini-stats-row">'
  +'<div class="mini-stat"><div class="mini-stat-val" style="color:var(--danger);">'+totalFailedAu+'</div><div class="mini-stat-lbl">Failed</div></div>'
  +'<div class="mini-stat"><div class="mini-stat-val" style="color:var(--orange);">'+totalSalesMis+'</div><div class="mini-stat-lbl">Sales Mistakes</div></div>'
  +'<div class="mini-stat"><div class="mini-stat-val" style="color:var(--accent);">'+target.toLocaleString()+'</div><div class="mini-stat-lbl">Target</div></div>'
  +'</div></div>'

  +'<div class="card"><div class="card-header"><div class="card-header-left">'
  +'<div class="card-title">Error Contribution</div>'
  +'<div class="card-subtitle">'+errTotal.toLocaleString()+' total · '+wsLabel+'</div></div></div>'
  +'<div style="display:flex;flex-direction:column;gap:10px;">'+errRowsHTML+'</div>'
  +'<div style="margin-top:14px;"><div style="font-size:13px;font-weight:600;margin-bottom:10px;">Score by Team</div>'
  +'<div class="table-wrap"><table><thead><tr><th>Team</th><th>Score</th><th>Sales Mistakes</th><th>Audits</th><th>Sales Score</th></tr></thead>'
  +'<tbody>'+teamRows+'</tbody></table></div></div></div>'
  +'</div>'

  // Recent Evals
  +'<div class="card"><div class="card-header"><div class="card-header-left">'
  +'<div class="card-title">Recent Evaluations</div>'
  +'<div class="card-subtitle">'+wsLabel+' · '+totalEvals+' total</div></div>'
  +'<div class="card-actions"><button class="btn btn-ghost btn-sm" onclick="showPage(\'tracker\',null)">View All →</button></div></div>'
  +'<div class="table-wrap"><table><thead><tr><th>Eval ID</th><th>Agent</th><th>Specialist</th><th>Channel</th><th>Score</th><th>Status</th><th>Date</th></tr></thead>'
  +'<tbody id="recent-evals-tbody">'+recentRows+'</tbody></table></div></div>'

  // Top Agents + Workspace Performance
  +'<div class="grid-2">'
  +'<div class="card"><div class="card-header"><div class="card-title">🏆 Top Agents</div>'
  +'<span class="tag tag-green">'+activeAgents+' active</span></div>'
  +'<div style="display:flex;flex-direction:column;gap:10px;">'+topAgentsHTML+'</div></div>'
  +'<div class="card"><div class="card-header"><div class="card-title">📊 Workspace Performance</div></div>'
  +'<div style="display:flex;flex-direction:column;gap:12px;">'+wsBarsHTML+'</div></div>'
  +'</div>';

  setTimeout(drawTrendChart,50);

  } catch(err) {
    el.innerHTML = '<div style="padding:40px;text-align:center;color:var(--danger);">Dashboard error: '+err.message+'<br><small style="color:var(--text2);">Check console for details</small></div>';
    console.error('renderDashboard error:', err);
  }
}

// ═══ RECENT EVALS ═══
function renderRecentEvals(){
  const tbody = document.getElementById('recent-evals-tbody');
  if(!tbody) return;
  const wsEvals = typeof getActiveEvals === 'function' ? getActiveEvals() : evals;
  tbody.innerHTML = wsEvals.slice(0,10).map(e=>`
    <tr>
      <td style="font-family:'DM Mono',monospace;font-size:10px;color:var(--accent);">${e.id}</td>
      <td style="font-weight:500;">${e.agent}</td>
      <td style="font-size:11px;color:var(--text2);">${e.specialist||'—'}</td>
      <td>${chBadge(e.channel?.replace(/[📞💬📧]\s*/,'')||'Phone')}</td>
      <td><span class="score-badge ${scoreClass(e.score)}">${e.score}%</span></td>
      <td>${statusBadge(e.status)}</td>
      <td style="font-size:10px;color:var(--text2);">${e.date}</td>
    </tr>`).join('') || '<tr><td colspan="7" style="text-align:center;padding:20px;color:var(--text2);">No evaluations yet</td></tr>';
}

// ═══ AGENTS ═══
function renderAgents(){
  const q    = (document.getElementById('agent-search')||{value:''}).value.toLowerCase();
  const stF  = (document.getElementById('agent-filter-status')||{value:''}).value;
  const fromEl = document.getElementById('agent-date-from');
  const toEl   = document.getElementById('agent-date-to');

  // Apply date filter to evals, then recalculate agent stats
  let wsAgents = typeof getActiveAgents === 'function' ? getActiveAgents() : DB.agents;
  const wsEvals = typeof getActiveEvals === 'function' ? getActiveEvals() : DB.evaluations;

  // If date range set on agents page, recalc from evals
  const agFrom = fromEl && fromEl.value ? new Date(fromEl.value) : null;
  const agTo   = toEl   && toEl.value   ? new Date(toEl.value)   : null;
  if(agFrom || agTo){
    const filteredEvals = wsEvals.filter(function(e){
      if(!e.createdAt && !e.date) return true;
      const d = e.createdAt ? new Date(e.createdAt) : new Date(e.date);
      if(isNaN(d)) return true;
      const day = new Date(d.getFullYear(), d.getMonth(), d.getDate());
      if(agFrom && day < agFrom) return false;
      if(agTo   && day > agTo)   return false;
      return true;
    });
    wsAgents = wsAgents.map(function(a){
      const agEvals = filteredEvals.filter(e=>e.agent===a.name);
      if(!agEvals.length) return Object.assign({},a,{audits:0,score:0,salesMistakes:0,failedAudits:0,salesScore:'0%'});
      const avgSc  = Math.round(agEvals.reduce((s,e)=>s+e.score,0)/agEvals.length);
      const failed = agEvals.filter(e=>e.status==='Fail').length;
      return Object.assign({},a,{audits:agEvals.length, score:avgSc, failedAudits:failed});
    });
  }

  const filtered = wsAgents.filter(function(a){
    const status = (a.score??0)>=80?'Pass':(a.score??0)>=70?'Pending':'Fail';
    return(!q||a.name.toLowerCase().includes(q)||(a.empId||'').includes(q))&&(!stF||status===stF);
  });
  const tbody=document.getElementById('agents-tbody');
  if(!tbody) return;
  const colors=['var(--accent)','var(--accent2)','var(--green)','var(--orange)','var(--purple)','var(--accent3)'];
  tbody.innerHTML=filtered.map((a,i)=>{
    const score=a.score??0;
    const status=score>=80?'Pass':score>=70?'Pending':'Fail';
    const col=colors[i%colors.length];
    const salesNeg=parseFloat(a.salesScore??'0')<0;
    return`<tr>
      <td><div style="display:flex;align-items:center;gap:8px;">
        <div style="width:28px;height:28px;background:linear-gradient(135deg,${col},var(--bg));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;flex-shrink:0;">${a.name.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase()}</div>
        <div><div style="font-weight:500;font-size:12px;">${a.name}</div><div style="font-size:10px;color:var(--text2);font-family:'DM Mono',monospace;">${a.empId||''}</div></div>
      </div></td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;">${a.targetCalls??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;">${a.audits??'—'}</td>
      <td><span class="score-badge ${scoreClass(score)}">${score}</span></td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;color:${salesNeg?'var(--danger)':'var(--text)'};">${a.salesScore??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;color:${(a.salesMistakes??0)>5?'var(--danger)':(a.salesMistakes??0)>2?'var(--orange)':'var(--green)'};">${a.salesMistakes??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;color:${(a.failedAudits??0)>5?'var(--danger)':(a.failedAudits??0)>2?'var(--orange)':'var(--green)'};">${a.failedAudits??'—'}</td>
      <td>${statusBadge(status)}</td>
      <td><button class="btn btn-ghost btn-xs" onclick="openAgentModalById('${a.id}')">Details</button></td>
    </tr>`;
  }).join('');
}

function openAgentModalById(id){
  const a=DB.agents.find(x=>x.id===id);
  if(!a) return;
  const score=a.score??0;
  const status=score>=80?'Pass':score>=70?'Needs Coaching':'Failing';
  document.getElementById('am-avatar').style.background=`linear-gradient(135deg,var(--accent),var(--bg))`;
  document.getElementById('am-avatar').textContent=a.name.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase();
  document.getElementById('am-name').textContent=a.name;
  document.getElementById('am-meta').textContent=`Emp: ${a.empId||'—'} · KSA Team · ${a.audits??0} audits`;
  document.getElementById('am-score').textContent=score;
  document.getElementById('am-score').style.color=scoreColor(score);
  document.getElementById('am-evals').textContent=a.audits??'—';
  const trend=(a.score??0)>=80?'▲':'▼';
  document.getElementById('am-trend-v').textContent=trend==='▲'?'Passing':'Needs Work';
  document.getElementById('am-trend-v').style.color=trend==='▲'?'var(--green)':'var(--danger)';
  document.getElementById('am-last').textContent='Mar 2026';
  document.getElementById('am-score-badge').innerHTML=statusBadge(status);
  const bd=document.getElementById('am-breakdown');
  const salesScore=parseFloat(a.salesScore??'0')||0;
  bd.innerHTML=`
    <div><div style="display:flex;justify-content:space-between;font-size:12px;margin-bottom:4px;"><span>⭐ Overall Score</span><span style="font-family:'DM Mono',monospace;color:var(--accent);">${score}/100</span></div>
    <div class="prog-bar"><div class="prog-fill" style="width:${Math.max(0,score)}%;background:var(--accent);"></div></div></div>
    <div><div style="display:flex;justify-content:space-between;font-size:12px;margin-bottom:4px;"><span>💰 Sales Expertise</span><span style="font-family:'DM Mono',monospace;color:${salesScore<0?'var(--danger)':'var(--accent2)'};">${a.salesScore??'—'}</span></div>
    <div class="prog-bar"><div class="prog-fill" style="width:${Math.max(0,salesScore)}%;background:var(--accent2);"></div></div></div>
    <div style="display:flex;gap:16px;margin-top:8px;">
      <div class="mini-stat"><div class="mini-stat-val" style="color:var(--orange);">${a.salesMistakes??0}</div><div class="mini-stat-lbl">Sales Mistakes</div></div>
      <div class="mini-stat"><div class="mini-stat-val" style="color:var(--danger);">${a.failedAudits??0}</div><div class="mini-stat-lbl">Failed Audits</div></div>
      <div class="mini-stat"><div class="mini-stat-val" style="color:var(--accent);">${a.targetCalls??0}</div><div class="mini-stat-lbl">Target Calls</div></div>
    </div>`;
  document.getElementById('agent-modal').classList.add('open');
}

// ═══ AGENT DRILL-DOWN ═══
function openAgentModal(i){
  const a=agentData[i];
  const status=a.score>=80?'Pass':a.score>=70?'Needs Coaching':'Failing';
  const colors=['var(--accent)','var(--accent2)','var(--green)','var(--orange)','var(--purple)','var(--accent3)'];
  const col=colors[i%colors.length];
  document.getElementById('am-avatar').style.background=`linear-gradient(135deg,${col},var(--bg))`;
  document.getElementById('am-avatar').textContent=a.name.split(' ').map(w=>w[0]).join('').slice(0,2);
  document.getElementById('am-name').textContent=a.name;
  document.getElementById('am-meta').textContent=`${a.team} · ${a.channel} · ${a.evals} evaluations`;
  document.getElementById('am-score').textContent=a.score+'%';
  document.getElementById('am-score').style.color=scoreColor(a.score);
  document.getElementById('am-evals').textContent=a.evals;
  document.getElementById('am-trend-v').textContent=a.trend==='▲'?'Improving':a.trend==='▼'?'Declining':'Stable';
  document.getElementById('am-trend-v').style.color=a.trend==='▲'?'var(--green)':a.trend==='▼'?'var(--danger)':'var(--text2)';
  document.getElementById('am-last').textContent=a.lastEval;
  document.getElementById('am-score-badge').innerHTML=statusBadge(status);
  const bd=document.getElementById('am-breakdown');
  const cats=[['Opening & Greeting',a.opening,25,'var(--accent)'],['Issue Resolution',a.resolution,35,'var(--accent2)'],['Communication',a.comms,25,'var(--accent3)'],['Closing & Compliance',a.closing,15,'var(--green)']];
  bd.innerHTML=cats.map(([name,val,max,color])=>`
    <div><div style="display:flex;justify-content:space-between;font-size:12px;margin-bottom:4px;"><span>${name}</span><span style="font-family:'DM Mono',monospace;color:${color};">${val}/${max}</span></div>
    <div class="prog-bar"><div class="prog-fill" style="width:${(val/max)*100}%;background:${color};"></div></div></div>`).join('');
  document.getElementById('agent-modal').classList.add('open');
}
function closeAgentModal(){document.getElementById('agent-modal').classList.remove('open');}

// ═══ SPECIALISTS ═══
function renderSpecialists(){
  const specData=[
    {name:'Sara Ahmed',initials:'SA',evals:284,avgScore:97,accuracy:98,variance:2.1,color:'var(--green)'},
    {name:'Mohamed Karim',initials:'MK',evals:271,avgScore:94,accuracy:95,variance:3.4,color:'var(--accent)'},
    {name:'Lina Rashid',initials:'LR',evals:263,avgScore:91,accuracy:92,variance:6.2,color:'var(--accent2)'},
    {name:'Omar Tarek',initials:'OT',evals:258,avgScore:89,accuracy:90,variance:9.8,color:'var(--accent3)'},
    {name:'Rania Fouad',initials:'RF',evals:247,avgScore:87,accuracy:88,variance:4.5,color:'var(--purple)'},
    {name:'Khaled Mostafa',initials:'KM',evals:239,avgScore:85,accuracy:86,variance:7.1,color:'var(--orange)'},
  ];
  const grid=document.getElementById('specialists-grid');
  grid.innerHTML=specData.map(s=>`
    <div style="background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius);padding:18px;">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;">
        <div style="width:42px;height:42px;background:linear-gradient(135deg,${s.color},var(--bg));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;color:#fff;flex-shrink:0;">${s.initials}</div>
        <div style="flex:1;"><div style="font-size:14px;font-weight:600;">${s.name}</div><div style="font-size:10px;color:var(--text2);margin-top:1px;">${s.evals} evals this month</div></div>
        <div style="font-family:'Syne',sans-serif;font-size:22px;font-weight:800;color:${s.color};">${s.avgScore}%</div>
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;font-size:11px;">
        <div><div style="color:var(--text2);margin-bottom:4px;">Accuracy</div><div class="prog-bar"><div class="prog-fill" style="width:${s.accuracy}%;background:${s.color};"></div></div><div style="color:${s.color};font-family:'DM Mono',monospace;margin-top:3px;">${s.accuracy}%</div></div>
        <div><div style="color:var(--text2);margin-bottom:4px;">Variance</div><div class="prog-bar"><div class="prog-fill" style="width:${Math.min(s.variance*5,100)}%;background:${s.variance>5?'var(--danger)':'var(--green)'};"></div></div><div style="color:${s.variance>5?'var(--danger)':'var(--green)'};font-family:'DM Mono',monospace;margin-top:3px;">±${s.variance}%</div></div>
      </div>
    </div>`).join('');
}

// ═══ CHARTS ═══
function drawTrendChart(){
  const canvas=document.getElementById('trend-chart');
  if(!canvas)return;
  const ctx=canvas.getContext('2d');
  const w=canvas.offsetWidth,h=120;
  canvas.width=w*2;canvas.height=h*2;canvas.style.width=w+'px';canvas.style.height=h+'px';
  ctx.scale(2,2);
  const data=[79,81,80,82,83,82,85,84];
  const target=80;
  const minV=72,maxV=90,range=maxV-minV;
  const pad={t:16,r:10,b:20,l:28};
  const cw=w-pad.l-pad.r,ch=h-pad.t-pad.b;
  ctx.clearRect(0,0,w,h);

  // Grid lines
  [75,80,85,90].forEach(v=>{
    const y=pad.t+ch-(v-minV)/range*ch;
    ctx.beginPath();ctx.strokeStyle='rgba(30,45,66,0.8)';ctx.lineWidth=1;
    ctx.moveTo(pad.l,y);ctx.lineTo(pad.l+cw,y);ctx.stroke();
    ctx.fillStyle='rgba(107,122,154,0.6)';ctx.font='9px DM Mono';ctx.textAlign='right';
    ctx.fillText(v+'%',pad.l-4,y+3);
  });

  // Target line
  const ty=pad.t+ch-(target-minV)/range*ch;
  ctx.beginPath();ctx.setLineDash([4,4]);ctx.strokeStyle='rgba(0,229,195,0.4)';ctx.lineWidth=1;
  ctx.moveTo(pad.l,ty);ctx.lineTo(pad.l+cw,ty);ctx.stroke();ctx.setLineDash([]);

  // Gradient fill
  const pts=data.map((v,i)=>({x:pad.l+i*(cw/(data.length-1)),y:pad.t+ch-(v-minV)/range*ch}));
  const grad=ctx.createLinearGradient(0,pad.t,0,pad.t+ch);
  grad.addColorStop(0,'rgba(79,142,247,0.25)');grad.addColorStop(1,'rgba(79,142,247,0)');
  ctx.beginPath();ctx.moveTo(pts[0].x,pts[0].y);
  pts.slice(1).forEach(p=>ctx.lineTo(p.x,p.y));
  ctx.lineTo(pts[pts.length-1].x,pad.t+ch);ctx.lineTo(pts[0].x,pad.t+ch);
  ctx.closePath();ctx.fillStyle=grad;ctx.fill();

  // Line
  ctx.beginPath();ctx.strokeStyle='#4f8ef7';ctx.lineWidth=2;ctx.lineJoin='round';
  pts.forEach((p,i)=>i===0?ctx.moveTo(p.x,p.y):ctx.lineTo(p.x,p.y));ctx.stroke();

  // Dots
  pts.forEach((p,i)=>{
    ctx.beginPath();ctx.arc(p.x,p.y,3.5,0,Math.PI*2);
    ctx.fillStyle=i===pts.length-1?'#4f8ef7':'#0d1117';ctx.fill();
    ctx.strokeStyle='#4f8ef7';ctx.lineWidth=1.5;ctx.stroke();
  });

  // X labels
  ['W1','W2','W3','W4','W5','W6','W7','W8'].forEach((l,i)=>{
    const x=pad.l+i*(cw/(data.length-1));
    ctx.fillStyle='rgba(107,122,154,0.7)';ctx.font='8px DM Mono';ctx.textAlign='center';
    ctx.fillText(l,x,h-4);
  });
}

function drawMonthlyChart(){
  const canvas=document.getElementById('monthly-chart');
  if(!canvas)return;
  const ctx=canvas.getContext('2d');
  const w=canvas.offsetWidth,h=150;
  canvas.width=w*2;canvas.height=h*2;canvas.style.width=w+'px';canvas.style.height=h+'px';
  ctx.scale(2,2);
  const months=['Jan','Feb','Mar'];
  const phoneData=[81,83,86];const chatData=[78,80,83];const emailData=[76,79,82];
  const datasets=[{data:phoneData,color:'#4f8ef7'},{data:chatData,color:'#00e5c3'},{data:emailData,color:'#f7b94f'}];
  const pad={t:12,r:10,b:22,l:30};
  const cw=w-pad.l-pad.r,ch=h-pad.t-pad.b;
  const barW=cw/(months.length*datasets.length+months.length*1.5);
  ctx.clearRect(0,0,w,h);
  const minV=70,maxV=92,range=maxV-minV;
  [75,80,85,90].forEach(v=>{
    const y=pad.t+ch-(v-minV)/range*ch;
    ctx.beginPath();ctx.strokeStyle='rgba(30,45,66,0.8)';ctx.lineWidth=1;
    ctx.moveTo(pad.l,y);ctx.lineTo(pad.l+cw,y);ctx.stroke();
    ctx.fillStyle='rgba(107,122,154,0.6)';ctx.font='8px DM Mono';ctx.textAlign='right';
    ctx.fillText(v,pad.l-3,y+3);
  });
  months.forEach((m,mi)=>{
    const groupX=pad.l+mi*(cw/months.length);
    datasets.forEach((ds,di)=>{
      const x=groupX+di*(barW+1)+barW*0.5;
      const bh=((ds.data[mi]-minV)/range)*ch;
      const y=pad.t+ch-bh;
      ctx.fillStyle=ds.color;ctx.globalAlpha=0.8;
      ctx.beginPath();ctx.roundRect(x,y,barW,bh,2);ctx.fill();ctx.globalAlpha=1;
    });
    ctx.fillStyle='rgba(107,122,154,0.7)';ctx.font='9px DM Mono';ctx.textAlign='center';
    ctx.fillText(m,groupX+cw/months.length/2,h-5);
  });
}

function drawDistChart(){
  const canvas=document.getElementById('dist-chart');
  if(!canvas)return;
  const ctx=canvas.getContext('2d');
  const w=canvas.offsetWidth,h=150;
  canvas.width=w*2;canvas.height=h*2;canvas.style.width=w+'px';canvas.style.height=h+'px';
  ctx.scale(2,2);
  const data=[{label:'Excellent',val:380,color:'#3dd68c'},{label:'Pass',val:520,color:'#4f8ef7'},{label:'Needs Work',val:280,color:'#f7b94f'},{label:'Fail',val:104,color:'#f75f5f'}];
  const total=data.reduce((s,d)=>s+d.val,0);
  const cx=w/2,cy=h/2,r=Math.min(w,h)/2-10;
  let startAngle=-Math.PI/2;
  ctx.clearRect(0,0,w,h);
  data.forEach(d=>{
    const slice=(d.val/total)*Math.PI*2;
    ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,r,startAngle,startAngle+slice);ctx.closePath();
    ctx.fillStyle=d.color;ctx.globalAlpha=0.85;ctx.fill();ctx.globalAlpha=1;
    ctx.strokeStyle=w<300?'rgba(8,11,18,.5)':'rgba(8,11,18,.8)';ctx.lineWidth=2;ctx.stroke();
    startAngle+=slice;
  });
  // Center hole
  ctx.beginPath();ctx.arc(cx,cy,r*0.5,0,Math.PI*2);ctx.fillStyle='#080b12';ctx.fill();
  ctx.fillStyle='rgba(226,232,246,0.9)';ctx.font='bold 13px Syne';ctx.textAlign='center';ctx.textBaseline='middle';
  ctx.fillText('1,284',cx,cy-4);
  ctx.fillStyle='rgba(107,122,154,0.7)';ctx.font='8px DM Mono';
  ctx.fillText('EVALS',cx,cy+10);
}

// ═══ NAVIGATION ═══
const pageTitles={dashboard:'Dashboard',scorecard:'Evaluation Scorecard',tracker:'QA Tracker',feedback:'Agent Feedback',calibration:'Calibration Sessions',disputes:'Dispute Management',agents:'Agent Performance',specialists:'QA Specialists',reports:'Reports & Analytics'};
const pageBadges={dashboard:'March 2026',scorecard:'QA Form',tracker:'All Evals',reports:'Analytics'};

function showPage(id,navEl){
  // Access control check
  if(currentUser && !canAccess(id)){
    document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
    document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
    let denied=document.getElementById('page-denied');
    if(!denied){
      denied=document.createElement('div');
      denied.id='page-denied';
      denied.className='page active';
      denied.innerHTML=`<div class="access-denied"><div class="access-denied-icon">🔒</div><div class="access-denied-title">Access Restricted</div><div class="access-denied-sub">Your role (${ROLES[currentUser.role].label}) does not have permission to view this page.<br>Contact your administrator if you need access.</div></div>`;
      document.querySelector('.content').appendChild(denied);
    } else {
      document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
      denied.classList.add('active');
    }
    document.getElementById('page-title').textContent='Access Restricted';
    document.getElementById('page-title-badge').textContent='';
    document.getElementById('page-title-badge').style.display='none';
    return;
  }

  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
  const pg=document.getElementById('page-'+id);
  if(pg)pg.classList.add('active');
  document.getElementById('page-title').textContent=pageTitles[id]||id;
  const badge=document.getElementById('page-title-badge');
  badge.textContent=pageBadges[id]||'';
  badge.style.display=pageBadges[id]?'':'none';
  if(navEl)navEl.classList.add('active');
  else{
    document.querySelectorAll('.nav-item').forEach(n=>{
      if(n.onclick&&n.onclick.toString().includes("'"+id+"'"))n.classList.add('active');
    });
  }
  // Draw charts on show
  if(id==='dashboard'){ renderDashboard(); setTimeout(drawTrendChart,100); }
  if(id==='reports'){setTimeout(()=>{drawMonthlyChart();drawDistChart();},50);}
  // Role-based disputes view
  if(id==='disputes'){ renderDisputesView(); setTimeout(initDisputeForm,50); }
  // Role Management
  if(id==='role-mgmt'){ renderRoleMgmt(); switchAdminTab('users', document.querySelector('#admin-tabs .tab-btn')); }
  if(id==='sc-builder') renderScBuilder();
  if(id==='scorecard') setTimeout(initScorecardPage,50);
  // Workspaces
  if(id==='workspaces') renderWorkspacesPage();
  if(id==='ws-overview') renderWsOverview();
}

// ═══ MISC ═══
function showToast(msg,type=''){
  const t=document.getElementById('toast');const m=document.getElementById('toast-msg');
  m.textContent=msg;
  t.className='toast show'+(type==='error'?' error':'');
  setTimeout(()=>t.classList.remove('show'),3000);
}
function openNewEval(){
  const modal = document.getElementById('eval-modal');

  // ── Workspaces — always read live from DB ──
  const wsSel = document.getElementById('eval-workspace');
  const allWs = DB.workspaces.filter(w=>!w.isGlobal && w.status==='active');
  wsSel.innerHTML = '<option value="">— Choose workspace —</option>' +
    allWs.map(ws=>`<option value="${ws.id}" ${activeWorkspaceId===ws.id&&activeWorkspaceId!=='ws-global'?'selected':''}>${ws.icon} ${ws.name} · ${ws.region}</option>`).join('');

  // ── Specialists — load ALL from DB immediately (will re-filter on ws select) ──
  const specSel = document.getElementById('eval-specialist');
  const allSpecs = DB.specialists.length ? DB.specialists : [];
  let initSpecs = allSpecs;
  if(currentUser && currentUser.role==='qa-specialist'){
    const self = allSpecs.find(s=>s.name===currentUser.name);
    if(self) initSpecs = [self];
  }
  specSel.innerHTML = '<option value="">— Select specialist —</option>' +
    initSpecs.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
  if(currentUser && currentUser.role==='qa-specialist'){
    const opt = Array.from(specSel.options).find(o=>o.value===currentUser.name);
    if(opt) opt.selected = true;
  }

  // ── Date — default today ──
  const dateEl = document.getElementById('eval-date');
  if(dateEl) dateEl.value = new Date().toISOString().split('T')[0];

  // ── Reset score ──
  const scoreEl = document.getElementById('eval-score');
  if(scoreEl){ scoreEl.value = 80; updateEvalScore(80); }

  // ── Reset preview ──
  document.getElementById('eval-ws-preview').style.display = 'none';
  document.getElementById('eval-agent').innerHTML = '<option value="">— Select workspace first —</option>';

  // ── If already on a workspace, load its agents ──
  if(activeWorkspaceId !== 'ws-global'){
    wsSel.value = activeWorkspaceId;
    onEvalWsChange();
  }

  modal.classList.add('open');
}

function updateEvalScore(val){
  const n = parseInt(val);
  const disp = document.getElementById('eval-score-display');
  if(!disp) return;
  disp.textContent = n;
  disp.style.color = n>=90?'var(--green)':n>=80?'var(--accent)':n>=70?'var(--accent3)':'var(--danger)';
}

function onEvalWsChange(){
  const wsId = document.getElementById('eval-workspace').value;
  const agentSel = document.getElementById('eval-agent');
  const preview  = document.getElementById('eval-ws-preview');

  if(!wsId){
    agentSel.innerHTML = '<option value="">— Select workspace first —</option>';
    preview.style.display = 'none';
    return;
  }

  const ws = getWsById(wsId);
  if(!ws){ preview.style.display='none'; return; }

  // Show workspace preview badge
  preview.style.display = 'flex';
  document.getElementById('eval-ws-dot').style.background = ws.color;
  document.getElementById('eval-ws-preview-name').textContent = ws.icon+' '+ws.name;
  document.getElementById('eval-ws-preview-name').style.color = ws.color;

  // ── AGENTS: always use full DB.agents list ──
  // ws.agents may be stale string-name arrays; DB.agents is the live source of truth
  const allAgents = DB.agents.filter(a => a.status !== 'Inactive');
  // Try workspace filter first, but fall back immediately to ALL if empty
  let wsAgents = [];
  if(ws.agents && ws.agents.length > 0){
    wsAgents = allAgents.filter(a => ws.agents.includes(a.name) || ws.agents.includes(a.empId));
  }
  if(wsAgents.length === 0) wsAgents = allAgents; // always fall back

  agentSel.innerHTML = '<option value="">— Select agent —</option>' +
    wsAgents.sort((a,b)=>a.name.localeCompare(b.name))
            .map(a=>`<option value="${a.name}" data-id="${a.id||''}" data-emp="${a.empId||''}" data-email="${a.email||''}" data-team="${a.team||''}">${a.name}${a.empId?' ('+a.empId+')':''}</option>`)
            .join('');

  // ── SPECIALISTS: always use DB.specialists as primary source ──
  const specSel  = document.getElementById('eval-specialist');
  const curSpec  = specSel ? specSel.value : '';
  let wsSpecs = [];
  if(ws.specialists && ws.specialists.length > 0){
    wsSpecs = DB.specialists.filter(s => ws.specialists.includes(s.name));
  }
  if(wsSpecs.length === 0) wsSpecs = DB.specialists; // always fall back

  // If logged-in user is a QA Specialist, lock to themselves only
  if(currentUser && currentUser.role === 'qa-specialist'){
    const self = wsSpecs.find(s => s.name === currentUser.name);
    if(self) wsSpecs = [self];
  }

  if(specSel){
    specSel.innerHTML = '<option value="">— Select specialist —</option>' +
      wsSpecs.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
    // Restore previous selection, or auto-select if single, or select current user
    if(wsSpecs.length === 1){ specSel.value = wsSpecs[0].name; }
    else if(curSpec && wsSpecs.find(s=>s.name===curSpec)){ specSel.value = curSpec; }
    if(currentUser && currentUser.role==='qa-specialist'){
      const me = wsSpecs.find(s=>s.name===currentUser.name);
      if(me) specSel.value = me.name;
    }
  }

  document.getElementById('eval-agent-info').textContent = wsAgents.length + ' agents';
}

function onEvalAgentChange(){
  const sel = document.getElementById('eval-agent');
  const opt = sel.options[sel.selectedIndex];
  if(!opt || !opt.value) return;
  const info = document.getElementById('eval-agent-info');
  if(info){
    const team = opt.getAttribute('data-team');
    const emp = opt.getAttribute('data-emp');
    info.textContent = [team, emp?'#'+emp:''].filter(Boolean).join(' · ');
  }
}

function goFullScorecard(){
  const wsId = document.getElementById('eval-workspace').value;
  const agentName = document.getElementById('eval-agent').value;
  const spec = document.getElementById('eval-specialist').value;
  if(wsId){ activeWorkspaceId = wsId; }
  document.getElementById('eval-modal').classList.remove('open');
  showPage('scorecard', null);
  setTimeout(()=>{
    // Pre-fill scorecard fields from eval modal selections
    if(agentName){
      const agEl = document.getElementById('sc-agent');
      if(agEl) agEl.value = agentName;
      const agSel = document.getElementById('sc-agent-select');
      if(agSel){
        Array.from(agSel.options).forEach(o=>{ if(o.value===agentName) o.selected=true; });
      }
    }
    if(spec){
      const spEl = document.getElementById('sc-specialist');
      if(spEl) spEl.value = spec;
    }
    if(wsId){
      const wsSel = document.getElementById('sc-workspace');
      if(wsSel){ wsSel.value=wsId; onScWsChange(); }
    }
  }, 250);
}

function quickSubmit(){
  const wsId  = document.getElementById('eval-workspace').value;
  const agent = document.getElementById('eval-agent').value;
  const ch    = document.getElementById('eval-channel').value;
  const spec  = document.getElementById('eval-specialist').value;
  const score = parseInt(document.getElementById('eval-score').value) || 80;
  const dateVal = document.getElementById('eval-date').value;

  if(!wsId){  showToast('Please select a workspace','error'); return; }
  if(!agent){ showToast('Please select an agent','error'); return; }
  if(!spec){  showToast('Please select a QA specialist','error'); return; }

  const ws = getWsById(wsId);
  const status = score>=80?'Pass':score>=70?'Pending':'Fail';
  const date = dateVal
    ? new Date(dateVal).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})
    : new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'});

  const newEval = {
    id: `${wsId.toUpperCase().replace(/-/g,'').slice(0,8)}-${Date.now()}`,
    agent, specialist:spec,
    channel: ch.replace(/[📞💬📧]\s*/,''),
    score, status, date,
    workspace: wsId,
    createdAt: new Date().toISOString(),
  };

  // Save to workspace + global
  if(ws){ ws.evaluations.unshift(newEval); }
  DB.evaluations.unshift(newEval);

  // Save to Firebase
  try{ DataLayer.addEval(newEval); }catch(e){}

  // Update workspace live stats
  if(ws && ws.evaluations.length){
    ws.stats.evals = ws.evaluations.length;
    ws.stats.avgScore = parseFloat((ws.evaluations.reduce((s,e)=>s+e.score,0)/ws.evaluations.length).toFixed(1));
    ws.stats.passRate = Math.round((ws.evaluations.filter(e=>e.status==='Pass').length/ws.evaluations.length)*100);
  }

  // Switch to workspace + refresh views
  activeWorkspaceId = wsId;
  if(ws){
    document.getElementById('topbar-ws-name').textContent = ws.name;
    document.getElementById('topbar-ws-dot').style.background = ws.color;
  }

  document.getElementById('eval-modal').classList.remove('open');
  renderRecentEvals();
  renderTracker();

  showToast(`✅ Saved to ${ws?ws.icon+' '+ws.name:'workspace'} — Score: ${score}% (${status})`);
}
function submitFeedback(){const a=document.getElementById('fb-agent').value;if(!a){showToast('Enter agent name','error');return;}showToast('Feedback sent to agent!');}
function submitCalibration(){const t=document.getElementById('cal-title').value;if(!t){showToast('Enter session title','error');return;}showToast('Calibration session scheduled!');}
// TL disputes view — role-based switching
function renderDisputesView(){
  const isTL = currentUser && currentUser.role === 'tl-cc';
  const submitOnly = document.getElementById('disputes-submit-only');
  const fullView   = document.getElementById('disputes-full');
  if(!submitOnly || !fullView) return;
  // TL sees submit form only; everyone else sees full management view
  submitOnly.style.display = isTL ? 'block' : 'none';
  fullView.style.display   = isTL ? 'none'  : 'block';
  if(isTL) renderTLDisputes();
}

// TL disputes view — shows their submitted disputes
const tlDisputes = [
  {id:'DISP-2026-003', agent:'Ahmed Hassan', eval:'QA-2026-0198', channel:'📞 Phone', score:62, status:'Open',   date:'Mar 26'},
  {id:'DISP-2026-002', agent:'Nour Saleh',   eval:'QA-2026-0187', channel:'💬 Chat',  score:71, status:'Open',   date:'Mar 24'},
  {id:'DISP-2026-001', agent:'Yara Mostafa', eval:'QA-2026-0175', channel:'📧 Email', score:69, status:'Review', date:'Mar 22'},
];

function renderTLDisputes(){
  const list = document.getElementById('tl-disputes-list');
  const countEl = document.getElementById('tl-open-count');
  if(!list) return;
  const open = tlDisputes.filter(d=>d.status==='Open').length;
  if(countEl) countEl.textContent = open + ' open';
  list.innerHTML = tlDisputes.map(d=>`
    <div class="dispute-card">
      <div class="dispute-header">
        <div>
          <div class="dispute-id">${d.id}</div>
          <div style="font-size:13px;font-weight:600;margin-top:3px;">${d.agent} — ${d.eval}</div>
          <div style="font-size:11px;color:var(--text2);margin-top:2px;font-family:'DM Mono',monospace;">${d.channel} · Score: ${d.score}% · ${d.date}</div>
        </div>
        <span class="status-badge ${d.status==='Open'?'status-fail':'status-pending'}">${d.status}</span>
      </div>
    </div>`).join('');
}

function initDisputeForm(){
  // Collect agents from DB.agents PLUS any workspace-level name lists
  // so the dropdown is always fully populated regardless of workspace state
  const nameSet = new Set();
  const agentList = [];

  // Primary source: DB.agents (full records)
  DB.agents.forEach(a => {
    if(!nameSet.has(a.name)){
      nameSet.add(a.name);
      agentList.push(a);
    }
  });
  // Secondary: workspace agents arrays (string names) — catch any not in DB.agents
  DB.workspaces.forEach(ws => {
    (ws.agents || []).forEach(name => {
      if(!nameSet.has(name)){
        nameSet.add(name);
        agentList.push({name, id:name, empId:''});
      }
    });
  });
  // Sort alphabetically
  agentList.sort((a,b) => a.name.localeCompare(b.name));

  const opts = '<option value="">— Select agent —</option>'
    + agentList.map(a=>`<option value="${a.name}">${a.name}${a.empId?' ('+a.empId+')':''}</option>`).join('');

  const sel1 = document.getElementById('disp-agent');
  const sel2 = document.getElementById('disp-agent-mgr');
  if(sel1) sel1.innerHTML = opts;
  if(sel2) sel2.innerHTML = opts;

  // Reset eval dropdowns
  const ev1 = document.getElementById('disp-eval');
  const ev2 = document.getElementById('disp-eval-mgr');
  if(ev1) ev1.innerHTML = '<option value="">— Select agent first —</option>';
  if(ev2) ev2.innerHTML = '<option value="">— Select agent first —</option>';
}

function populateDisputeEvals(agentName, evalSelId, scoreElId){
  const evalSel = document.getElementById(evalSelId);
  if(!evalSel) return;
  if(!agentName){ evalSel.innerHTML = '<option value="">— Select agent first —</option>'; return; }

  // Get ALL evals for this agent across ALL workspaces + DB.evaluations
  const seen = new Set();
  const allEvals = [];
  DB.workspaces.filter(w=>!w.isGlobal).forEach(w=>{
    (w.evaluations||[]).forEach(e=>{ if(!seen.has(e.id)){seen.add(e.id);allEvals.push(e);} });
  });
  (DB.evaluations||[]).forEach(e=>{ if(!seen.has(e.id)){seen.add(e.id);allEvals.push(e);} });
  const agEvals = allEvals.filter(e => e.agent === agentName || e.agentName === agentName);

  if(!agEvals.length){
    evalSel.innerHTML = '<option value="">No evaluations found for this agent</option>';
    return;
  }

  evalSel.innerHTML = '<option value="">— Select evaluation —</option>'
    + agEvals.map(e=>`<option value="${e.id}" data-score="${e.score}" data-date="${e.date}">${e.id} · ${e.score}% · ${e.date}</option>`).join('');

  // Auto-select first eval
  evalSel.selectedIndex = 1;
  const opt = evalSel.options[1];
  if(scoreElId && opt){
    const scEl = document.getElementById(scoreElId);
    if(scEl) scEl.value = opt.getAttribute('data-score') || '';
  }
}

function onDisputeAgentChange(){
  const agentName = document.getElementById('disp-agent').value;
  populateDisputeEvals(agentName, 'disp-eval', 'disp-score');
}

function onDisputeAgentChangeMgr(){
  const agentName = document.getElementById('disp-agent-mgr').value;
  populateDisputeEvals(agentName, 'disp-eval-mgr', null);
}

function logDispute(){
  // TL form
  const agentEl = document.getElementById('disp-agent');
  const evalEl  = document.getElementById('disp-eval');
  const agent   = agentEl ? agentEl.value.trim() : '';
  const evalId  = evalEl  ? evalEl.value.trim()  : '';
  const reason  = (document.getElementById('disp-reason-tl')||{value:''}).value.trim();
  const score   = (document.getElementById('disp-score')||{value:0}).value;

  if(!agent){  showToast('⚠️ Please select an agent','error'); return; }
  if(!evalId){ showToast('⚠️ Please select an evaluation','error'); return; }
  if(!reason){ showToast('⚠️ Enter dispute reason','error'); return; }

  const newId = 'DISP-2026-'+String(Date.now()).slice(-4);
  const date  = new Date().toLocaleDateString('en-US',{month:'short',day:'numeric'});
  tlDisputes.unshift({id:newId, agent, eval:evalId, channel:'📞 Phone', score:parseInt(score)||0, status:'Open', date});

  // Save to active workspace disputes
  const ws = getActiveWs();
  if(ws && !ws.isGlobal && ws.disputes) ws.disputes.unshift({id:newId, agent, evalId, score:parseInt(score)||0, status:'Open', date, reason});

  // Clear form
  ['disp-reason-tl','disp-evidence-tl'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
  if(agentEl) agentEl.value='';
  if(evalEl)  evalEl.innerHTML='<option value="">— Select agent first —</option>';
  const scEl=document.getElementById('disp-score'); if(scEl) scEl.value='';

  const badge=document.getElementById('dispute-badge');
  if(badge) badge.textContent=parseInt(badge.textContent||0)+1;
  renderTLDisputes();
  showToast('🚩 Dispute submitted successfully!');
}

function logDisputeMgr(){
  // Manager/admin form
  const agentEl = document.getElementById('disp-agent-mgr');
  const evalEl  = document.getElementById('disp-eval-mgr');
  const agent   = agentEl ? agentEl.value.trim() : '';
  const evalId  = evalEl  ? evalEl.value.trim()  : '';
  const reason  = (document.getElementById('disp-reason')||{value:''}).value.trim();

  if(!agent){  showToast('⚠️ Please select an agent','error'); return; }
  if(!evalId){ showToast('⚠️ Please select an evaluation','error'); return; }
  if(!reason){ showToast('⚠️ Enter dispute reason','error'); return; }

  const date = new Date().toLocaleDateString('en-US',{month:'short',day:'numeric'});
  const ws   = getActiveWs();
  if(ws && !ws.isGlobal && ws.disputes) ws.disputes.unshift({id:'DISP-'+Date.now(), agent, evalId, status:'Open', date, reason});

  ['disp-reason','disp-evidence'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
  if(agentEl) agentEl.value='';
  if(evalEl)  evalEl.innerHTML='<option value="">— Select agent first —</option>';

  const badge=document.getElementById('dispute-badge');
  if(badge) badge.textContent=parseInt(badge.textContent||0)+1;
  showToast('🚩 Dispute logged successfully!');
}
function resolveDispute(btn,type){const card=btn.closest('.dispute-card');card.style.opacity='.5';card.style.pointerEvents='none';const badge=card.querySelector('.status-badge');badge.className='status-badge status-pass';badge.textContent=type==='uphold'?'Upheld':'Adjusted';const cur=parseInt(document.getElementById('dispute-badge').textContent);if(cur>0)document.getElementById('dispute-badge').textContent=cur-1;showToast(type==='uphold'?'Score upheld. Agent notified.':'Score adjusted. Agent notified.');}

function exportCSV(){
  let csv='Eval ID,Agent,Team,Specialist,Channel,Score,Status,Date\n';
  evals.forEach(e=>{csv+=`${e.id},${e.agent},${e.team},${e.specialist},${e.channel},${e.score}%,${e.status},${e.date}\n`;});
  const a=document.createElement('a');a.href='data:text/csv;charset=utf-8,'+encodeURIComponent(csv);a.download='qa_evaluations.csv';a.click();
  showToast('Exported successfully!');
}

// ═══ NOTIFICATIONS ═══
function toggleNotif(){document.getElementById('notif-panel').classList.toggle('open');}
function markAllRead(){
  document.querySelectorAll('.notif-item.unread').forEach(el=>el.classList.remove('unread'));
  document.getElementById('notif-count').textContent='0';
  document.getElementById('notif-count').style.display='none';
  document.getElementById('notif-panel').classList.remove('open');
  showToast('All notifications marked as read');
}
function markOneRead(el){
  el.classList.remove('unread');
  const count=document.querySelectorAll('.notif-item.unread').length;
  const badge=document.getElementById('notif-count');
  badge.textContent=count;
  if(count===0){badge.style.display='none';document.getElementById('notif-panel').classList.remove('open');}
}
document.addEventListener('click',function(e){
  const panel=document.getElementById('notif-panel');
  if(panel.classList.contains('open')&&!e.target.closest('.notif-btn')&&!e.target.closest('.notif-panel'))panel.classList.remove('open');
});

// ═══ SCORECARD BUILDER ═══

let editingSectionId=null, editingItemId=null, editingItemSectionId=null;
let memberType='agent', editingMemberId=null;

function genId(){return 'x'+Math.random().toString(36).slice(2,8);}

function renderScBuilder(){
  const list=document.getElementById('sc-builder-list');
  const preview=document.getElementById('sc-preview-list');
  if(!list)return;
  let totalPts=scSections.filter(s=>s.type==='scored').reduce((a,s)=>a+s.pts,0);
  document.getElementById('sc-total-pts').textContent=totalPts;

  list.innerHTML=scSections.map((sec,si)=>`
    <div style="background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:14px;margin-bottom:10px;">
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px;">
        <div style="flex:1;">
          <div style="font-size:13px;font-weight:700;color:${sec.type==='fatal'?'var(--danger)':'var(--accent)'};">${si+1}. ${sec.name}</div>
          <div style="font-size:10px;color:var(--text2);margin-top:2px;">${sec.type==='fatal'?'★ Fatal — any deduct = fail':sec.pts+' pts · any deduct = −'+sec.pts+' pts'}</div>
        </div>
        <button class="btn btn-ghost btn-xs" onclick="openEditSection('${sec.id}')">✏️ Edit</button>
        <button class="btn btn-danger btn-xs" onclick="deleteSection('${sec.id}')">🗑</button>
      </div>
      <div style="display:flex;flex-direction:column;gap:5px;">
        ${sec.items.map(item=>`
          <div style="display:flex;align-items:center;gap:8px;padding:7px 10px;background:var(--surface);border-radius:6px;border:1px solid var(--border);">
            <div style="flex:1;">
              <div style="font-size:12px;font-weight:500;">${item.name}</div>
              <div style="font-size:10px;color:var(--text2);">${item.desc}</div>
            </div>
            <button class="btn btn-ghost btn-xs" onclick="openEditItem('${sec.id}','${item.id}')">✏️</button>
            <button class="btn btn-danger btn-xs" onclick="deleteItem('${sec.id}','${item.id}')">🗑</button>
          </div>`).join('')}
      </div>
      <button class="btn btn-ghost btn-xs" style="margin-top:8px;width:100%;" onclick="openAddItem('${sec.id}')">+ Add Attribute</button>
    </div>`).join('');

  preview.innerHTML=scSections.map((sec,si)=>`
    <div style="padding:8px 12px;background:var(--surface2);border-radius:8px;border-left:3px solid ${sec.type==='fatal'?'var(--danger)':'var(--accent)'};">
      <div style="display:flex;justify-content:space-between;font-size:12px;font-weight:600;">
        <span>${si+1}. ${sec.name}</span>
        <span style="color:${sec.type==='fatal'?'var(--danger)':'var(--accent)'};">${sec.type==='fatal'?'★ Fatal':sec.pts+' pts'}</span>
      </div>
      <div style="font-size:10px;color:var(--text2);margin-top:3px;">${sec.items.map(i=>i.name).join(' · ')}</div>
    </div>`).join('');
}

// Section CRUD
function openAddSection(){
  editingSectionId=null;
  document.getElementById('section-modal-title').textContent='Add Section';
  document.getElementById('sm-name').value='';
  document.getElementById('sm-pts').value='';
  document.getElementById('sm-type').value='scored';
  document.getElementById('section-modal').classList.add('open');
}
function openEditSection(id){
  const sec=scSections.find(s=>s.id===id);
  if(!sec)return;
  editingSectionId=id;
  document.getElementById('section-modal-title').textContent='Edit Section';
  document.getElementById('sm-name').value=sec.name;
  document.getElementById('sm-pts').value=sec.pts;
  document.getElementById('sm-type').value=sec.type;
  document.getElementById('section-modal').classList.add('open');
}
function saveSection(){
  const name=document.getElementById('sm-name').value.trim();
  const pts=parseInt(document.getElementById('sm-pts').value)||0;
  const type=document.getElementById('sm-type').value;
  if(!name){showToast('Enter a section name','error');return;}
  if(editingSectionId){
    const sec=scSections.find(s=>s.id===editingSectionId);
    sec.name=name;sec.pts=pts;sec.type=type;
  } else {
    scSections.push({id:genId(),name,pts,type,items:[]});
  }
  closeSectionModal();renderScBuilder();showToast('Section saved!');
}
function deleteSection(id){
  if(!confirm('Delete this section and all its attributes?'))return;
  scSections=scSections.filter(s=>s.id!==id);
  renderScBuilder();showToast('Section deleted');
}
function closeSectionModal(){document.getElementById('section-modal').classList.remove('open');}

// Item CRUD
function openAddItem(secId){
  editingItemId=null;editingItemSectionId=secId;
  document.getElementById('item-modal-title').textContent='Add Attribute';
  document.getElementById('im-name').value='';
  document.getElementById('im-desc').value='';
  document.getElementById('item-modal').classList.add('open');
}
function openEditItem(secId,itemId){
  const sec=scSections.find(s=>s.id===secId);
  const item=sec&&sec.items.find(i=>i.id===itemId);
  if(!item)return;
  editingItemId=itemId;editingItemSectionId=secId;
  document.getElementById('item-modal-title').textContent='Edit Attribute';
  document.getElementById('im-name').value=item.name;
  document.getElementById('im-desc').value=item.desc;
  document.getElementById('item-modal').classList.add('open');
}
function saveItem(){
  const name=document.getElementById('im-name').value.trim();
  const desc=document.getElementById('im-desc').value.trim();
  if(!name){showToast('Enter attribute name','error');return;}
  const sec=scSections.find(s=>s.id===editingItemSectionId);
  if(!sec)return;
  if(editingItemId){
    const item=sec.items.find(i=>i.id===editingItemId);
    item.name=name;item.desc=desc;
  } else {
    sec.items.push({id:genId(),name,desc});
  }
  closeItemModal();renderScBuilder();showToast('Attribute saved!');
}
function deleteItem(secId,itemId){
  const sec=scSections.find(s=>s.id===secId);
  if(sec)sec.items=sec.items.filter(i=>i.id!==itemId);
  renderScBuilder();showToast('Attribute deleted');
}
function closeItemModal(){document.getElementById('item-modal').classList.remove('open');}

function applyScorecard(){
  showToast('Scorecard updated & applied to evaluations!');
}

// ═══ TEAM MANAGEMENT ═══

function switchTeamTab(tab,btn){
  document.querySelectorAll('#page-team-mgmt .tab-btn').forEach(b=>b.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('team-tab-agents').style.display=tab==='agents'?'block':'none';
  document.getElementById('team-tab-specialists').style.display=tab==='specialists'?'block':'none';
}

function renderTeamAgents(){
  const q=(document.getElementById('tm-agent-search')||{value:''}).value.toLowerCase();
  const team=(document.getElementById('tm-agent-team')||{value:''}).value;
  const ch=(document.getElementById('tm-agent-channel')||{value:''}).value;
  const filtered=DB.agents.filter(a=>
    (!q||a.name.toLowerCase().includes(q)||(a.empId||'').includes(q))&&
    (!team||a.team===team)&&(!ch||a.channel===ch)
  );
  const lbl=document.getElementById('agent-count-label');
  if(lbl) lbl.textContent=filtered.length+' agents';
  const tbody=document.getElementById('tm-agents-tbody');
  if(!tbody) return;
  tbody.innerHTML=filtered.map(a=>`
    <tr>
      <td style="font-family:'DM Mono',monospace;font-size:10px;color:var(--text2);">${a.empId||'—'}</td>
      <td><div style="display:flex;align-items:center;gap:8px;">
        <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--accent),var(--purple));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;flex-shrink:0;">${a.name.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase()}</div>
        <span style="font-weight:500;font-size:12px;">${a.name}</span>
      </div></td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;">${a.targetCalls??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;">${a.audits??'—'}</td>
      <td><span class="score-badge ${scoreClass(a.score??0)}">${a.score??'—'}</span></td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;color:${parseFloat(a.salesScore)<0?'var(--danger)':'var(--text)'};">${a.salesScore??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;color:${(a.salesMistakes??0)>5?'var(--danger)':(a.salesMistakes??0)>2?'var(--orange)':'var(--green)'};">${a.salesMistakes??'—'}</td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;text-align:center;color:${(a.failedAudits??0)>5?'var(--danger)':(a.failedAudits??0)>2?'var(--orange)':'var(--green)'};">${a.failedAudits??'—'}</td>
      <td><span class="status-badge ${a.status==='Active'?'status-pass':'status-fail'}">${a.status}</span></td>
      <td style="display:flex;gap:4px;">
        <button class="btn btn-ghost btn-xs" onclick="openEditMember('agent','${a.id}')">✏️</button>
        <button class="btn btn-danger btn-xs" onclick="deleteMember('agent','${a.id}')">🗑</button>
      </td>
    </tr>`).join('');
}

function renderTeamSpecs(){
  const q=(document.getElementById('tm-spec-search')||{value:''}).value.toLowerCase();
  const filtered=teamSpecs.filter(s=>!q||s.name.toLowerCase().includes(q)||s.email.toLowerCase().includes(q));
  document.getElementById('spec-count-label').textContent=filtered.length+' specialists';
  document.getElementById('tm-specs-tbody').innerHTML=filtered.map(s=>`
    <tr>
      <td><div style="display:flex;align-items:center;gap:8px;">
        <div style="width:28px;height:28px;background:linear-gradient(135deg,var(--green),var(--accent2));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;flex-shrink:0;">${s.name.split(' ').map(w=>w[0]).join('').slice(0,2)}</div>
        <span style="font-weight:500;">${s.name}</span>
      </div></td>
      <td style="font-size:11px;color:var(--text2);">${s.email}</td>
      <td style="font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;">${s.phone}</td>
      <td style="font-family:'DM Mono',monospace;font-size:12px;">${s.evals}</td>
      <td><span class="score-badge ${scoreClass(s.avgScore)}">${s.avgScore}%</span></td>
      <td style="max-width:200px;">${(()=>{
        const wsIds = s.workspaces && s.workspaces.length ? s.workspaces
                    : DB.workspaces.filter(w=>!w.isGlobal&&w.specialists&&w.specialists.includes(s.name)).map(w=>w.id);
        if(!wsIds.length) return '<span style="font-size:11px;color:var(--text3);">No workspace assigned</span>';
        return wsIds.map(wid=>{const w=DB.workspaces.find(x=>x.id===wid);return w?`<span style="display:inline-block;padding:1px 6px;background:var(--surface2);border:1px solid var(--border);border-radius:4px;margin:1px;font-size:10px;font-family:'DM Mono',monospace;">${w.icon} ${w.name}</span>`:'';}).join('');
      })()}</td>
      <td><span class="status-badge ${s.status==='Active'?'status-pass':'status-fail'}">${s.status}</span></td>
      <td style="display:flex;gap:4px;">
        <button class="btn btn-ghost btn-xs" onclick="openEditMember('specialist','${s.id}')">✏️</button>
        <button class="btn btn-danger btn-xs" onclick="deleteMember('specialist','${s.id}')">🗑</button>
      </td>
    </tr>`).join('');
}

// ── Workspace checkbox helpers for member modal ──
function mmBuildWsGrid(assignedNames) {
  const grid = document.getElementById('mm-ws-grid');
  if (!grid) return;
  const workspaces = DB.workspaces.filter(w => !w.isGlobal);
  grid.innerHTML = workspaces.map(ws => {
    const checked = assignedNames && assignedNames.indexOf(ws.name || ws.id) !== -1;
    const chkId = 'mm-ws-' + ws.id;
    return `<label for="${chkId}" style="display:flex;align-items:center;gap:8px;font-size:12.5px;cursor:pointer;padding:8px 10px;background:var(--surface2);border-radius:var(--radius-sm);border:1px solid ${checked?'var(--accent)':'var(--border)'};transition:border-color .2s;">
      <input type="checkbox" id="${chkId}" data-wsid="${ws.id}" data-wsname="${ws.name}" ${checked?'checked':''} style="accent-color:var(--accent);width:13px;height:13px;" onchange="this.closest('label').style.borderColor=this.checked?'var(--accent)':'var(--border)'"/>
      <span style="font-size:14px;">${ws.icon}</span>
      <span style="flex:1;font-size:12.5px;">${ws.name}</span>
      <span style="font-size:9px;color:var(--text2);font-family:'DM Mono',monospace;">${ws.region}</span>
    </label>`;
  }).join('');
}
function mmGetCheckedWsIds()  { return Array.from(document.querySelectorAll('#mm-ws-grid input:checked')).map(b=>b.getAttribute('data-wsid')); }
function mmGetCheckedWsNames(){ return Array.from(document.querySelectorAll('#mm-ws-grid input:checked')).map(b=>b.getAttribute('data-wsname')); }
function mmSelectAllWs(val) {
  document.querySelectorAll('#mm-ws-grid input[type=checkbox]').forEach(b=>{
    b.checked=val;
    b.closest('label').style.borderColor=val?'var(--accent)':'var(--border)';
  });
}

function openMemberModal(type){
  memberType = type;
  editingMemberId = null;
  // Set title
  document.getElementById('member-modal-title').textContent = 'Add ' + (type==='agent' ? 'CC Agent' : 'QA Specialist');
  // Show/hide agent-specific fields
  const agentFields = document.getElementById('mm-agent-fields');
  if(agentFields) agentFields.style.display = type==='agent' ? 'block' : 'none';
  // Show/hide specialist workspace section
  const specFields = document.getElementById('mm-specialist-fields');
  if(specFields) { specFields.style.display = type==='specialist' ? 'block' : 'none'; }
  if(type==='specialist') mmBuildWsGrid([]);
  // Show bulk tab only for agents
  const tabs = document.getElementById('member-modal-tabs');
  if(tabs) tabs.style.display = type==='agent' ? 'flex' : 'none';
  // Reset to single tab safely
  const singleTab = document.getElementById('member-tab-single');
  const bulkTab = document.getElementById('member-tab-bulk');
  if(singleTab) singleTab.style.display = 'block';
  if(bulkTab) bulkTab.style.display = 'none';
  // Reset tab btn active states
  document.querySelectorAll('#member-modal-tabs .tab-btn').forEach((b,i) => {
    b.classList.toggle('active', i===0);
  });
  // Clear bulk preview
  try{ clearBulkPreview(); }catch(e){}
  // Clear form fields
  ['mm-name','mm-email','mm-phone'].forEach(id => {
    const el = document.getElementById(id);
    if(el) el.value = '';
  });
  const statusEl = document.getElementById('mm-status');
  if(statusEl) statusEl.value = 'Active';
  // Open modal
  document.getElementById('member-modal').classList.add('open');
}
function openEditMember(type,id){
  memberType=type;editingMemberId=id;
  const rec=type==='agent'?teamAgents.find(a=>a.id===id):teamSpecs.find(s=>s.id===id);
  if(!rec)return;
  document.getElementById('member-modal-title').textContent='Edit '+(type==='agent'?'CC Agent':'QA Specialist')+' — '+rec.name;
  document.getElementById('mm-agent-fields').style.display=type==='agent'?'block':'none';
  // Show/hide specialist workspace section
  const specFields = document.getElementById('mm-specialist-fields');
  if(specFields) specFields.style.display = type==='specialist' ? 'block' : 'none';
  // Show bulk tab only for agents
  const tabs = document.getElementById('member-modal-tabs');
  if(tabs) tabs.style.display = type==='agent' ? 'flex' : 'none';
  document.getElementById('mm-name').value=rec.name||'';
  document.getElementById('mm-email').value=rec.email||'';
  document.getElementById('mm-phone').value=rec.phone||'';
  document.getElementById('mm-status').value=rec.status||'Active';
  if(type==='agent'){
    document.getElementById('mm-team').value=rec.team||'Team A';
    document.getElementById('mm-channel').value=rec.channel||'📞 Phone';
  }
  if(type==='specialist'){
    // Pre-tick workspaces where this specialist is currently assigned
    const assignedWsIds = rec.workspaces || [];
    // Also check from ws.specialists arrays as fallback
    const fromWs = DB.workspaces.filter(w=>!w.isGlobal && w.specialists && w.specialists.includes(rec.name)).map(w=>w.id);
    const allAssigned = [...new Set([...assignedWsIds, ...fromWs])];
    mmBuildWsGrid(allAssigned.length ? allAssigned : []);
    // Override: use IDs not names in grid check — rebuild with IDs
    setTimeout(()=>{
      document.querySelectorAll('#mm-ws-grid input[type=checkbox]').forEach(cb=>{
        const wsid = cb.getAttribute('data-wsid');
        const chk = allAssigned.indexOf(wsid) !== -1;
        cb.checked = chk;
        cb.closest('label').style.borderColor = chk ? 'var(--accent)' : 'var(--border)';
      });
    }, 50);
  }
  document.getElementById('member-modal').classList.add('open');
}
function saveMember(){
  const name=document.getElementById('mm-name').value.trim();
  const email=document.getElementById('mm-email').value.trim();
  const phone=document.getElementById('mm-phone').value.trim();
  const status=document.getElementById('mm-status').value;
  if(!name){showToast('Enter a name','error');return;}
  if(memberType==='agent'){
    const team=document.getElementById('mm-team').value;
    const channel=document.getElementById('mm-channel').value;
    if(editingMemberId){
      const a=DB.agents.find(x=>x.id===editingMemberId);
      if(a){
        Object.assign(a,{name,email,phone,team,channel,status});
        // Auto-save to Firebase
        try{ DataLayer.updateAgent(a.id,a); }catch(e){}
      }
    } else {
      const newAgent={id:genId(),name,email,phone,team,channel,status};
      DB.agents.push(newAgent);
      teamAgents = DB.agents;
      // Auto-save to Firebase
      try{ DataLayer.addAgent(newAgent); }catch(e){}
    }
    renderTeamAgents();
  } else {
    // Gather workspace assignments from checkboxes
    const wsIds   = mmGetCheckedWsIds();
    const wsNames = mmGetCheckedWsNames();
    // Sync each workspace's specialists array
    DB.workspaces.forEach(ws => {
      if(ws.isGlobal) return;
      if(!ws.specialists) ws.specialists = [];
      const specName = name;
      const shouldBeIn = wsIds.indexOf(ws.id) !== -1;
      const isIn = ws.specialists.indexOf(specName) !== -1;
      if(shouldBeIn && !isIn) ws.specialists.push(specName);
      if(!shouldBeIn && isIn) ws.specialists = ws.specialists.filter(n=>n!==specName);
      // If editing and name changed, remove old name
      if(editingMemberId){
        const oldSpec = DB.specialists.find(x=>x.id===editingMemberId);
        if(oldSpec && oldSpec.name !== specName){
          ws.specialists = ws.specialists.filter(n=>n!==oldSpec.name);
        }
      }
      try{ DataLayer.saveWorkspace(ws); }catch(e){}
    });
    if(editingMemberId){
      const s=DB.specialists.find(x=>x.id===editingMemberId);
      if(s){
        Object.assign(s,{name,email,phone,status,workspaces:wsIds});
        try{ DataLayer.updateSpecialist(s.id,s); }catch(e){}
      }
    } else {
      const newSpec={id:genId(),name,email,phone,evals:0,avgScore:0,status,workspaces:wsIds};
      DB.specialists.push(newSpec);
      teamSpecs = DB.specialists;
      try{ DataLayer.addSpecialist(newSpec); }catch(e){}
    }
    renderTeamSpecs();
  }
  updateSpecialistDropdowns();
  closeMemberModal();
  showToast((memberType==='agent'?'Agent':'Specialist')+' saved! 🔥 Synced to Firebase');
}

function deleteMember(type,id){
  if(!confirm('Delete this member?'))return;
  if(type==='agent'){
    DB.agents = DB.agents.filter(a=>a.id!==id);
    teamAgents = DB.agents;
    try{ DataLayer.deleteAgent(id); }catch(e){}
  } else {
    DB.specialists = DB.specialists.filter(s=>s.id!==id);
    teamSpecs = DB.specialists;
    try{ DataLayer.deleteSpecialist(id); }catch(e){}
  }
  renderTeamAgents();renderTeamSpecs();
  updateSpecialistDropdowns();
  showToast('Member deleted');
}
function closeMemberModal(){document.getElementById('member-modal').classList.remove('open');}

function updateSpecialistDropdowns(){
  const names=teamSpecs.map(s=>s.name);
  ['sc-specialist','m-spec'].forEach(id=>{
    const sel=document.getElementById(id);
    if(!sel)return;
    const cur=sel.value;
    // Keep empty option for sc-specialist
    if(id==='sc-specialist'){
      sel.innerHTML='<option value="">Select specialist</option>';
    } else {
      sel.innerHTML='';
    }
    names.forEach(n=>{
      const o=document.createElement('option');
      o.value=n;o.textContent=n;sel.appendChild(o);
    });
    if(names.includes(cur))sel.value=cur;
  });
}

// ═══ RBAC — ROLES & ACCESS CONTROL ═══

// Role definitions: which pages each role can access

// Demo users

let currentUser = null;

function quickLogin(username, password){
  document.getElementById('login-user').value = username;
  document.getElementById('login-pass').value = password;
  doLogin();
}

function completeLogin(user){
  currentUser=user;
  const userWs = DB.workspaces.find(ws=>
    !ws.isGlobal && ws.users && ws.users.includes(user.username)
  );
  if(userWs && user.role !== 'admin' && user.role !== 'qa-manager'){
    activeWorkspaceId = userWs.id;
  } else {
    activeWorkspaceId = 'ws-global';
  }
  document.getElementById('login-screen').style.display='none';
  document.querySelector('.sidebar').style.display='flex';
  document.querySelector('.main').style.display='flex';
  applyRoleUI();
  const ws = getActiveWs();
  document.getElementById('topbar-ws-name').textContent = ws.name;
  document.getElementById('topbar-ws-dot').style.background = ws.color;
  renderWsList();
  const role=ROLES[user.role];
  const firstPage=role&&role.pages[0]?role.pages[0]:'dashboard';
  showPage(firstPage,null);
  showToast('Welcome, '+user.name+'!');
}

async function doLogin(){
  const u=document.getElementById('login-user').value.trim().toLowerCase();
  const p=document.getElementById('login-pass').value;
  const errEl=document.getElementById('login-error');

  if(!u || !p){
    errEl.classList.add('show');
    errEl.textContent='Enter your username/email and password.';
    setTimeout(()=>errEl.classList.remove('show'),3000);
    return;
  }

  try {
    const user = await DataLayer.findUser(u,p);
    if(!user){
      errEl.classList.add('show');
      errEl.textContent='Invalid Firebase credentials or missing user profile.';
      setTimeout(()=>errEl.classList.remove('show'),3000);
      return;
    }
    await storeAuthToken(_fbAuth.currentUser);
    completeLogin(user);
  } catch(e) {
    errEl.classList.add('show');
    errEl.textContent = e && e.message ? e.message : 'Firebase login failed.';
    setTimeout(()=>errEl.classList.remove('show'),4000);
  }
}

async function doLogout(){
  currentUser=null;
  try{
    if(_fbAuth && _fbAuth.currentUser) await _fbAuth.signOut();
  }catch(e){
    console.warn('Firebase sign-out failed:', e.message || e);
  } finally {
    clearStoredAuthToken();
  }
  document.getElementById('login-screen').style.display='flex';
  document.querySelector('.sidebar').style.display='none';
  document.querySelector('.main').style.display='none';
  document.getElementById('login-user').value='';
  document.getElementById('login-pass').value='';
}

function restoreSession(){
  // Local session restore disabled: auth is Firebase-only.
  return false;
}

function applyRoleUI(){
  if(!currentUser) return;
  const role = ROLES[currentUser.role];

  // Update sidebar user info
  document.getElementById('sb-avatar').textContent = currentUser.initials;
  document.getElementById('sb-username').textContent = currentUser.name;
  document.getElementById('sb-role').textContent = role.label;

  // Update topbar role badge
  const rb = document.getElementById('topbar-role-badge');
  rb.textContent = role.label;
  rb.className = 'role-badge ' + role.badge;

  // Show/hide nav items based on allowed pages
  // sc-builder is ALWAYS visible for anyone who can do evaluations
  const alwaysVisible = ['sc-builder'];
  document.querySelectorAll('.nav-item').forEach(item => {
    const onclick = item.getAttribute('onclick') || '';
    const match = onclick.match(/showPage\('([^']+)'/);
    if(!match) return;
    const pageId = match[1];
    const allowed = alwaysVisible.includes(pageId) || role.pages.includes(pageId) || role.canAdmin;
    item.style.display = allowed ? 'flex' : 'none';
  });

  // Show/hide nav sections if all items hidden
  document.querySelectorAll('.nav-section').forEach(sec => {
    let next = sec.nextElementSibling;
    let anyVisible = false;
    while(next && !next.classList.contains('nav-section')){
      if(next.classList.contains('nav-item') && next.style.display !== 'none') anyVisible = true;
      next = next.nextElementSibling;
    }
    sec.style.display = anyVisible ? '' : 'none';
  });

  // Show/hide action buttons
  const newEvalBtn = document.querySelector('.btn-primary[onclick="openNewEval()"]');
  if(newEvalBtn) newEvalBtn.style.display = role.canNewEval ? '' : 'none';
  const exportBtn = document.querySelector('.btn-ghost[onclick="exportCSV()"]');
  if(exportBtn) exportBtn.style.display = role.canExport ? '' : 'none';
}

function canAccess(pageId){
  if(!currentUser) return false;
  // sc-builder accessible to everyone except basic agents
  if(pageId==='sc-builder' && currentUser.role!=='agent') return true;
  const role = ROLES[currentUser.role];
  return role.pages.includes(pageId) || role.canAdmin;
}

// Wrap showPage to enforce access — merged into single definition above

// ═══ ADMIN CONTROL CENTER ═══

const ALL_PAGES = ['dashboard','scorecard','tracker','feedback','calibration','disputes','agents','specialists','reports','sc-builder','team-mgmt','role-mgmt','workspaces','ws-overview'];
const PAGE_LABELS = {dashboard:'Dashboard',scorecard:'Scorecard',tracker:'QA Tracker',feedback:'Feedback',calibration:'Calibration',disputes:'Disputes',agents:'Agents',specialists:'QA Specialists',reports:'Reports','sc-builder':'Scorecard Builder','team-mgmt':'Team Mgmt','role-mgmt':'Access & Roles',workspaces:'Workspaces','ws-overview':'WS Overview'};

// App config (editable)

let editingUserId = null;

function switchAdminTab(tab, btn){
  document.querySelectorAll('#admin-tabs .tab-btn').forEach(b=>b.classList.remove('active'));
  if(btn) btn.classList.add('active');
  ['users','roles','scorecard-cfg','app-settings'].forEach(t=>{
    const el = document.getElementById('admin-tab-'+t);
    if(el) el.style.display = t===tab ? 'block' : 'none';
  });
  if(tab==='users') renderAdminUsers();
  if(tab==='roles') renderRoleEditor();
  if(tab==='scorecard-cfg') renderScBuilderCfg();
  if(tab==='app-settings') renderAppSettings();
}

// ── USERS ──
function renderAdminUsers(){
  const tbody = document.getElementById('admin-users-tbody');
  if(!tbody) return;
  tbody.innerHTML = DataLayer.getUsers().map(u => {
    const r = ROLES[u.role]||{label:u.role,badge:'',color:'var(--text2)'};
    return `<tr>
      <td><div style="display:flex;align-items:center;gap:8px;">
        <div style="width:28px;height:28px;background:linear-gradient(135deg,${r.color},var(--bg));border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff;flex-shrink:0;">${u.initials}</div>
        <span style="font-weight:500;">${u.name}</span>
      </div></td>
      <td style="font-family:'DM Mono',monospace;font-size:11px;color:var(--accent);">@${u.username}</td>
      <td><span class="role-badge ${r.badge}">${r.label}</span></td>
      <td><span class="status-badge status-pass">Active</span></td>
      <td style="font-size:11px;color:var(--text2);font-family:'DM Mono',monospace;">Mar 29, 2026</td>
      <td style="display:flex;gap:4px;">
        <button class="btn btn-ghost btn-xs" onclick="openEditUser('${u.id}')">✏️ Edit</button>
        <button class="btn btn-danger btn-xs" onclick="deleteUser('${u.id}')">🗑</button>
      </td>
    </tr>`;
  }).join('');
}

function openUserModal(){
  editingUserId = null;
  document.getElementById('user-modal-title').textContent = 'Add New User';
  ['um-name','um-username','um-password','um-initials'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('um-role').value = 'qa-specialist';
  document.getElementById('um-status').value = 'Active';
  document.getElementById('user-modal').classList.add('open');
}

function openEditUser(id){
  editingUserId = id;
  const u = DataLayer.getUsers().find(x=>x.id===id);
  if(!u) return;
  document.getElementById('user-modal-title').textContent = 'Edit User';
  document.getElementById('um-name').value = u.name;
  document.getElementById('um-username').value = u.username;
  document.getElementById('um-password').value = u.password;
  document.getElementById('um-role').value = u.role;
  document.getElementById('um-initials').value = u.initials;
  document.getElementById('user-modal').classList.add('open');
}

function saveUser(){
  const name = document.getElementById('um-name').value.trim();
  const username = document.getElementById('um-username').value.trim().toLowerCase();
  const password = document.getElementById('um-password').value.trim();
  const role = document.getElementById('um-role').value;
  const initials = document.getElementById('um-initials').value.trim().toUpperCase() || name.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase();
  if(!name || !username || !password){ showToast('Please fill all required fields','error'); return; }
  if(editingUserId !== null){
    DataLayer.updateUser(editingUserId, {name, username, password, role, initials});
    showToast('User updated: '+name);
  } else {
    if(DataLayer.getUsers().find(u=>u.username===username)){ showToast('Username already exists','error'); return; }
    DataLayer.addUser({id:genId(), username, password, role, name, initials});
    showToast('User created: '+name);
  }
  closeUserModal();
  renderAdminUsers();
  updateSpecialistDropdowns();
}

function deleteUser(id){
  const u = DataLayer.getUsers().find(x=>x.id===id);
  if(!u) return;
  if(currentUser && u.username === currentUser.username){ showToast('Cannot delete your own account','error'); return; }
  if(!confirm('Delete user: '+u.name+'?')) return;
  DataLayer.deleteUser(id);
  renderAdminUsers();
  showToast('User deleted');
}

function closeUserModal(){ document.getElementById('user-modal').classList.remove('open'); }

// ── ROLES EDITOR ──
function renderRoleEditor(){
  // Page access matrix
  const matrix = document.getElementById('page-access-matrix');
  if(matrix){
    const roleKeys = Object.keys(ROLES);
    matrix.innerHTML = `
      <div style="overflow-x:auto;">
        <table style="width:100%;font-size:11px;">
          <thead>
            <tr>
              <th style="text-align:left;padding:6px 8px;color:var(--text2);font-family:'DM Mono',monospace;font-size:9px;">PAGE</th>
              ${roleKeys.map(k=>`<th style="padding:6px 4px;text-align:center;"><span class="role-badge ${ROLES[k].badge}" style="font-size:8px;">${ROLES[k].label.split(' ')[0]}</span></th>`).join('')}
            </tr>
          </thead>
          <tbody>
            ${ALL_PAGES.map(page=>`
              <tr style="border-bottom:1px solid var(--border);">
                <td style="padding:7px 8px;font-size:11px;color:var(--text);font-family:'DM Mono',monospace;">${page}</td>
                ${roleKeys.map(rk=>`
                  <td style="text-align:center;padding:7px 4px;">
                    <input type="checkbox" ${ROLES[rk].pages.includes(page)?'checked':''}
                      onchange="togglePageAccess('${rk}','${page}',this.checked)"
                      ${rk==='admin'?'disabled title="Admin always has full access"':''}
                      style="width:14px;height:14px;cursor:pointer;accent-color:var(--accent);">
                  </td>`).join('')}
              </tr>`).join('')}
          </tbody>
        </table>
      </div>`;
  }

  // Role permissions (canNewEval, canDispute, etc.)
  const perms = document.getElementById('role-permissions-editor');
  if(perms){
    const permKeys = ['canNewEval','canDispute','canExport','canAdmin'];
    const permLabels = {canNewEval:'New Evaluation',canDispute:'Submit Dispute',canExport:'Export CSV',canAdmin:'Admin Access'};
    perms.innerHTML = Object.entries(ROLES).map(([key,role])=>`
      <div style="background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:12px;margin-bottom:8px;">
        <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px;">
          <span class="role-badge ${role.badge}">${role.label}</span>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:6px;">
          ${permKeys.map(pk=>`
            <label style="display:flex;align-items:center;gap:6px;font-size:11px;color:var(--text);cursor:pointer;">
              <input type="checkbox" ${role[pk]?'checked':''} ${key==='admin'?'disabled':''}
                onchange="toggleRolePerm('${key}','${pk}',this.checked)"
                style="accent-color:var(--accent);width:13px;height:13px;">
              ${permLabels[pk]}
            </label>`).join('')}
        </div>
      </div>`).join('');
  }
}

function togglePageAccess(roleKey, page, checked){
  const role = ROLES[roleKey];
  if(checked && !role.pages.includes(page)) role.pages.push(page);
  else if(!checked) role.pages = role.pages.filter(p=>p!==page);
  showToast(`${ROLES[roleKey].label}: ${page} ${checked?'enabled':'disabled'}`);
  // Re-apply UI if current user's role changed
  if(currentUser && currentUser.role === roleKey) applyRoleUI();
}

function toggleRolePerm(roleKey, perm, checked){
  ROLES[roleKey][perm] = checked;
  showToast(`${ROLES[roleKey].label}: ${perm} ${checked?'enabled':'disabled'}`);
  if(currentUser && currentUser.role === roleKey) applyRoleUI();
}

// ── SCORECARD CONFIG ──
function renderScBuilderCfg(){
  const list = document.getElementById('sc-cfg-list');
  const preview = document.getElementById('sc-cfg-preview');
  const totalNum = document.getElementById('sc-cfg-total-num');
  if(!list) return;
  const total = scSections.filter(s=>s.type==='scored').reduce((a,s)=>a+s.pts,0);
  if(totalNum) totalNum.textContent = total;
  list.innerHTML = scSections.map((sec,si)=>`
    <div style="background:var(--surface2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:12px;margin-bottom:8px;">
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:8px;">
        <div style="flex:1;">
          <span style="font-size:12px;font-weight:700;color:${sec.type==='fatal'?'var(--danger)':'var(--accent)'};">${si+1}. ${sec.name}</span>
          <span style="font-size:10px;color:var(--text2);margin-left:8px;">${sec.type==='fatal'?'★ Fatal':sec.pts+' pts'}</span>
        </div>
        <button class="btn btn-ghost btn-xs" onclick="openEditSection('${sec.id}')">✏️</button>
        <button class="btn btn-danger btn-xs" onclick="deleteSection('${sec.id}');renderScBuilderCfg()">🗑</button>
      </div>
      ${sec.items.map(item=>`
        <div style="display:flex;align-items:center;gap:6px;padding:5px 8px;background:var(--surface);border-radius:5px;margin-bottom:4px;border:1px solid var(--border);">
          <span style="flex:1;font-size:11px;">${item.name}</span>
          <button class="btn btn-ghost btn-xs" onclick="openEditItem('${sec.id}','${item.id}')">✏️</button>
          <button class="btn btn-danger btn-xs" onclick="deleteItem('${sec.id}','${item.id}');renderScBuilderCfg()">🗑</button>
        </div>`).join('')}
      <button class="btn btn-ghost btn-xs" style="width:100%;margin-top:4px;" onclick="openAddItem('${sec.id}')">+ Attribute</button>
    </div>`).join('');
  if(preview){
    preview.innerHTML = scSections.map((sec,si)=>`
      <div style="padding:7px 10px;background:var(--surface2);border-radius:6px;border-left:3px solid ${sec.type==='fatal'?'var(--danger)':'var(--accent)'};margin-bottom:6px;">
        <div style="display:flex;justify-content:space-between;font-size:12px;">
          <span style="font-weight:600;">${si+1}. ${sec.name}</span>
          <span style="color:${sec.type==='fatal'?'var(--danger)':'var(--accent)'};">${sec.type==='fatal'?'★ Fatal':sec.pts+' pts'}</span>
        </div>
      </div>`).join('');
  }
}

// ── APP SETTINGS ──
function renderAppSettings(){
  // Populate fields from appConfig
  const fields = {
    'cfg-company': appConfig.company,
    'cfg-dept': appConfig.dept,
    'cfg-target': appConfig.target,
    'cfg-monthly-target': appConfig.monthlyTarget,
    'cfg-pass': appConfig.passThreshold,
    'cfg-excellent': appConfig.excellentThreshold,
    'cfg-dispute-window': appConfig.disputeWindow,
    'cfg-coaching-threshold': appConfig.coachingThreshold,
  };
  Object.entries(fields).forEach(([id,val])=>{ const el=document.getElementById(id); if(el) el.value=val; });

  // Render theme grid
  const grid = document.getElementById('admin-theme-grid');
  if(grid){
    grid.innerHTML = THEMES.map(t=>`
      <div class="theme-swatch ${activeTheme===t.id?'active':''}" onclick="applyTheme('${t.id}')" title="${t.name}">
        <div class="theme-swatch-preview" style="background:linear-gradient(135deg,${t.colors[0]} 0%,${t.colors[1]} 60%,${t.colors[2]} 100%);border:1px solid rgba(255,255,255,0.1);"></div>
        <div class="theme-swatch-label">${t.name}</div>
      </div>`).join('');
  }
}

function saveAppSettings(){
  appConfig = {
    company: document.getElementById('cfg-company').value || appConfig.company,
    dept: document.getElementById('cfg-dept').value || appConfig.dept,
    target: parseInt(document.getElementById('cfg-target').value) || 80,
    monthlyTarget: parseInt(document.getElementById('cfg-monthly-target').value) || 1350,
    passThreshold: parseInt(document.getElementById('cfg-pass').value) || 80,
    excellentThreshold: parseInt(document.getElementById('cfg-excellent').value) || 90,
    disputeWindow: parseInt(document.getElementById('cfg-dispute-window').value) || 48,
    coachingThreshold: parseInt(document.getElementById('cfg-coaching-threshold').value) || 75,
  };
  // Update logo title
  const logoTitle = document.querySelector('.logo-title');
  if(logoTitle) logoTitle.textContent = appConfig.company;
  showToast('✓ App settings saved!');
}

// Keep old renderRoleMgmt as alias for when role-mgmt page loads
function renderRoleMgmt(){
  renderAdminUsers();
}

pageTitles['role-mgmt'] = 'Access & Roles';
pageBadges['role-mgmt'] = 'Admin';

// ═══ THEME SWITCHER ═══
const THEMES = [
  { id: 'default', name: 'Ocean Blue',   colors: ['#080b12','#4f8ef7','#00e5c3'] },
  { id: 'purple',  name: 'Purple Galaxy',colors: ['#0a0812','#a78bfa','#f472b6'] },
  { id: 'emerald', name: 'Emerald Dark', colors: ['#061210','#10b981','#06d6a0'] },
  { id: 'red',     name: 'Midnight Red', colors: ['#0f0808','#ef4444','#f97316'] },
  { id: 'teal',    name: 'Ocean Teal',   colors: ['#060e12','#06b6d4','#22d3ee'] },
  { id: 'gold',    name: 'Sunset Gold',  colors: ['#0f0c04','#f59e0b','#fb923c'] },
  { id: 'slate',   name: 'Slate Pro',    colors: ['#0d1117','#58a6ff','#39d353'] },
  { id: 'light',   name: 'Light Mode',   colors: ['#f1f5f9','#3b82f6','#06b6d4'] },
];

let activeTheme = 'default';

function renderThemeGrid(){
  const grid = document.getElementById('theme-grid');
  if(!grid) return;
  grid.innerHTML = THEMES.map(t => `
    <div class="theme-swatch ${activeTheme===t.id?'active':''}" onclick="applyTheme('${t.id}')" title="${t.name}">
      <div class="theme-swatch-preview" style="background:linear-gradient(135deg,${t.colors[0]} 0%,${t.colors[1]} 60%,${t.colors[2]} 100%);border:1px solid rgba(255,255,255,0.1);"></div>
      <div class="theme-swatch-label">${t.name}</div>
    </div>`).join('');
}

function applyTheme(id){
  activeTheme = id;
  document.documentElement.setAttribute('data-theme', id === 'default' ? '' : id);
  renderThemeGrid();
  // Save preference
  try{ localStorage.setItem('qms-theme', id); } catch(e){}
  document.getElementById('theme-panel').classList.remove('open');
  showToast('Theme applied: ' + (THEMES.find(t=>t.id===id)||{name:id}).name);
}

function toggleThemePanel(){
  const panel = document.getElementById('theme-panel');
  panel.classList.toggle('open');
  renderThemeGrid();
}

// Close theme panel on outside click
document.addEventListener('click', function(e){
  const panel = document.getElementById('theme-panel');
  if(panel && panel.classList.contains('open') && !e.target.closest('.theme-switcher-btn') && !e.target.closest('.theme-panel')){
    panel.classList.remove('open');
  }
});

// Load saved theme
try{
  const saved = localStorage.getItem('qms-theme');
  if(saved) applyTheme(saved);
} catch(e){}

// ═══ BULK UPLOAD ═══
let bulkParsedAgents = [];

function switchMemberTab(tab, btn){
  document.querySelectorAll('#member-modal-tabs .tab-btn').forEach(b=>b.classList.remove('active'));
  if(btn) btn.classList.add('active');
  document.getElementById('member-tab-single').style.display = tab==='single' ? 'block' : 'none';
  document.getElementById('member-tab-bulk').style.display   = tab==='bulk'   ? 'block' : 'none';
}

function downloadBulkTemplate(){
  const csv = `name,email,phone,team,channel,status
Ahmed Hassan,ahmed@company.com,+201000000001,Team A,Phone,Active
Nour Saleh,nour@company.com,+201000000002,Team B,Chat,Active
Fatima Al-Zahraa,fatima@company.com,+201000000003,Team C,Email,Active`;
  const a = document.createElement('a');
  a.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(csv);
  a.download = 'agents_template.csv';
  a.click();
  showToast('Template downloaded!');
}

function handleBulkDrop(e){
  e.preventDefault();
  document.getElementById('bulk-dropzone').classList.remove('drag-over');
  const file = e.dataTransfer.files[0];
  if(file && file.name.endsWith('.csv')) parseBulkCSV(file);
  else showToast('Please upload a .csv file','error');
}

function handleBulkFile(input){
  const file = input.files[0];
  if(file) parseBulkCSV(file);
  input.value = '';
}

function parseBulkCSV(file){
  const reader = new FileReader();
  reader.onload = function(e){
    const text = e.target.result;
    const lines = text.trim().split('\n').map(l=>l.trim()).filter(l=>l);
    if(lines.length < 2){ showToast('CSV is empty or has no data rows','error'); return; }

    const headers = lines[0].split(',').map(h=>h.trim().toLowerCase());
    const required = ['name','email','phone','team','channel','status'];
    const missing = required.filter(r=>!headers.includes(r));
    if(missing.length > 0){
      showToast('Missing columns: '+missing.join(', '),'error');
      return;
    }

    bulkParsedAgents = [];
    const errors = [];
    const validChannels = ['phone','chat','email'];
    const validTeams = ['team a','team b','team c','team d'];
    const validStatus = ['active','inactive'];

    lines.slice(1).forEach((line, idx)=>{
      const cols = line.split(',').map(c=>c.trim());
      const row = {};
      headers.forEach((h,i)=>{ row[h] = cols[i]||''; });
      const rowErrors = [];
      if(!row.name) rowErrors.push('Name required');
      if(!row.email || !row.email.includes('@')) rowErrors.push('Valid email required');
      if(!validChannels.includes(row.channel.toLowerCase().replace(/[📞💬📧]\s*/,''))) rowErrors.push('Channel must be Phone/Chat/Email');
      if(!validTeams.includes(row.team.toLowerCase())) rowErrors.push('Team must be Team A/B/C/D');
      if(!validStatus.includes(row.status.toLowerCase())) rowErrors.push('Status must be Active/Inactive');

      bulkParsedAgents.push({
        name: row.name,
        email: row.email,
        phone: row.phone || '—',
        team: row.team,
        channel: row.channel.charAt(0).toUpperCase() + row.channel.slice(1).toLowerCase(),
        status: row.status.charAt(0).toUpperCase() + row.status.slice(1).toLowerCase(),
        errors: rowErrors
      });
    });

    renderBulkPreview();
  };
  reader.readAsText(file);
}

function renderBulkPreview(){
  const preview = document.getElementById('bulk-preview');
  const tbody = document.getElementById('bulk-preview-tbody');
  const head = document.getElementById('bulk-preview-head');
  const countEl = document.getElementById('bulk-preview-count');
  const errorsEl = document.getElementById('bulk-errors');
  const submitBtn = document.getElementById('bulk-submit-btn');

  preview.style.display = 'block';
  const validCount = bulkParsedAgents.filter(a=>a.errors.length===0).length;
  const errorCount = bulkParsedAgents.filter(a=>a.errors.length>0).length;

  countEl.innerHTML = `<span style="color:var(--green);">✓ ${validCount} valid</span>${errorCount>0?` &nbsp;·&nbsp; <span style="color:var(--danger);">✕ ${errorCount} errors</span>`:''}`;
  submitBtn.style.display = validCount > 0 ? 'flex' : 'none';
  submitBtn.textContent = `✓ Import ${validCount} Agent${validCount!==1?'s':''}`;

  head.innerHTML = ['Name','Email','Phone','Team','Channel','Status',''].map(h=>
    `<th style="padding:7px 10px;text-align:left;font-family:'DM Mono',monospace;font-size:9px;color:var(--text2);border-bottom:1px solid var(--border);text-transform:uppercase;letter-spacing:1px;">${h}</th>`
  ).join('');

  tbody.innerHTML = bulkParsedAgents.map((a,i)=>{
    const hasErr = a.errors.length > 0;
    return `<tr class="${hasErr?'bulk-error-row':''}">
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);">${a.name}</td>
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);color:var(--text2);">${a.email}</td>
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);color:var(--text2);font-family:'DM Mono',monospace;">${a.phone}</td>
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);">${a.team}</td>
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);">${a.channel}</td>
      <td style="padding:7px 10px;font-size:11px;border-bottom:1px solid var(--border);">${a.status}</td>
      <td style="padding:7px 10px;font-size:10px;border-bottom:1px solid var(--border);color:${hasErr?'var(--danger)':'var(--green)'};">${hasErr?'⚠ '+a.errors.join(', '):'✓'}</td>
    </tr>`;
  }).join('');
}

function clearBulkPreview(){
  bulkParsedAgents = [];
  document.getElementById('bulk-preview').style.display = 'none';
  document.getElementById('bulk-submit-btn').style.display = 'none';
  document.getElementById('bulk-file-input').value = '';
}

function submitBulkAgents(){
  const valid = bulkParsedAgents.filter(a=>a.errors.length===0);
  if(valid.length === 0){ showToast('No valid agents to import','error'); return; }
  valid.forEach(a=>{
    teamAgents.push({
      id: genId(),
      name: a.name,
      email: a.email,
      phone: a.phone,
      team: a.team,
      channel: a.channel==='Phone'?'📞 Phone': a.channel==='Chat'?'💬 Chat':'📧 Email',
      status: a.status
    });
  });
  renderTeamAgents();
  closeMemberModal();
  showToast(`✓ ${valid.length} agent${valid.length!==1?'s':''} imported successfully!`);
  clearBulkPreview();
  // Also update sidebar agent count
  const skVal = document.querySelector('.sk-val.green');
  if(skVal) skVal.textContent = teamAgents.length;
}

// ╔══════════════════════════════════════════════════════════════╗
// ║              WORKSPACE SYSTEM — FULLY ISOLATED              ║
// ║  Each workspace has its own: agents, specialists,           ║
// ║  scorecard, evaluations, disputes, users, roles, config     ║
// ╚══════════════════════════════════════════════════════════════╝

// Workspace types
const WS_TYPES = {
  telesales:    { label:'Telesales',    icon:'📞', color:'var(--accent)'  },
  confirmation: { label:'Confirmation', icon:'✅', color:'var(--green)'   },
  whatsapp:     { label:'WhatsApp',     icon:'💬', color:'var(--accent2)' },
};

// Helper — generate seeded evals for a workspace
function makeWsEvals(agentNames, count, wsId){
  const list=[], specs=DB.specialists.map(s=>s.name);
  const channels=['📞 Phone','💬 Chat','📧 Email'];
  const now = new Date();
  for(let i=0;i<count;i++){
    const score=randInt(45,100);
    const st=score>=80?'Pass':score>=70?(Math.random()>.5?'Pending':'Fail'):'Fail';
    // Real ISO date spread over last 90 days
    const daysAgo=randInt(0,89);
    const d=new Date(now); d.setDate(now.getDate()-daysAgo);
    const isoDate=d.toISOString();
    const displayDate=d.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'});
    // Score breakdown for category metrics
    const softScore   = randInt(60,100);
    const salesScore  = randInt(40,100);
    const docScore    = randInt(65,100);
    const perfScore   = randInt(75,100);
    const salesMistakes = salesScore < 70 ? randInt(1,5) : 0;
    list.push({
      id:`${wsId.toUpperCase().replace(/-/g,'').slice(0,8)}-${String(i+1).padStart(4,'0')}`,
      agent:randItem(agentNames.length?agentNames:['—']),
      specialist:randItem(specs),
      channel:randItem(channels),
      score, status:st,
      date:displayDate, createdAt:isoDate,
      workspace:wsId,
      softScore, salesScore, docScore, perfScore, salesMistakes
    });
  }
  return list;
}

// KSA agents split by workspace
const _ksaAgents = DB.agents.map(a=>a.name);
const _ksaTelesales    = _ksaAgents.slice(0,20);
const _ksaConfirmation = _ksaAgents.slice(20);

// ── WORKSPACE DATA ──
DB.workspaces = [
  {id:'ws-global',name:'Global View',type:'telesales',icon:'🌐',color:'var(--accent)',desc:'Combined view of all workspaces',region:'All Regions',manager:'System Admin',status:'active',isGlobal:true,createdAt:'Jan 2026',config:{target:80,evalTarget:1419,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:[],specialists:[],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:70.44,evals:1304,passRate:72,disputes:3}},
  {id:'ksa-telesales',name:'KSA Telesales',type:'telesales',icon:'🇸🇦',color:'var(--accent)',desc:'KSA Telesales — outbound sales calls',region:'Saudi Arabia',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Jan 2026',config:{target:80,evalTarget:300,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:_ksaTelesales,specialists:['Sara Ahmed','Mohamed Karim'],users:['admin','qa.manager','sara.ahmed'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:78.2,evals:287,passRate:71,disputes:2}},
  {id:'ksa-confirmation',name:'KSA Confirmation',type:'confirmation',icon:'🇸🇦',color:'var(--green)',desc:'KSA order confirmation calls',region:'Saudi Arabia',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Jan 2026',config:{target:82,evalTarget:250,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:_ksaConfirmation,specialists:['Lina Rashid','Omar Tarek'],users:['admin','qa.manager','sara.ahmed'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:74.1,evals:198,passRate:68,disputes:1}},
  {id:'egypt-telesales',name:'Egypt Telesales',type:'telesales',icon:'🇪🇬',color:'var(--purple)',desc:'Egypt Telesales — outbound sales calls',region:'Egypt',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Jan 2026',config:{target:80,evalTarget:350,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:[],specialists:['Sara Ahmed','Rania Fouad'],users:['admin','qa.manager','mk'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:76.3,evals:312,passRate:70,disputes:4}},
  {id:'egypt-confirmation',name:'Egypt Confirmation',type:'confirmation',icon:'🇪🇬',color:'var(--accent2)',desc:'Egypt order confirmation calls',region:'Egypt',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Jan 2026',config:{target:80,evalTarget:280,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:[],specialists:['Mohamed Karim','Lina Rashid'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:72.8,evals:241,passRate:66,disputes:3}},
  {id:'iraq-telesales',name:'Iraq Telesales',type:'telesales',icon:'🇮🇶',color:'var(--accent3)',desc:'Iraq Telesales — outbound sales calls',region:'Iraq',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Feb 2026',config:{target:78,evalTarget:200,passThreshold:78,excellentThreshold:88,disputeWindow:48},agents:[],specialists:['Omar Tarek'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:69.4,evals:178,passRate:62,disputes:5}},
  {id:'iraq-confirmation',name:'Iraq Confirmation',type:'confirmation',icon:'🇮🇶',color:'var(--orange)',desc:'Iraq order confirmation calls',region:'Iraq',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Feb 2026',config:{target:78,evalTarget:160,passThreshold:78,excellentThreshold:88,disputeWindow:48},agents:[],specialists:['Khaled Mostafa'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:67.2,evals:143,passRate:58,disputes:4}},
  {id:'gcc-telesales',name:'GCC Telesales',type:'telesales',icon:'🌍',color:'var(--danger)',desc:'GCC Telesales — UAE, Kuwait, Qatar, Bahrain',region:'GCC',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Feb 2026',config:{target:82,evalTarget:180,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:[],specialists:['Rania Fouad'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:80.1,evals:162,passRate:73,disputes:2}},
  {id:'gcc-confirmation',name:'GCC Confirmation',type:'confirmation',icon:'🌍',color:'var(--purple)',desc:'GCC order confirmation — UAE, Kuwait, Qatar, Bahrain',region:'GCC',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Feb 2026',config:{target:82,evalTarget:140,passThreshold:80,excellentThreshold:90,disputeWindow:48},agents:[],specialists:['Sara Ahmed'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:77.9,evals:119,passRate:70,disputes:1}},
  {id:'whatsapp',name:'WhatsApp',type:'whatsapp',icon:'💬',color:'var(--accent2)',desc:'WhatsApp channel quality assurance',region:'All Regions',manager:'QA Manager',status:'active',isGlobal:false,createdAt:'Mar 2026',config:{target:80,evalTarget:120,passThreshold:80,excellentThreshold:90,disputeWindow:24},agents:[],specialists:['Lina Rashid'],users:['admin','qa.manager'],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:82.4,evals:104,passRate:75,disputes:1}},
];

// Seed each workspace with evaluations
DB.workspaces.filter(w=>!w.isGlobal).forEach(ws=>{
  const names=ws.agents.length?ws.agents:_agentNames.slice(0,5);
  ws.evaluations=makeWsEvals(names,Math.min(ws.stats.evals,40),ws.id);
});

// ── STATE ──
let activeWorkspaceId='ws-global';
function getActiveWs(){return DB.workspaces.find(w=>w.id===activeWorkspaceId)||DB.workspaces[0];}
function getWsById(id){return DB.workspaces.find(w=>w.id===id);}
function getActiveEvals(){
  const ws=getActiveWs();
  if(!ws.isGlobal) return (ws.evaluations||[]).slice().sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0));
  // Global: aggregate ALL workspace evals
  const seen=new Set(), all=[];
  DB.workspaces.filter(w=>!w.isGlobal).forEach(w=>{
    (w.evaluations||[]).forEach(e=>{ if(!seen.has(e.id)){seen.add(e.id);all.push(e);} });
  });
  (DB.evaluations||[]).forEach(e=>{ if(!seen.has(e.id)){seen.add(e.id);all.push(e);} });
  return all.sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0));
}
function getActiveAgents(){const ws=getActiveWs();if(ws.isGlobal)return DB.agents;if(!ws.agents.length)return DB.agents;return DB.agents.filter(a=>ws.agents.includes(a.name));}

// ── SWITCHER ──
function toggleWsPanel(){const p=document.getElementById('ws-panel');if(p){p.classList.toggle('open');if(p.classList.contains('open'))renderWsList();}}
function closeWsPanel(){const p=document.getElementById('ws-panel');if(p)p.classList.remove('open');}

function renderWsList(){
  const list=document.getElementById('ws-list');
  if(!list)return;
  const regions=[
    {label:'🌐 Global',  ids:['ws-global']},
    {label:'🇸🇦 KSA',    ids:['ksa-telesales','ksa-confirmation']},
    {label:'🇪🇬 Egypt',  ids:['egypt-telesales','egypt-confirmation']},
    {label:'🇮🇶 Iraq',   ids:['iraq-telesales','iraq-confirmation']},
    {label:'🌍 GCC',     ids:['gcc-telesales','gcc-confirmation']},
    {label:'💬 Digital', ids:['whatsapp']},
  ];
  list.innerHTML=regions.map(r=>`
    <div style="padding:6px 12px 2px;font-size:9px;color:var(--text2);font-family:'DM Mono',monospace;text-transform:uppercase;letter-spacing:1px;border-top:1px solid var(--border);">${r.label}</div>
    ${r.ids.map(id=>{
      const ws=DB.workspaces.find(w=>w.id===id);if(!ws)return'';
      const active=activeWorkspaceId===ws.id;
      return`<div class="ws-item ${active?'active':''}" onclick="switchWorkspace('${ws.id}')">
        <div class="ws-item-icon" style="background:${ws.color}22;">${ws.icon}</div>
        <div style="flex:1;min-width:0;"><div class="ws-item-name">${ws.name}</div><div class="ws-item-meta">${ws.isGlobal?'All workspaces':WS_TYPES[ws.type]?.label+' · '+ws.region}</div></div>
        <div style="text-align:right;">${active?'<span style="color:var(--accent);">✓</span>':''}<div style="font-size:10px;color:var(--text2);font-family:\'DM Mono\',monospace;">${ws.stats.avgScore}%</div></div>
      </div>`;
    }).join('')}`).join('');
  const editBtn=document.getElementById('ws-edit-btn');
  if(editBtn){const ws=getActiveWs();editBtn.style.display=ws.isGlobal?'none':'flex';editBtn.textContent=ws.isGlobal?'':'✏️ Edit "'+ws.name+'"';}
}

function switchWorkspace(id){
  activeWorkspaceId=id;
  if(window._fbSubscribeEvals) window._fbSubscribeEvals(id);
  const ws=getActiveWs();
  closeWsPanel();
  // Workspace is kept in memory for Firebase-auth sessions.
  document.getElementById('topbar-ws-name').textContent=ws.name;
  document.getElementById('topbar-ws-dot').style.background=ws.color;
  document.getElementById('topbar-ws-dot').style.boxShadow=`0 0 6px ${ws.color}`;
  const editBtn=document.getElementById('topbar-ws-edit-btn');
  if(editBtn){editBtn.style.display=ws.isGlobal?'none':'flex';}
  const badge=document.getElementById('page-title-badge');
  if(badge)badge.textContent=ws.isGlobal?'March 2026':ws.name;
  const agents=getActiveAgents(),specs=ws.isGlobal?DB.specialists:DB.specialists.filter(s=>ws.specialists.includes(s.name));
  const qaVal=document.querySelector('.sk-val'),agVal=document.querySelector('.sk-val.green');
  if(qaVal)qaVal.textContent=specs.length||DB.specialists.length;
  if(agVal)agVal.textContent=agents.length||DB.agents.length;
  renderRecentEvals();renderTracker();renderAgents();renderDashboard();
  showToast(`Switched to: ${ws.icon} ${ws.name}`);
}

// ── WORKSPACE CARDS PAGE ──
function renderWorkspacesPage(){
  const grid=document.getElementById('ws-cards-grid');if(!grid)return;
  const q=(document.getElementById('ws-search')||{value:''}).value.toLowerCase();
  const typeF=(document.getElementById('ws-filter-type')||{value:''}).value;
  const filtered=DB.workspaces.filter(w=>!w.isGlobal&&(!q||w.name.toLowerCase().includes(q)||(w.region||'').toLowerCase().includes(q))&&(!typeF||w.type===typeF));
  if(!filtered.length){grid.innerHTML='<div class="empty-state" style="grid-column:1/-1;"><div class="empty-icon">🏢</div><div class="empty-title">No workspaces found</div></div>';return;}
  grid.innerHTML=filtered.map(ws=>`
    <div class="ws-card">
      <div class="ws-card-icon" style="background:${ws.color}22;">${ws.icon}</div>
      <div style="flex:1;min-width:0;">
        <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:8px;margin-bottom:4px;">
          <div><div class="ws-card-name">${ws.name}</div><div class="ws-card-type" style="color:${ws.color};">${WS_TYPES[ws.type]?.label||ws.type} · ${ws.region}</div></div>
          <span class="status-badge ${ws.status==='active'?'status-pass':'status-fail'}">${ws.status}</span>
        </div>
        <div style="font-size:11px;color:var(--text2);margin-bottom:6px;">${ws.desc}</div>
        <div style="font-size:11px;color:var(--text2);display:flex;gap:10px;flex-wrap:wrap;"><span>👤 ${ws.manager}</span><span>🎯 ${ws.config.target}% target</span><span>👥 ${ws.agents.length||DB.agents.length} agents</span></div>
        <div class="ws-card-stats">
          <div class="ws-card-stat"><div class="ws-card-stat-val" style="color:${ws.color};">${ws.stats.avgScore}%</div><div class="ws-card-stat-lbl">Avg Score</div></div>
          <div class="ws-card-stat"><div class="ws-card-stat-val" style="color:var(--accent);">${ws.stats.evals}</div><div class="ws-card-stat-lbl">Evals</div></div>
          <div class="ws-card-stat"><div class="ws-card-stat-val" style="color:var(--green);">${ws.stats.passRate}%</div><div class="ws-card-stat-lbl">Pass Rate</div></div>
        </div>
        <div style="display:flex;gap:6px;margin-top:12px;flex-wrap:wrap;">
          <button class="btn btn-primary btn-xs" onclick="switchWorkspace('${ws.id}');showPage('dashboard',null)">📊 Dashboard</button>
          <button class="btn btn-ghost btn-xs" onclick="openEditWorkspace('${ws.id}')">✏️ Edit</button>
          <button class="btn btn-ghost btn-xs" onclick="switchWorkspace('${ws.id}');showPage('tracker',null)">📈 Tracker</button>
          <button class="btn btn-danger btn-xs" onclick="deleteWorkspace('${ws.id}')">🗑</button>
        </div>
      </div>
    </div>`).join('');
}

// ── CROSS-WORKSPACE OVERVIEW ──
function renderWsOverview(){
  const wsData=DB.workspaces.filter(w=>!w.isGlobal);
  const kpis=document.getElementById('ws-overview-kpis');
  if(kpis){
    const avgScore=(wsData.reduce((s,w)=>s+w.stats.avgScore,0)/wsData.length).toFixed(1);
    const totalEvals=wsData.reduce((s,w)=>s+w.stats.evals,0);
    const totalDisputes=wsData.reduce((s,w)=>s+w.stats.disputes,0);
    kpis.innerHTML=`<div class="kpi-card blue"><span class="kpi-icon">🏢</span><div class="kpi-label">Workspaces</div><div class="kpi-value">${wsData.length}</div><div class="kpi-sub">${wsData.filter(w=>w.status==='active').length} active</div></div><div class="kpi-card green"><span class="kpi-icon">📊</span><div class="kpi-label">Avg Score</div><div class="kpi-value">${avgScore}<span style="font-size:14px">%</span></div></div><div class="kpi-card orange"><span class="kpi-icon">✅</span><div class="kpi-label">Total Evals</div><div class="kpi-value">${totalEvals.toLocaleString()}</div></div><div class="kpi-card teal"><span class="kpi-icon">🚩</span><div class="kpi-label">Disputes</div><div class="kpi-value">${totalDisputes}</div></div>`;
  }
  const bars=document.getElementById('ws-score-bars');
  if(bars){
    bars.innerHTML=[...wsData].sort((a,b)=>b.stats.avgScore-a.stats.avgScore).map(ws=>`
      <div><div style="display:flex;justify-content:space-between;margin-bottom:5px;font-size:12px;"><span>${ws.icon} ${ws.name}</span><span style="font-family:'DM Mono',monospace;color:${ws.color};">${ws.stats.avgScore}%</span></div>
      <div style="position:relative;"><div class="prog-bar prog-bar-thick"><div class="prog-fill" style="width:${ws.stats.avgScore}%;background:${ws.color};"></div></div><div style="position:absolute;top:0;left:${ws.config.target}%;width:2px;height:100%;background:var(--danger);opacity:0.6;"></div></div></div>`).join('');
  }
  const tbody=document.getElementById('ws-leaderboard-tbody');
  if(tbody){
    tbody.innerHTML=[...wsData].sort((a,b)=>b.stats.avgScore-a.stats.avgScore).map(ws=>`
      <tr>
        <td><div style="display:flex;align-items:center;gap:8px;"><div style="width:24px;height:24px;border-radius:6px;background:${ws.color}22;display:flex;align-items:center;justify-content:center;">${ws.icon}</div><div><div style="font-weight:600;font-size:13px;">${ws.name}</div><div style="font-size:10px;color:var(--text2);">${ws.region}</div></div></div></td>
        <td><span style="font-size:10px;padding:2px 7px;border-radius:4px;background:${ws.color}22;color:${ws.color};font-family:'DM Mono',monospace;">${WS_TYPES[ws.type]?.label||ws.type}</span></td>
        <td style="font-family:'DM Mono',monospace;font-size:12px;">${ws.agents.length||DB.agents.length}</td>
        <td style="font-family:'DM Mono',monospace;font-size:12px;">${ws.stats.evals}</td>
        <td><span class="score-badge ${scoreClass(ws.stats.avgScore)}">${ws.stats.avgScore}%</span></td>
        <td><span class="score-badge ${scoreClass(ws.stats.passRate)}">${ws.stats.passRate}%</span></td>
        <td style="font-family:'DM Mono',monospace;font-size:12px;color:${ws.stats.disputes>3?'var(--danger)':'var(--text2)'};">${ws.stats.disputes}</td>
        <td><span class="status-badge ${ws.status==='active'?'status-pass':'status-fail'}">${ws.status}</span>&nbsp;<button class="btn btn-ghost btn-xs" onclick="switchWorkspace('${ws.id}');showPage('dashboard',null)">Open →</button></td>
      </tr>`).join('');
  }
  setTimeout(drawWsEvalsChart,50);
}

function drawWsEvalsChart(){
  const canvas=document.getElementById('ws-evals-chart');if(!canvas)return;
  const ctx=canvas.getContext('2d');const w=canvas.offsetWidth,h=180;
  canvas.width=w*2;canvas.height=h*2;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.scale(2,2);
  const data=DB.workspaces.filter(ws=>!ws.isGlobal);if(!data.length)return;
  const maxVal=Math.max(...data.map(d=>d.stats.evals),1);
  const pad={t:20,r:10,b:32,l:10},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b;
  ctx.clearRect(0,0,w,h);
  const cmap={'var(--accent)':'#4f8ef7','var(--green)':'#3dd68c','var(--purple)':'#a78bfa','var(--accent2)':'#00e5c3','var(--accent3)':'#f7b94f','var(--danger)':'#f75f5f','var(--orange)':'#f79c4f'};
  const step=cw/data.length,bw=step*0.55;
  data.forEach((ws,i)=>{
    const x=pad.l+i*step+(step-bw)/2,bh=(ws.stats.evals/maxVal)*ch,y=pad.t+ch-bh;
    ctx.fillStyle=cmap[ws.color]||'#4f8ef7';ctx.globalAlpha=0.85;
    ctx.beginPath();if(ctx.roundRect)ctx.roundRect(x,y,bw,bh,3);else ctx.rect(x,y,bw,bh);ctx.fill();ctx.globalAlpha=1;
    ctx.fillStyle='rgba(226,232,246,0.9)';ctx.font='7px DM Mono';ctx.textAlign='center';ctx.fillText(ws.stats.evals,x+bw/2,y-4);
    ctx.fillStyle='rgba(107,122,154,0.8)';ctx.font='7px DM Sans';ctx.textAlign='center';
    ctx.fillText(ws.name.replace(' Telesales',' TS').replace(' Confirmation',' CF'),x+bw/2,h-8);
  });
}

// ── CRUD ──
let editingWsId=null;
function editCurrentWorkspace(){closeWsPanel();const ws=getActiveWs();if(ws.isGlobal){showPage('workspaces',null);setTimeout(()=>showToast('Select a workspace to edit'),300);return;}openEditWorkspace(ws.id);}
function openAddWorkspace(){editingWsId=null;document.getElementById('ws-modal-title').textContent='New Workspace';['wm-name','wm-desc','wm-region','wm-manager'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});document.getElementById('wm-type').value='telesales';document.getElementById('wm-icon').value='🏢';document.getElementById('wm-color').value='var(--accent)';document.getElementById('wm-target').value=80;document.getElementById('wm-eval-target').value=200;document.getElementById('wm-status').value='active';populateWsSelects(null);document.getElementById('ws-modal').classList.add('open');}
function openEditWorkspace(id){editingWsId=id;const ws=getWsById(id);if(!ws)return;document.getElementById('ws-modal-title').textContent='Edit: '+ws.name;document.getElementById('wm-name').value=ws.name;document.getElementById('wm-type').value=ws.type;document.getElementById('wm-icon').value=ws.icon;document.getElementById('wm-color').value=ws.color;document.getElementById('wm-desc').value=ws.desc||'';document.getElementById('wm-region').value=ws.region||'';document.getElementById('wm-manager').value=ws.manager||'';document.getElementById('wm-target').value=ws.config.target;document.getElementById('wm-eval-target').value=ws.config.evalTarget;document.getElementById('wm-status').value=ws.status;populateWsSelects(ws);document.getElementById('ws-modal').classList.add('open');}
function populateWsSelects(ws){const as=document.getElementById('wm-agents'),ss=document.getElementById('wm-specs');if(as)as.innerHTML=DB.agents.map(a=>`<option value="${a.name}" ${ws&&ws.agents.includes(a.name)?'selected':''}>${a.name} (${a.empId||''})</option>`).join('');if(ss)ss.innerHTML=DB.specialists.map(s=>`<option value="${s.name}" ${ws&&ws.specialists.includes(s.name)?'selected':''}>${s.name}</option>`).join('');}
function saveWorkspace(){
  const name=document.getElementById('wm-name').value.trim();if(!name){showToast('Please enter a name','error');return;}
  const as=document.getElementById('wm-agents'),ss=document.getElementById('wm-specs');
  const selA=as?Array.from(as.selectedOptions).map(o=>o.value):[];
  const selS=ss?Array.from(ss.selectedOptions).map(o=>o.value):[];
  const cfg={target:parseInt(document.getElementById('wm-target').value)||80,evalTarget:parseInt(document.getElementById('wm-eval-target').value)||200,passThreshold:80,excellentThreshold:90,disputeWindow:48};
  if(editingWsId){
    const ws=getWsById(editingWsId);
    if(ws){Object.assign(ws,{name,type:document.getElementById('wm-type').value,icon:document.getElementById('wm-icon').value||'🏢',color:document.getElementById('wm-color').value,desc:document.getElementById('wm-desc').value,region:document.getElementById('wm-region').value,manager:document.getElementById('wm-manager').value,status:document.getElementById('wm-status').value,config:cfg});if(selA.length){ws.agents=selA;ws.evaluations=makeWsEvals(selA,Math.min(ws.stats.evals,40),ws.id);}if(selS.length)ws.specialists=selS;}
    showToast('Workspace updated: '+name);
  } else {
    const nw={id:'ws-'+genId(),name,type:document.getElementById('wm-type').value,icon:document.getElementById('wm-icon').value||'🏢',color:document.getElementById('wm-color').value,desc:document.getElementById('wm-desc').value,region:document.getElementById('wm-region').value,manager:document.getElementById('wm-manager').value,status:document.getElementById('wm-status').value,isGlobal:false,createdAt:new Date().toLocaleDateString('en-US',{month:'short',year:'numeric'}),config:cfg,agents:selA,specialists:selS,users:[],scorecard:null,evaluations:[],disputes:[],feedback:[],calibrations:[],stats:{avgScore:0,evals:0,passRate:0,disputes:0}};
    DB.workspaces.push(nw);showToast('Workspace created: '+name);
  }
  closeWsModal();renderWorkspacesPage();renderWsList();
}
function deleteWorkspace(id){const ws=getWsById(id);if(!ws||ws.isGlobal){showToast('Cannot delete','error');return;}if(!confirm('Delete "'+ws.name+'"?'))return;DB.workspaces=DB.workspaces.filter(w=>w.id!==id);if(activeWorkspaceId===id)switchWorkspace('ws-global');renderWorkspacesPage();renderWsList();showToast('Workspace deleted');}
function closeWsModal(){document.getElementById('ws-modal').classList.remove('open');}
document.addEventListener('click',function(e){const p=document.getElementById('ws-panel');if(p&&p.classList.contains('open')&&!e.target.closest('#topbar-ws-wrap'))p.classList.remove('open');});

pageTitles['ws-overview']='Cross-Workspace Analytics';
pageBadges['workspaces']='Admin';
pageBadges['ws-overview']='Analytics';

// ═══ INIT ═══
initScorecard();
renderDashboard();
renderRecentEvals();
renderTracker();
renderAgents();
renderSpecialists();
renderScBuilder();
renderTeamAgents();
renderTeamSpecs();
updateSpecialistDropdowns();
renderWsList();
// Init scorecard page selects with live data
setTimeout(initScorecardPage, 100);
['workspaces','ws-overview'].forEach(p=>{
  if(DB.roles['admin']&&!DB.roles['admin'].pages.includes(p)) DB.roles['admin'].pages.push(p);
  if(DB.roles['qa-manager']&&!DB.roles['qa-manager'].pages.includes(p)) DB.roles['qa-manager'].pages.push(p);
});
// Hide everything first
document.getElementById('login-screen').style.display='none';
document.querySelector('.sidebar').style.display='none';
document.querySelector('.main').style.display='none';
// Try restoring session, else show login
(function(){
  try{
    const saved=localStorage.getItem('qms-session');
    if(!saved){document.getElementById('login-screen').style.display='flex';return;}
    const parsed=JSON.parse(saved);
    if(!parsed||!parsed.username){document.getElementById('login-screen').style.display='flex';return;}
    const user=DB.users.find(u=>u.username===parsed.username);
    if(!user){localStorage.removeItem('qms-session');document.getElementById('login-screen').style.display='flex';return;}
    currentUser=user;
    if(parsed.workspaceId){const ws=DB.workspaces.find(w=>w.id===parsed.workspaceId);if(ws)activeWorkspaceId=parsed.workspaceId;}
    document.querySelector('.sidebar').style.display='flex';
    document.querySelector('.main').style.display='flex';
    applyRoleUI();
    renderWsList();
    const ws=getActiveWs();
    document.getElementById('topbar-ws-name').textContent=ws.name;
    document.getElementById('topbar-ws-dot').style.background=ws.color;
    const role=ROLES[user.role];
    const firstPage=role&&role.pages[0]?role.pages[0]:'dashboard';
    showPage(firstPage,null);
  }catch(e){
    console.warn('Session restore error:',e);
    localStorage.removeItem('qms-session');
    document.getElementById('login-screen').style.display='flex';
  }
})();
setTimeout(drawTrendChart,300);
try { connectFirebase(); } catch(e){ console.warn("Firebase not configured yet:", e.message); }
</script>
</body>
</html>




